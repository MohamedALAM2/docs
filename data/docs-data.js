window.CRIMELENS_DOCS = {"project": {"name": "CrimeLens", "tagline": "AI-assisted Computer-Aided Dispatch documentation", "version": "Enterprise Static Docs", "updated": "23 June 2026"}, "nav": [{"title": "Getting Started", "icon": "rocket", "items": [{"slug": "overview", "title": "Overview"}, {"slug": "architecture", "title": "System Architecture"}, {"slug": "modules", "title": "Modules"}, {"slug": "installation", "title": "Installation & Setup"}]}, {"title": "Concepts", "icon": "layers", "items": [{"slug": "actors", "title": "Actors & Roles"}, {"slug": "workflow", "title": "End-to-End Workflow"}, {"slug": "security", "title": "Authentication & Security"}, {"slug": "data-model", "title": "Data Model"}]}, {"title": "Features", "icon": "sparkles", "items": [{"slug": "dispatch", "title": "Incident & Dispatch"}, {"slug": "priority-engine", "title": "Priority Engine"}, {"slug": "citizen-tips", "title": "Citizen Tips"}, {"slug": "cameras", "title": "Cameras & Streaming"}, {"slug": "ai-integration", "title": "AI Model Integration"}, {"slug": "crime-lifecycle", "title": "Crime Lifecycle & Field Ops"}, {"slug": "realtime", "title": "Real-Time & Notifications"}]}, {"title": "Reference", "icon": "book", "items": [{"slug": "api", "title": "API Reference"}, {"slug": "jobs", "title": "Background Jobs & Scheduling"}, {"slug": "testing", "title": "Testing"}, {"slug": "glossary", "title": "Glossary"}]}], "docs": [{"slug": "overview", "title": "Overview", "section": "Getting Started", "description": "Overview What is CrimeLens? what is crimelens The Problem the problem The Approach: AI Originated CAD the approach Surfaces surfaces Key Capabilities key capabilities Technology Stack tech s…", "raw": "# Overview\n\n---\n\n- [What is CrimeLens?](#what-is-crimelens)\n- [The Problem](#the-problem)\n- [The Approach: AI-Originated CAD](#the-approach)\n- [Surfaces](#surfaces)\n- [Key Capabilities](#key-capabilities)\n- [Technology Stack](#tech-stack)\n\n<a name=\"what-is-crimelens\"></a>\n## What is CrimeLens?\n\n**CrimeLens** is an AI-assisted **Computer-Aided Dispatch (CAD)** platform for law enforcement. It connects surveillance cameras to AI detection services, turns their detections into reviewable **incidents**, and routes confirmed crimes to field officers. Human dispatcher review is the default, with a deliberately narrow optional auto-dispatch policy.\n\nWhere traditional CAD systems start from a phone call, CrimeLens starts from an **AI detection** and carries a defensible, audited trail from the moment a camera sees something until an officer closes the case.\n\n> {primary} CrimeLens is a graduation project built as a production-grade reference implementation. It is intentionally architected like a real CAD product: multi-tenant, multi-guard, event-driven, and observable.\n\n<a name=\"the-problem\"></a>\n## The Problem\n\nManual, radio-driven police response has three structural weaknesses:\n\n1. **Detection is reactive.** Incidents are only known once a citizen calls them in — minutes after they start.\n2. **Dispatch is opaque.** Decisions live in a dispatcher's head and on a radio log; there is no machine-readable, replayable record of *why* an officer was sent.\n3. **AI is untrusted.** Pure \"AI auto-dispatch\" is dangerous and politically unacceptable — a false positive sends armed officers to an innocent person.\n\nCrimeLens addresses all three: proactive AI detection, a fully audited decision ledger, and a mandatory **human-in-the-loop** review layer before any officer is dispatched.\n\n<a name=\"the-approach\"></a>\n## The Approach: AI-Originated CAD\n\nThe core design principle is the **incident layer**. AI never creates a crime or commands a camera directly. Instead:\n\n```\nAI detection  ─▶  Incident (pending_review)  ─▶  Dispatcher review  ─▶  Crime  ─▶  Officer\n                       │                                                  ▲\n                       └── Priority Engine scores it ─────────────────────┘\n```\n\n- The **AI service** only *reports* — it raises alerts and confirmed detections through an authenticated API.\n- The **Priority Engine** scores every incident with a transparent, auditable weighted formula.\n- A **human dispatcher** reviews the incident on a live console and decides to **dispatch** (creating a crime + assigning an officer) or **reject** it as a false alarm.\n- Only the **backend** commands cameras (alarms, PTZ) — never the AI and never the field officer.\n\nSee [End-to-End Workflow](/{{route}}/{{version}}/workflow) for the full lifecycle.\n\n<a name=\"surfaces\"></a>\n## Surfaces\n\nCrimeLens exposes three distinct user surfaces, each tuned to its operator:\n\n| Surface | Audience | Stack |\n|---------|----------|-------|\n| **Admin Console** | System administrators | Web (Inertia + React) |\n| **Station / Dispatcher Console** | Police-station operators & dispatchers | Web (Inertia + React) |\n| **Officer App** | Field officers | Mobile (Flutter) + API |\n\nTwo non-human surfaces complete the system: the **AI Detection Service** (API client) and the **Camera Gateway** (RTSP fan-out service).\n\n<a name=\"key-capabilities\"></a>\n## Key Capabilities\n\n- **Dispatcher Console** — a tri-pane operations screen (live queue · map · incident detail) with claim/release, manual incident creation, and live updates over WebSockets.\n- **Priority Engine** — weighted, explainable incident scoring into Critical / High / Medium / Low tiers.\n- **Citizen Tips** — public web + SMS (Twilio) intake, with dispatcher triage to promote a tip into an incident.\n- **Camera Streaming** — a Python gateway fans a single RTSP feed into HLS (mobile), WebRTC (web), and raw RTSP (AI), plus on-demand evidence recording.\n- **AI Integration** — IP-whitelisted, per-session AES-256 encrypted camera credentials, heartbeat monitoring, alert & crime reporting.\n- **Field Operations** — officer assignment by proximity, escalation on no-response, GPS tracking, panic/SOS, BOLO broadcasts, patrol zones.\n- **Pattern Detection** — cross-incident weapon-cluster alerts.\n- **Decision Ledger** — an append-only, hash-chained audit trail of every consequential action.\n- **Real-Time** — Laravel Echo / Pusher channels for the web consoles; Firebase Cloud Messaging for mobile.\n\n<a name=\"tech-stack\"></a>\n## Technology Stack\n\n| Layer | Technology |\n|-------|-----------|\n| Backend | PHP 8.4, Laravel 13 |\n| Modularity | `nwidart/laravel-modules` (6 modules) |\n| Web frontend | Inertia.js v3 + React 19, Tailwind CSS v4, Vite |\n| Maps & video | MapLibre GL, HLS.js |\n| Auth | Laravel Sanctum (multi-guard) |\n| Database | PostgreSQL + PostGIS |\n| Cache / Queue / Presence | Redis |\n| Queues & monitoring | Laravel Horizon |\n| Real-time | Pusher protocol (Laravel Echo) |\n| Push notifications | Firebase Cloud Messaging (`kreait/laravel-firebase`) |\n| SMS / voice intake | Twilio |\n| RBAC | `spatie/laravel-permission` |\n| DTOs | `spatie/laravel-data` |\n| Reports | `barryvdh/laravel-dompdf`, `maatwebsite/excel` |\n| Camera gateway | Python (Flask + FFmpeg + MediaMTX) |\n| Testing | Pest (PHP), Playwright (E2E) |\n| Observability | Laravel Telescope, Pail |\n\nContinue to [System Architecture](/{{route}}/{{version}}/architecture).\n", "html": "<h1 id=\"overview\"><a class=\"anchor\" href=\"#overview\">#</a>Overview</h1>\n<hr>\n<ul>\n<li><a href=\"#what-is-crimelens\">What is CrimeLens?</a></li>\n<li><a href=\"#the-problem\">The Problem</a></li>\n<li><a href=\"#the-approach\">The Approach: AI-Originated CAD</a></li>\n<li><a href=\"#surfaces\">Surfaces</a></li>\n<li><a href=\"#key-capabilities\">Key Capabilities</a></li>\n<li><a href=\"#tech-stack\">Technology Stack</a></li>\n</ul>\n<h2 id=\"what-is-crimelens\"><a class=\"anchor\" href=\"#what-is-crimelens\">#</a>What is CrimeLens?</h2>\n<p><strong>CrimeLens</strong> is an AI-assisted <strong>Computer-Aided Dispatch (CAD)</strong> platform for law enforcement. It connects surveillance cameras to AI detection services, turns their detections into reviewable <strong>incidents</strong>, and routes confirmed crimes to field officers. Human dispatcher review is the default, with a deliberately narrow optional auto-dispatch policy.</p>\n<p>Where traditional CAD systems start from a phone call, CrimeLens starts from an <strong>AI detection</strong> and carries a defensible, audited trail from the moment a camera sees something until an officer closes the case.</p>\n<aside class=\"callout primary\"><div class=\"callout-icon\">✦</div><div><strong>Design note</strong><p>CrimeLens is a graduation project built as a production-grade reference implementation. It is intentionally architected like a real CAD product: multi-tenant, multi-guard, event-driven, and observable.</p></div></aside>\n<h2 id=\"the-problem\"><a class=\"anchor\" href=\"#the-problem\">#</a>The Problem</h2>\n<p>Manual, radio-driven police response has three structural weaknesses:</p>\n<ol>\n<li><strong>Detection is reactive.</strong> Incidents are only known once a citizen calls them in — minutes after they start.</li>\n<li><strong>Dispatch is opaque.</strong> Decisions live in a dispatcher's head and on a radio log; there is no machine-readable, replayable record of <em>why</em> an officer was sent.</li>\n<li><strong>AI is untrusted.</strong> Pure \"AI auto-dispatch\" is dangerous and politically unacceptable — a false positive sends armed officers to an innocent person.</li>\n</ol>\n<p>CrimeLens addresses all three: proactive AI detection, a fully audited decision ledger, and a mandatory <strong>human-in-the-loop</strong> review layer before any officer is dispatched.</p>\n<h2 id=\"the-approach\"><a class=\"anchor\" href=\"#the-approach\">#</a>The Approach: AI-Originated CAD</h2>\n<p>The core design principle is the <strong>incident layer</strong>. AI never creates a crime or commands a camera directly. Instead:</p>\n<div class=\"code-card\"><div class=\"code-toolbar\"><span>text</span><button class=\"copy-btn\" type=\"button\">Copy</button></div><pre><code class=\"language-text\">AI detection  ─▶  Incident (pending_review)  ─▶  Dispatcher review  ─▶  Crime  ─▶  Officer\n                       │                                                  ▲\n                       └── Priority Engine scores it ─────────────────────┘</code></pre></div>\n<ul>\n<li>The <strong>AI service</strong> only <em>reports</em> — it raises alerts and confirmed detections through an authenticated API.</li>\n<li>The <strong>Priority Engine</strong> scores every incident with a transparent, auditable weighted formula.</li>\n<li>A <strong>human dispatcher</strong> reviews the incident on a live console and decides to <strong>dispatch</strong> (creating a crime + assigning an officer) or <strong>reject</strong> it as a false alarm.</li>\n<li>Only the <strong>backend</strong> commands cameras (alarms, PTZ) — never the AI and never the field officer.</li>\n</ul>\n<p>See <a href=\"#/docs/workflow\">End-to-End Workflow</a> for the full lifecycle.</p>\n<h2 id=\"surfaces\"><a class=\"anchor\" href=\"#surfaces\">#</a>Surfaces</h2>\n<p>CrimeLens exposes three distinct user surfaces, each tuned to its operator:</p>\n<div class=\"table-shell\"><table><thead><tr><th>Surface</th><th>Audience</th><th>Stack</th></tr></thead><tbody><tr><td><strong>Admin Console</strong></td><td>System administrators</td><td>Web (Inertia + React)</td></tr><tr><td><strong>Station / Dispatcher Console</strong></td><td>Police-station operators &amp; dispatchers</td><td>Web (Inertia + React)</td></tr><tr><td><strong>Officer App</strong></td><td>Field officers</td><td>Mobile (Flutter) + API</td></tr></tbody></table></div>\n<p>Two non-human surfaces complete the system: the <strong>AI Detection Service</strong> (API client) and the <strong>Camera Gateway</strong> (RTSP fan-out service).</p>\n<h2 id=\"key-capabilities\"><a class=\"anchor\" href=\"#key-capabilities\">#</a>Key Capabilities</h2>\n<ul>\n<li><strong>Dispatcher Console</strong> — a tri-pane operations screen (live queue · map · incident detail) with claim/release, manual incident creation, and live updates over WebSockets.</li>\n<li><strong>Priority Engine</strong> — weighted, explainable incident scoring into Critical / High / Medium / Low tiers.</li>\n<li><strong>Citizen Tips</strong> — public web + SMS (Twilio) intake, with dispatcher triage to promote a tip into an incident.</li>\n<li><strong>Camera Streaming</strong> — a Python gateway fans a single RTSP feed into HLS (mobile), WebRTC (web), and raw RTSP (AI), plus on-demand evidence recording.</li>\n<li><strong>AI Integration</strong> — IP-whitelisted, per-session AES-256 encrypted camera credentials, heartbeat monitoring, alert &amp; crime reporting.</li>\n<li><strong>Field Operations</strong> — officer assignment by proximity, escalation on no-response, GPS tracking, panic/SOS, BOLO broadcasts, patrol zones.</li>\n<li><strong>Pattern Detection</strong> — cross-incident weapon-cluster alerts.</li>\n<li><strong>Decision Ledger</strong> — an append-only, hash-chained audit trail of every consequential action.</li>\n<li><strong>Real-Time</strong> — Laravel Echo / Pusher channels for the web consoles; Firebase Cloud Messaging for mobile.</li>\n</ul>\n<h2 id=\"tech-stack\"><a class=\"anchor\" href=\"#tech-stack\">#</a>Technology Stack</h2>\n<div class=\"table-shell\"><table><thead><tr><th>Layer</th><th>Technology</th></tr></thead><tbody><tr><td>Backend</td><td>PHP 8.4, Laravel 13</td></tr><tr><td>Modularity</td><td><code>nwidart/laravel-modules</code> (6 modules)</td></tr><tr><td>Web frontend</td><td>Inertia.js v3 + React 19, Tailwind CSS v4, Vite</td></tr><tr><td>Maps &amp; video</td><td>MapLibre GL, HLS.js</td></tr><tr><td>Auth</td><td>Laravel Sanctum (multi-guard)</td></tr><tr><td>Database</td><td>PostgreSQL + PostGIS</td></tr><tr><td>Cache / Queue / Presence</td><td>Redis</td></tr><tr><td>Queues &amp; monitoring</td><td>Laravel Horizon</td></tr><tr><td>Real-time</td><td>Pusher protocol (Laravel Echo)</td></tr><tr><td>Push notifications</td><td>Firebase Cloud Messaging (<code>kreait/laravel-firebase</code>)</td></tr><tr><td>SMS / voice intake</td><td>Twilio</td></tr><tr><td>RBAC</td><td><code>spatie/laravel-permission</code></td></tr><tr><td>DTOs</td><td><code>spatie/laravel-data</code></td></tr><tr><td>Reports</td><td><code>barryvdh/laravel-dompdf</code>, <code>maatwebsite/excel</code></td></tr><tr><td>Camera gateway</td><td>Python (Flask + FFmpeg + MediaMTX)</td></tr><tr><td>Testing</td><td>Pest (PHP), Playwright (E2E)</td></tr><tr><td>Observability</td><td>Laravel Telescope, Pail</td></tr></tbody></table></div>\n<p>Continue to <a href=\"#/docs/architecture\">System Architecture</a>.</p>", "headings": [{"level": 1, "title": "Overview", "id": "overview"}, {"level": 2, "title": "What is CrimeLens?", "id": "what-is-crimelens"}, {"level": 2, "title": "The Problem", "id": "the-problem"}, {"level": 2, "title": "The Approach: AI-Originated CAD", "id": "the-approach"}, {"level": 2, "title": "Surfaces", "id": "surfaces"}, {"level": 2, "title": "Key Capabilities", "id": "key-capabilities"}, {"level": 2, "title": "Technology Stack", "id": "tech-stack"}]}, {"slug": "architecture", "title": "System Architecture", "section": "Getting Started", "description": "System Architecture High Level Diagram high level Modular Monolith modular monolith Request & Auth Flow request flow Real Time Backbone realtime backbone Streaming Pipeline streaming pipelin…", "raw": "# System Architecture\n\n---\n\n- [High-Level Diagram](#high-level)\n- [Modular Monolith](#modular-monolith)\n- [Request & Auth Flow](#request-flow)\n- [Real-Time Backbone](#realtime-backbone)\n- [Streaming Pipeline](#streaming-pipeline)\n- [Data & Storage](#data-storage)\n- [Layered Code Conventions](#conventions)\n\n<a name=\"high-level\"></a>\n## High-Level Diagram\n\n```\n                 ┌───────────────┐   ┌────────────────┐   ┌──────────────┐\n                 │ Admin Console │   │ Station Console│   │  Officer App │\n                 │  (React/Web)  │   │  (React/Web)   │   │   (Flutter)  │\n                 └──────┬────────┘   └───────┬────────┘   └──────┬───────┘\n                        │  Inertia            │ Inertia / Echo    │ REST + FCM\n                        ▼                     ▼                   ▼\n        ┌───────────────────────────────────────────────────────────────────┐\n        │                      Laravel 13 Modular Monolith                    │\n        │  Admin · Police · Camera · Gateway · AiModel · Core   (6 modules)   │\n        │  Controllers → FormRequests/DTOs → Services/Actions → Models        │\n        └───────┬───────────────┬──────────────────┬─────────────┬───────────┘\n                │               │                  │             │\n          ┌─────▼────┐    ┌─────▼─────┐      ┌──────▼─────┐  ┌────▼─────┐\n          │PostgreSQL│    │   Redis   │      │  Horizon   │  │  Pusher  │\n          │ + PostGIS│    │ cache/    │      │  queues    │  │  (Echo)  │\n          │          │    │ presence  │      │            │  │          │\n          └──────────┘    └───────────┘      └────────────┘  └──────────┘\n                │                                                  \n          ┌─────▼──────────────┐        ┌──────────────────────────────┐\n          │   AI Detection     │◀──RTSP─│   Camera Gateway (Python)     │◀── Tapo / ONVIF cameras\n          │   Service (API)    │        │   Flask + FFmpeg + MediaMTX   │──▶ HLS / WebRTC\n          └────────────────────┘        └──────────────────────────────┘\n```\n\n<a name=\"modular-monolith\"></a>\n## Modular Monolith\n\nCrimeLens is a **modular monolith** built on `nwidart/laravel-modules`. Each module is a self-contained bounded context with its own routes, controllers, services, models, migrations, translations, and tests, while sharing a single deployable application and database.\n\n| Module | Responsibility |\n|--------|----------------|\n| **Core** | Cross-cutting services: chat, settings, the decision ledger, geocoding, shared concerns. |\n| **Police** | The heart of the system: incidents, dispatcher console, crimes, officers, citizen tips, BOLO, panic, patrol, station web UI. |\n| **Camera** | Camera CRUD, Tapo/ONVIF control, recording, scene extraction, tamper detection, detection filters. |\n| **Gateway** | Bridge to the Python streaming gateway: HLS/WebRTC stream provisioning and proxying. |\n| **AiModel** | AI service identity, camera assignment, encrypted credential delivery, alert/crime intake, heartbeat. |\n| **Admin** | Administrative console: police-station & AI-model management, settings, notifications, system health. |\n\nSee [Modules](/{{route}}/{{version}}/modules) for a deeper breakdown.\n\n<a name=\"request-flow\"></a>\n## Request & Auth Flow\n\nCrimeLens exposes **eight application-facing authentication guards** from one Laravel app — session guards for the web consoles and Sanctum bearer guards for mobile/API and machine clients. Laravel also registers its internal `sanctum` guard:\n\n| Guard | Driver | Provider | Used by |\n|-------|--------|----------|---------|\n| `admin` | session | admins | Admin web console |\n| `police_station_web` | session | police_stations | Station console (institutional login) |\n| `station_user_web` | session | station_users | Station console (individual dispatcher) |\n| `police_station` | sanctum | police_stations | Station companion mobile |\n| `officer` | sanctum | officers | Officer mobile app |\n| `ai_model` | sanctum | ai_models | AI detection service |\n| `api` | sanctum | officers | Default API guard |\n| `web` | session | users | Framework default |\n\nEvery layer is thin and single-purpose: **Controller** (HTTP only) → **FormRequest / Spatie Data DTO** (validation) → **Service / Action** (business logic) → **Model** (persistence). See [Authentication & Security](/{{route}}/{{version}}/security).\n\n<a name=\"realtime-backbone\"></a>\n## Real-Time Backbone\n\nBrowser consoles subscribe to **private Pusher channels** through Laravel Echo. The principal channel is `station.{id}`, authorised for both `police_station_web` and `station_user_web` guards. Domain events (`IncidentCreated`, `IncidentAssigned`, `CitizenTipReceived`, `PanicActivated`, …) are broadcast on these channels and consumed by the React consoles to update queues, maps, and toasts without a page refresh.\n\nMobile clients receive **Firebase Cloud Messaging** push for background delivery. See [Real-Time & Notifications](/{{route}}/{{version}}/realtime).\n\n<a name=\"streaming-pipeline\"></a>\n## Streaming Pipeline\n\nA camera produces one RTSP feed, but three consumers need different formats. The **Python Camera Gateway** (Flask + FFmpeg + MediaMTX) solves this by fanning a single source into:\n\n- **HLS** — for mobile and lightweight web playback,\n- **WebRTC** — for low-latency monitoring grids in the dispatcher console,\n- **RTSP** — passed through to the AI detection service.\n\nThe camera subsystem can cut evidence on demand by merging segments overlapping an incident window. The legacy direct model-report flow queues this extraction. The newer AI → Incident → Crime path currently accepts the model time window but does not persist it on the Incident, so automatic extraction is not yet guaranteed for every AI-originated incident. See [Cameras & Streaming](/{{route}}/{{version}}/cameras).\n\n<a name=\"data-storage\"></a>\n## Data & Storage\n\n- **PostgreSQL + PostGIS** — the system of record (~35 tables) and spatial queries for historical analytics and heatmaps.\n- **Redis** — cache, queues, dispatcher presence (sorted sets), and high-frequency officer GPS lookup (hot path). The current location service also synchronizes the latest point to PostGIS.\n- **Object/file storage** — recorded evidence, citizen-tip media, and report exports.\n\n<a name=\"conventions\"></a>\n## Layered Code Conventions\n\nThe codebase follows a strict separation of concerns:\n\n- **Thin controllers** — accept the request, call a service/action, return a resource.\n- **FormRequests & DTOs** — all validation lives outside controllers (`spatie/laravel-data` for typed transfer objects).\n- **Services** — CRUD and simple business logic.\n- **Actions** — complex, multi-step operations with side effects.\n- **Events & Listeners** — broadcasting and decoupled side effects.\n- **Jobs** — anything slow runs on a Redis queue under Horizon.\n- **Enums** — PHP 8 backed enums for every status/type field.\n- **API Resources** — every response is shaped; raw models are never returned.\n\nContinue to [Modules](/{{route}}/{{version}}/modules).\n", "html": "<h1 id=\"system-architecture\"><a class=\"anchor\" href=\"#system-architecture\">#</a>System Architecture</h1>\n<hr>\n<ul>\n<li><a href=\"#high-level\">High-Level Diagram</a></li>\n<li><a href=\"#modular-monolith\">Modular Monolith</a></li>\n<li><a href=\"#request-flow\">Request &amp;amp; Auth Flow</a></li>\n<li><a href=\"#realtime-backbone\">Real-Time Backbone</a></li>\n<li><a href=\"#streaming-pipeline\">Streaming Pipeline</a></li>\n<li><a href=\"#data-storage\">Data &amp;amp; Storage</a></li>\n<li><a href=\"#conventions\">Layered Code Conventions</a></li>\n</ul>\n<h2 id=\"high-level\"><a class=\"anchor\" href=\"#high-level\">#</a>High-Level Diagram</h2>\n<div class=\"code-card\"><div class=\"code-toolbar\"><span>text</span><button class=\"copy-btn\" type=\"button\">Copy</button></div><pre><code class=\"language-text\">                 ┌───────────────┐   ┌────────────────┐   ┌──────────────┐\n                 │ Admin Console │   │ Station Console│   │  Officer App │\n                 │  (React/Web)  │   │  (React/Web)   │   │   (Flutter)  │\n                 └──────┬────────┘   └───────┬────────┘   └──────┬───────┘\n                        │  Inertia            │ Inertia / Echo    │ REST + FCM\n                        ▼                     ▼                   ▼\n        ┌───────────────────────────────────────────────────────────────────┐\n        │                      Laravel 13 Modular Monolith                    │\n        │  Admin · Police · Camera · Gateway · AiModel · Core   (6 modules)   │\n        │  Controllers → FormRequests/DTOs → Services/Actions → Models        │\n        └───────┬───────────────┬──────────────────┬─────────────┬───────────┘\n                │               │                  │             │\n          ┌─────▼────┐    ┌─────▼─────┐      ┌──────▼─────┐  ┌────▼─────┐\n          │PostgreSQL│    │   Redis   │      │  Horizon   │  │  Pusher  │\n          │ + PostGIS│    │ cache/    │      │  queues    │  │  (Echo)  │\n          │          │    │ presence  │      │            │  │          │\n          └──────────┘    └───────────┘      └────────────┘  └──────────┘\n                │                                                  \n          ┌─────▼──────────────┐        ┌──────────────────────────────┐\n          │   AI Detection     │◀──RTSP─│   Camera Gateway (Python)     │◀── Tapo / ONVIF cameras\n          │   Service (API)    │        │   Flask + FFmpeg + MediaMTX   │──▶ HLS / WebRTC\n          └────────────────────┘        └──────────────────────────────┘</code></pre></div>\n<h2 id=\"modular-monolith\"><a class=\"anchor\" href=\"#modular-monolith\">#</a>Modular Monolith</h2>\n<p>CrimeLens is a <strong>modular monolith</strong> built on <code>nwidart/laravel-modules</code>. Each module is a self-contained bounded context with its own routes, controllers, services, models, migrations, translations, and tests, while sharing a single deployable application and database.</p>\n<div class=\"table-shell\"><table><thead><tr><th>Module</th><th>Responsibility</th></tr></thead><tbody><tr><td><strong>Core</strong></td><td>Cross-cutting services: chat, settings, the decision ledger, geocoding, shared concerns.</td></tr><tr><td><strong>Police</strong></td><td>The heart of the system: incidents, dispatcher console, crimes, officers, citizen tips, BOLO, panic, patrol, station web UI.</td></tr><tr><td><strong>Camera</strong></td><td>Camera CRUD, Tapo/ONVIF control, recording, scene extraction, tamper detection, detection filters.</td></tr><tr><td><strong>Gateway</strong></td><td>Bridge to the Python streaming gateway: HLS/WebRTC stream provisioning and proxying.</td></tr><tr><td><strong>AiModel</strong></td><td>AI service identity, camera assignment, encrypted credential delivery, alert/crime intake, heartbeat.</td></tr><tr><td><strong>Admin</strong></td><td>Administrative console: police-station &amp; AI-model management, settings, notifications, system health.</td></tr></tbody></table></div>\n<p>See <a href=\"#/docs/modules\">Modules</a> for a deeper breakdown.</p>\n<h2 id=\"request-flow\"><a class=\"anchor\" href=\"#request-flow\">#</a>Request &amp; Auth Flow</h2>\n<p>CrimeLens exposes <strong>eight application-facing authentication guards</strong> from one Laravel app — session guards for the web consoles and Sanctum bearer guards for mobile/API and machine clients. Laravel also registers its internal <code>sanctum</code> guard:</p>\n<div class=\"table-shell\"><table><thead><tr><th>Guard</th><th>Driver</th><th>Provider</th><th>Used by</th></tr></thead><tbody><tr><td><code>admin</code></td><td>session</td><td>admins</td><td>Admin web console</td></tr><tr><td><code>police_station_web</code></td><td>session</td><td>police_stations</td><td>Station console (institutional login)</td></tr><tr><td><code>station_user_web</code></td><td>session</td><td>station_users</td><td>Station console (individual dispatcher)</td></tr><tr><td><code>police_station</code></td><td>sanctum</td><td>police_stations</td><td>Station companion mobile</td></tr><tr><td><code>officer</code></td><td>sanctum</td><td>officers</td><td>Officer mobile app</td></tr><tr><td><code>ai_model</code></td><td>sanctum</td><td>ai_models</td><td>AI detection service</td></tr><tr><td><code>api</code></td><td>sanctum</td><td>officers</td><td>Default API guard</td></tr><tr><td><code>web</code></td><td>session</td><td>users</td><td>Framework default</td></tr></tbody></table></div>\n<p>Every layer is thin and single-purpose: <strong>Controller</strong> (HTTP only) → <strong>FormRequest / Spatie Data DTO</strong> (validation) → <strong>Service / Action</strong> (business logic) → <strong>Model</strong> (persistence). See <a href=\"#/docs/security\">Authentication &amp;amp; Security</a>.</p>\n<h2 id=\"realtime-backbone\"><a class=\"anchor\" href=\"#realtime-backbone\">#</a>Real-Time Backbone</h2>\n<p>Browser consoles subscribe to <strong>private Pusher channels</strong> through Laravel Echo. The principal channel is <code>station.{id}</code>, authorised for both <code>police_station_web</code> and <code>station_user_web</code> guards. Domain events (<code>IncidentCreated</code>, <code>IncidentAssigned</code>, <code>CitizenTipReceived</code>, <code>PanicActivated</code>, …) are broadcast on these channels and consumed by the React consoles to update queues, maps, and toasts without a page refresh.</p>\n<p>Mobile clients receive <strong>Firebase Cloud Messaging</strong> push for background delivery. See <a href=\"#/docs/realtime\">Real-Time &amp;amp; Notifications</a>.</p>\n<h2 id=\"streaming-pipeline\"><a class=\"anchor\" href=\"#streaming-pipeline\">#</a>Streaming Pipeline</h2>\n<p>A camera produces one RTSP feed, but three consumers need different formats. The <strong>Python Camera Gateway</strong> (Flask + FFmpeg + MediaMTX) solves this by fanning a single source into:</p>\n<ul>\n<li><strong>HLS</strong> — for mobile and lightweight web playback,</li>\n<li><strong>WebRTC</strong> — for low-latency monitoring grids in the dispatcher console,</li>\n<li><strong>RTSP</strong> — passed through to the AI detection service.</li>\n</ul>\n<p>The camera subsystem can cut evidence on demand by merging segments overlapping an incident window. The legacy direct model-report flow queues this extraction. The newer AI → Incident → Crime path currently accepts the model time window but does not persist it on the Incident, so automatic extraction is not yet guaranteed for every AI-originated incident. See <a href=\"#/docs/cameras\">Cameras &amp;amp; Streaming</a>.</p>\n<h2 id=\"data-storage\"><a class=\"anchor\" href=\"#data-storage\">#</a>Data &amp; Storage</h2>\n<ul>\n<li><strong>PostgreSQL + PostGIS</strong> — the system of record (~35 tables) and spatial queries for historical analytics and heatmaps.</li>\n<li><strong>Redis</strong> — cache, queues, dispatcher presence (sorted sets), and high-frequency officer GPS lookup (hot path). The current location service also synchronizes the latest point to PostGIS.</li>\n<li><strong>Object/file storage</strong> — recorded evidence, citizen-tip media, and report exports.</li>\n</ul>\n<h2 id=\"conventions\"><a class=\"anchor\" href=\"#conventions\">#</a>Layered Code Conventions</h2>\n<p>The codebase follows a strict separation of concerns:</p>\n<ul>\n<li><strong>Thin controllers</strong> — accept the request, call a service/action, return a resource.</li>\n<li><strong>FormRequests &amp; DTOs</strong> — all validation lives outside controllers (<code>spatie/laravel-data</code> for typed transfer objects).</li>\n<li><strong>Services</strong> — CRUD and simple business logic.</li>\n<li><strong>Actions</strong> — complex, multi-step operations with side effects.</li>\n<li><strong>Events &amp; Listeners</strong> — broadcasting and decoupled side effects.</li>\n<li><strong>Jobs</strong> — anything slow runs on a Redis queue under Horizon.</li>\n<li><strong>Enums</strong> — PHP 8 backed enums for every status/type field.</li>\n<li><strong>API Resources</strong> — every response is shaped; raw models are never returned.</li>\n</ul>\n<p>Continue to <a href=\"#/docs/modules\">Modules</a>.</p>", "headings": [{"level": 1, "title": "System Architecture", "id": "system-architecture"}, {"level": 2, "title": "High-Level Diagram", "id": "high-level"}, {"level": 2, "title": "Modular Monolith", "id": "modular-monolith"}, {"level": 2, "title": "Request & Auth Flow", "id": "request-flow"}, {"level": 2, "title": "Real-Time Backbone", "id": "realtime-backbone"}, {"level": 2, "title": "Streaming Pipeline", "id": "streaming-pipeline"}, {"level": 2, "title": "Data & Storage", "id": "data-storage"}, {"level": 2, "title": "Layered Code Conventions", "id": "conventions"}]}, {"slug": "modules", "title": "Modules", "section": "Getting Started", "description": "Modules CrimeLens is split into six Laravel modules under Modules/ . Each owns its routes, controllers, services, models, migrations, translations, and tests. Core core Police police Camera …", "raw": "# Modules\n\n---\n\nCrimeLens is split into six Laravel modules under `Modules/`. Each owns its routes, controllers, services, models, migrations, translations, and tests.\n\n- [Core](#core)\n- [Police](#police)\n- [Camera](#camera)\n- [Gateway](#gateway)\n- [AiModel](#aimodel)\n- [Admin](#admin)\n- [Dependency Direction](#dependencies)\n\n<a name=\"core\"></a>\n## Core\n\nCross-cutting building blocks shared by every other module.\n\n- **Chat** — conversations and messages between admin, station, and officers, with real-time delivery and voice-note transcription.\n- **Settings** — key/value system configuration with Redis caching (`SettingsService`).\n- **Decision Ledger** — an append-only, hash-chained audit trail (`ledger_entries`) with a `ledger:verify` integrity check.\n- **Geocoding** — reverse-geocoding for incident and tip locations.\n\nRoutes: `Modules/Core/routes/{web,api}.php`.\n\n<a name=\"police\"></a>\n## Police\n\nThe largest module and the operational heart of the platform. It owns the entire incident/dispatch domain and the station web console.\n\n- **Incidents & Dispatch** — `Incident` model, dispatcher console (`DispatcherController`), claim/release (`IncidentClaimController`), priority scoring, snapshots, presence, and load-balancing.\n- **Crimes** — crime lifecycle, officer actions, escalation.\n- **Citizen Tips** — public intake (web + Twilio SMS), triage, promote/dismiss, media, the dispatcher inbox.\n- **Officers** — CRUD, GPS/location, shifts, status, panic/SOS, BOLO, patrol zones.\n- **Pattern detection** — cross-incident weapon-cluster alerts.\n- **Station web UI** — all Inertia/React pages under `Modules/Police/resources/js`.\n\nRoutes: `Modules/Police/routes/{web,api,station.web,channels}.php`.\n\n<a name=\"camera\"></a>\n## Camera\n\nEverything about the physical cameras and their evidence.\n\n- **Camera CRUD** and assignment.\n- **Tapo / ONVIF control** — PTZ, alarm, privacy, LED, motion.\n- **Recording & scene extraction** — continuous segment recording for storage-less cameras and on-demand evidence cutting.\n- **Health & tamper** — health checks, offline detection, and tamper events.\n- **Detection filters** — per-camera zones/rules that shape what the AI reports.\n\nRoutes: `Modules/Camera/routes/{web,api}.php`.\n\n<a name=\"gateway\"></a>\n## Gateway\n\nThe Laravel-side bridge to the external **Python streaming gateway** (Flask + FFmpeg + MediaMTX). It provisions and proxies camera streams into HLS and WebRTC, and keeps the gateway's published streams in sync with camera state via jobs such as `SyncCameraGatewayStreamJob`.\n\nRoutes: `Modules/Gateway/routes/*`.\n\n<a name=\"aimodel\"></a>\n## AiModel\n\nThe contract surface for the AI detection service.\n\n- **Identity & security** — `AiModel` accounts, IP whitelisting, per-session AES-256 encryption keys.\n- **Camera assignment** — many-to-many `camera_ai_model` pivot; the model only ever sees its assigned cameras.\n- **Intake** — login, heartbeat, alert, and crime-detection endpoints that feed the incident layer.\n- **Monitoring** — heartbeat timeout detection (`model:check-heartbeats`) and `ai_detection_logs`.\n\nRoutes: `Modules/AiModel/routes/{api,web}.php`.\n\n<a name=\"admin\"></a>\n## Admin\n\nThe administrative console (Inertia + React).\n\n- **Police-station management** — CRUD, Excel import/export, password resets.\n- **AI-model management** — registration, IP whitelist, camera assignment.\n- **Settings** — priority weights, radii, timeouts, Firebase, camera/gateway config.\n- **Notifications & system health** — heartbeats, camera status, queue depth.\n\nRoutes: `Modules/Admin/routes/{web,channels}.php`.\n\n<a name=\"dependencies\"></a>\n## Dependency Direction\n\n`Core` sits at the bottom and is depended on by everyone. `Police` is the central domain and consumes `Camera`, `Gateway`, `AiModel`, and `Core`. `Admin` orchestrates configuration across all modules. No module reaches \"upward\" into the consoles; UI talks to modules through their controllers only.\n\nContinue to [Installation & Setup](/{{route}}/{{version}}/installation).\n", "html": "<h1 id=\"modules\"><a class=\"anchor\" href=\"#modules\">#</a>Modules</h1>\n<hr>\n<p>CrimeLens is split into six Laravel modules under <code>Modules/</code>. Each owns its routes, controllers, services, models, migrations, translations, and tests.</p>\n<ul>\n<li><a href=\"#core\">Core</a></li>\n<li><a href=\"#police\">Police</a></li>\n<li><a href=\"#camera\">Camera</a></li>\n<li><a href=\"#gateway\">Gateway</a></li>\n<li><a href=\"#aimodel\">AiModel</a></li>\n<li><a href=\"#admin\">Admin</a></li>\n<li><a href=\"#dependencies\">Dependency Direction</a></li>\n</ul>\n<h2 id=\"core\"><a class=\"anchor\" href=\"#core\">#</a>Core</h2>\n<p>Cross-cutting building blocks shared by every other module.</p>\n<ul>\n<li><strong>Chat</strong> — conversations and messages between admin, station, and officers, with real-time delivery and voice-note transcription.</li>\n<li><strong>Settings</strong> — key/value system configuration with Redis caching (<code>SettingsService</code>).</li>\n<li><strong>Decision Ledger</strong> — an append-only, hash-chained audit trail (<code>ledger_entries</code>) with a <code>ledger:verify</code> integrity check.</li>\n<li><strong>Geocoding</strong> — reverse-geocoding for incident and tip locations.</li>\n</ul>\n<p>Routes: <code>Modules/Core/routes/{web,api}.php</code>.</p>\n<h2 id=\"police\"><a class=\"anchor\" href=\"#police\">#</a>Police</h2>\n<p>The largest module and the operational heart of the platform. It owns the entire incident/dispatch domain and the station web console.</p>\n<ul>\n<li><strong>Incidents &amp; Dispatch</strong> — <code>Incident</code> model, dispatcher console (<code>DispatcherController</code>), claim/release (<code>IncidentClaimController</code>), priority scoring, snapshots, presence, and load-balancing.</li>\n<li><strong>Crimes</strong> — crime lifecycle, officer actions, escalation.</li>\n<li><strong>Citizen Tips</strong> — public intake (web + Twilio SMS), triage, promote/dismiss, media, the dispatcher inbox.</li>\n<li><strong>Officers</strong> — CRUD, GPS/location, shifts, status, panic/SOS, BOLO, patrol zones.</li>\n<li><strong>Pattern detection</strong> — cross-incident weapon-cluster alerts.</li>\n<li><strong>Station web UI</strong> — all Inertia/React pages under <code>Modules/Police/resources/js</code>.</li>\n</ul>\n<p>Routes: <code>Modules/Police/routes/{web,api,station.web,channels}.php</code>.</p>\n<h2 id=\"camera\"><a class=\"anchor\" href=\"#camera\">#</a>Camera</h2>\n<p>Everything about the physical cameras and their evidence.</p>\n<ul>\n<li><strong>Camera CRUD</strong> and assignment.</li>\n<li><strong>Tapo / ONVIF control</strong> — PTZ, alarm, privacy, LED, motion.</li>\n<li><strong>Recording &amp; scene extraction</strong> — continuous segment recording for storage-less cameras and on-demand evidence cutting.</li>\n<li><strong>Health &amp; tamper</strong> — health checks, offline detection, and tamper events.</li>\n<li><strong>Detection filters</strong> — per-camera zones/rules that shape what the AI reports.</li>\n</ul>\n<p>Routes: <code>Modules/Camera/routes/{web,api}.php</code>.</p>\n<h2 id=\"gateway\"><a class=\"anchor\" href=\"#gateway\">#</a>Gateway</h2>\n<p>The Laravel-side bridge to the external <strong>Python streaming gateway</strong> (Flask + FFmpeg + MediaMTX). It provisions and proxies camera streams into HLS and WebRTC, and keeps the gateway's published streams in sync with camera state via jobs such as <code>SyncCameraGatewayStreamJob</code>.</p>\n<p>Routes: <code>Modules/Gateway/routes/*</code>.</p>\n<h2 id=\"aimodel\"><a class=\"anchor\" href=\"#aimodel\">#</a>AiModel</h2>\n<p>The contract surface for the AI detection service.</p>\n<ul>\n<li><strong>Identity &amp; security</strong> — <code>AiModel</code> accounts, IP whitelisting, per-session AES-256 encryption keys.</li>\n<li><strong>Camera assignment</strong> — many-to-many <code>camera_ai_model</code> pivot; the model only ever sees its assigned cameras.</li>\n<li><strong>Intake</strong> — login, heartbeat, alert, and crime-detection endpoints that feed the incident layer.</li>\n<li><strong>Monitoring</strong> — heartbeat timeout detection (<code>model:check-heartbeats</code>) and <code>ai_detection_logs</code>.</li>\n</ul>\n<p>Routes: <code>Modules/AiModel/routes/{api,web}.php</code>.</p>\n<h2 id=\"admin\"><a class=\"anchor\" href=\"#admin\">#</a>Admin</h2>\n<p>The administrative console (Inertia + React).</p>\n<ul>\n<li><strong>Police-station management</strong> — CRUD, Excel import/export, password resets.</li>\n<li><strong>AI-model management</strong> — registration, IP whitelist, camera assignment.</li>\n<li><strong>Settings</strong> — priority weights, radii, timeouts, Firebase, camera/gateway config.</li>\n<li><strong>Notifications &amp; system health</strong> — heartbeats, camera status, queue depth.</li>\n</ul>\n<p>Routes: <code>Modules/Admin/routes/{web,channels}.php</code>.</p>\n<h2 id=\"dependencies\"><a class=\"anchor\" href=\"#dependencies\">#</a>Dependency Direction</h2>\n<p><code>Core</code> sits at the bottom and is depended on by everyone. <code>Police</code> is the central domain and consumes <code>Camera</code>, <code>Gateway</code>, <code>AiModel</code>, and <code>Core</code>. <code>Admin</code> orchestrates configuration across all modules. No module reaches \"upward\" into the consoles; UI talks to modules through their controllers only.</p>\n<p>Continue to <a href=\"#/docs/installation\">Installation &amp;amp; Setup</a>.</p>", "headings": [{"level": 1, "title": "Modules", "id": "modules"}, {"level": 2, "title": "Core", "id": "core"}, {"level": 2, "title": "Police", "id": "police"}, {"level": 2, "title": "Camera", "id": "camera"}, {"level": 2, "title": "Gateway", "id": "gateway"}, {"level": 2, "title": "AiModel", "id": "aimodel"}, {"level": 2, "title": "Admin", "id": "admin"}, {"level": 2, "title": "Dependency Direction", "id": "dependencies"}]}, {"slug": "installation", "title": "Installation & Setup", "section": "Getting Started", "description": "Installation & Setup This page gets CrimeLens running locally — the Laravel application, its services, and the Python camera gateway. Requirements requirements Quick Start quick start Enviro…", "raw": "# Installation & Setup\n\n---\n\nThis page gets CrimeLens running locally — the Laravel application, its services, and the Python camera gateway.\n\n- [Requirements](#requirements)\n- [Quick Start](#quick-start)\n- [Environment](#environment)\n- [Running in Development](#dev)\n- [The Camera Gateway](#gateway)\n- [Production Notes](#production)\n\n<a name=\"requirements\"></a>\n## Requirements\n\n| Component | Version / Notes |\n|-----------|-----------------|\n| PHP | 8.4 (with `pdo_pgsql`, `redis`, `gd`, `openssl`) |\n| Composer | 2.x |\n| Node.js | 20+ (Vite 7, React 19) |\n| PostgreSQL | 14+ with the **PostGIS** extension |\n| Redis | 6+ (cache, queues, presence, GPS) |\n| FFmpeg | required by the gateway & scene extraction |\n| Python | 3.10+ for the camera gateway (Flask, pytapo) |\n\nExternal services (optional for a basic demo): a **Pusher** app (or compatible), **Firebase** credentials for FCM, and a **Twilio** number for SMS tips.\n\n<a name=\"quick-start\"></a>\n## Quick Start\n\n```bash\ngit clone <repo> && cd final-project\n\n# One-shot bootstrap (install, env, key, migrate, npm, build)\ncomposer run setup\n```\n\n`composer run setup` runs: `composer install` → copy `.env` → `key:generate` → `migrate --force` → `npm install` → `npm run build`.\n\nThen seed demo data and create the queue/dashboard:\n\n```bash\nphp artisan migrate --seed     # demo stations, officers, cameras, crimes, incidents\nphp artisan horizon            # start the queue workers\n```\n\n<a name=\"environment\"></a>\n## Environment\n\nKey `.env` values:\n\n```ini\nAPP_URL=http://127.0.0.1:8000\n\nDB_CONNECTION=pgsql\nDB_DATABASE=crimelens\n\nQUEUE_CONNECTION=redis\nCACHE_STORE=redis\n\nBROADCAST_CONNECTION=pusher\nPUSHER_APP_ID=...\nPUSHER_APP_KEY=...\nPUSHER_APP_SECRET=...\nPUSHER_APP_CLUSTER=eu\n\n# Camera gateway\nCAMERA_GATEWAY_API_BASE_URL=http://127.0.0.1:5555\nCAMERA_GATEWAY_SHARED_SECRET=...\n\n# Optional integrations\nFIREBASE_CREDENTIALS=...\nTWILIO_SID=...   TWILIO_TOKEN=...\n```\n\n> {warning} `BROADCAST_CONNECTION` and `QUEUE_CONNECTION` are set to `null`/`sync` in the test environment so the suite never makes live network calls.\n\n<a name=\"dev\"></a>\n## Running in Development\n\nA single command runs the whole local stack:\n\n```bash\ncomposer run dev\n```\n\nThis boots the gateway streams and runs, concurrently: the PHP server, the queue listener, the Pail log tail, the Vite dev server, and the scheduler — colour-coded under one process. Visit:\n\n- `http://127.0.0.1:8000/admin` — admin console\n- `http://127.0.0.1:8000/station` — dispatcher console\n- `http://127.0.0.1:8000/docs` — this documentation\n- `http://127.0.0.1:8000/horizon` — queue dashboard\n- `http://127.0.0.1:8000/telescope` — request/debug telemetry\n\n<a name=\"gateway\"></a>\n## The Camera Gateway\n\nThe Python gateway (`Modules/Gateway/tapo_server.py`) is a persistent Flask service that pools `pytapo` connections and fans each camera's RTSP into HLS (and, with MediaMTX, RTSP/WebRTC). Run it alongside Laravel:\n\n```bash\nGATEWAY_BIND_PORT=5555 \\\nCAMERA_GATEWAY_SHARED_SECRET=<same as .env> \\\npython Modules/Gateway/tapo_server.py\n```\n\nThen `php artisan camera:gateway:boot` (also run by `composer run dev` and the scheduler) registers and starts streams for all active cameras. See [Cameras & Streaming](/{{route}}/{{version}}/cameras) for the full pipeline.\n\n<a name=\"production\"></a>\n## Production Notes\n\n- Serve PHP-FPM 8.4 behind Nginx; run **Horizon** as a supervised process and the **scheduler** via a one-minute cron (`schedule:run`).\n- Run the gateway (and MediaMTX, for WebRTC) on a host with FFmpeg and network reach to the cameras.\n- Use Redis for cache/queue/session; PostgreSQL + PostGIS for the system of record.\n- Build assets with `npm run build` (and `build:ssr` if using SSR).\n\nContinue to the [Glossary](/{{route}}/{{version}}/glossary).\n", "html": "<h1 id=\"installation-setup\"><a class=\"anchor\" href=\"#installation-setup\">#</a>Installation &amp; Setup</h1>\n<hr>\n<p>This page gets CrimeLens running locally — the Laravel application, its services, and the Python camera gateway.</p>\n<ul>\n<li><a href=\"#requirements\">Requirements</a></li>\n<li><a href=\"#quick-start\">Quick Start</a></li>\n<li><a href=\"#environment\">Environment</a></li>\n<li><a href=\"#dev\">Running in Development</a></li>\n<li><a href=\"#gateway\">The Camera Gateway</a></li>\n<li><a href=\"#production\">Production Notes</a></li>\n</ul>\n<h2 id=\"requirements\"><a class=\"anchor\" href=\"#requirements\">#</a>Requirements</h2>\n<div class=\"table-shell\"><table><thead><tr><th>Component</th><th>Version / Notes</th></tr></thead><tbody><tr><td>PHP</td><td>8.4 (with <code>pdo_pgsql</code>, <code>redis</code>, <code>gd</code>, <code>openssl</code>)</td></tr><tr><td>Composer</td><td>2.x</td></tr><tr><td>Node.js</td><td>20+ (Vite 7, React 19)</td></tr><tr><td>PostgreSQL</td><td>14+ with the <strong>PostGIS</strong> extension</td></tr><tr><td>Redis</td><td>6+ (cache, queues, presence, GPS)</td></tr><tr><td>FFmpeg</td><td>required by the gateway &amp; scene extraction</td></tr><tr><td>Python</td><td>3.10+ for the camera gateway (Flask, pytapo)</td></tr></tbody></table></div>\n<p>External services (optional for a basic demo): a <strong>Pusher</strong> app (or compatible), <strong>Firebase</strong> credentials for FCM, and a <strong>Twilio</strong> number for SMS tips.</p>\n<h2 id=\"quick-start\"><a class=\"anchor\" href=\"#quick-start\">#</a>Quick Start</h2>\n<div class=\"code-card\"><div class=\"code-toolbar\"><span>bash</span><button class=\"copy-btn\" type=\"button\">Copy</button></div><pre><code class=\"language-bash\">git clone &lt;repo&gt; &amp;&amp; cd final-project\n\n# One-shot bootstrap (install, env, key, migrate, npm, build)\ncomposer run setup</code></pre></div>\n<p><code>composer run setup</code> runs: <code>composer install</code> → copy <code>.env</code> → <code>key:generate</code> → <code>migrate --force</code> → <code>npm install</code> → <code>npm run build</code>.</p>\n<p>Then seed demo data and create the queue/dashboard:</p>\n<div class=\"code-card\"><div class=\"code-toolbar\"><span>bash</span><button class=\"copy-btn\" type=\"button\">Copy</button></div><pre><code class=\"language-bash\">php artisan migrate --seed     # demo stations, officers, cameras, crimes, incidents\nphp artisan horizon            # start the queue workers</code></pre></div>\n<h2 id=\"environment\"><a class=\"anchor\" href=\"#environment\">#</a>Environment</h2>\n<p>Key <code>.env</code> values:</p>\n<div class=\"code-card\"><div class=\"code-toolbar\"><span>ini</span><button class=\"copy-btn\" type=\"button\">Copy</button></div><pre><code class=\"language-ini\">APP_URL=http://127.0.0.1:8000\n\nDB_CONNECTION=pgsql\nDB_DATABASE=crimelens\n\nQUEUE_CONNECTION=redis\nCACHE_STORE=redis\n\nBROADCAST_CONNECTION=pusher\nPUSHER_APP_ID=...\nPUSHER_APP_KEY=...\nPUSHER_APP_SECRET=...\nPUSHER_APP_CLUSTER=eu\n\n# Camera gateway\nCAMERA_GATEWAY_API_BASE_URL=http://127.0.0.1:5555\nCAMERA_GATEWAY_SHARED_SECRET=...\n\n# Optional integrations\nFIREBASE_CREDENTIALS=...\nTWILIO_SID=...   TWILIO_TOKEN=...</code></pre></div>\n<aside class=\"callout warning\"><div class=\"callout-icon\">⚠</div><div><strong>Important note</strong><p><code>BROADCAST_CONNECTION</code> and <code>QUEUE_CONNECTION</code> are set to <code>null</code>/<code>sync</code> in the test environment so the suite never makes live network calls.</p></div></aside>\n<h2 id=\"dev\"><a class=\"anchor\" href=\"#dev\">#</a>Running in Development</h2>\n<p>A single command runs the whole local stack:</p>\n<div class=\"code-card\"><div class=\"code-toolbar\"><span>bash</span><button class=\"copy-btn\" type=\"button\">Copy</button></div><pre><code class=\"language-bash\">composer run dev</code></pre></div>\n<p>This boots the gateway streams and runs, concurrently: the PHP server, the queue listener, the Pail log tail, the Vite dev server, and the scheduler — colour-coded under one process. Visit:</p>\n<ul>\n<li><code>http://127.0.0.1:8000/admin</code> — admin console</li>\n<li><code>http://127.0.0.1:8000/station</code> — dispatcher console</li>\n<li><code>http://127.0.0.1:8000/docs</code> — this documentation</li>\n<li><code>http://127.0.0.1:8000/horizon</code> — queue dashboard</li>\n<li><code>http://127.0.0.1:8000/telescope</code> — request/debug telemetry</li>\n</ul>\n<h2 id=\"gateway\"><a class=\"anchor\" href=\"#gateway\">#</a>The Camera Gateway</h2>\n<p>The Python gateway (<code>Modules/Gateway/tapo_server.py</code>) is a persistent Flask service that pools <code>pytapo</code> connections and fans each camera's RTSP into HLS (and, with MediaMTX, RTSP/WebRTC). Run it alongside Laravel:</p>\n<div class=\"code-card\"><div class=\"code-toolbar\"><span>bash</span><button class=\"copy-btn\" type=\"button\">Copy</button></div><pre><code class=\"language-bash\">GATEWAY_BIND_PORT=5555 \\\nCAMERA_GATEWAY_SHARED_SECRET=&lt;same as .env&gt; \\\npython Modules/Gateway/tapo_server.py</code></pre></div>\n<p>Then <code>php artisan camera:gateway:boot</code> (also run by <code>composer run dev</code> and the scheduler) registers and starts streams for all active cameras. See <a href=\"#/docs/cameras\">Cameras &amp;amp; Streaming</a> for the full pipeline.</p>\n<h2 id=\"production\"><a class=\"anchor\" href=\"#production\">#</a>Production Notes</h2>\n<ul>\n<li>Serve PHP-FPM 8.4 behind Nginx; run <strong>Horizon</strong> as a supervised process and the <strong>scheduler</strong> via a one-minute cron (<code>schedule:run</code>).</li>\n<li>Run the gateway (and MediaMTX, for WebRTC) on a host with FFmpeg and network reach to the cameras.</li>\n<li>Use Redis for cache/queue/session; PostgreSQL + PostGIS for the system of record.</li>\n<li>Build assets with <code>npm run build</code> (and <code>build:ssr</code> if using SSR).</li>\n</ul>\n<p>Continue to the <a href=\"#/docs/glossary\">Glossary</a>.</p>", "headings": [{"level": 1, "title": "Installation & Setup", "id": "installation-setup"}, {"level": 2, "title": "Requirements", "id": "requirements"}, {"level": 2, "title": "Quick Start", "id": "quick-start"}, {"level": 2, "title": "Environment", "id": "environment"}, {"level": 2, "title": "Running in Development", "id": "dev"}, {"level": 2, "title": "The Camera Gateway", "id": "gateway"}, {"level": 2, "title": "Production Notes", "id": "production"}]}, {"slug": "actors", "title": "Actors & Roles", "section": "Concepts", "description": "Actors & Roles CrimeLens has five actors — three human, two machine. Each is confined to the surface and authority appropriate to its role. The golden rule: only the dispatcher commits a dis…", "raw": "# Actors & Roles\n\n---\n\nCrimeLens has five actors — three human, two machine. Each is confined to the surface and authority appropriate to its role. The golden rule: **only the dispatcher commits a dispatch decision, and only the backend commands a camera.**\n\n- [System Administrator](#admin)\n- [Dispatcher / Station Operator](#dispatcher)\n- [Field Officer](#officer)\n- [AI Detection Service](#ai)\n- [Camera Device](#camera)\n- [Authority Matrix](#authority-matrix)\n\n<a name=\"admin\"></a>\n## System Administrator\n\nGoverns the platform from the **Admin Console** (web). Administrators do not handle live incidents; they configure the system the dispatchers operate.\n\n**Can:**\n- Manage police stations (CRUD, Excel import, password resets).\n- Register and manage AI detection services (IP whitelist, camera assignment, activation).\n- Configure system settings and preview/persist priority-weight proposals. Live incident scoring currently still uses fixed service constants.\n- View nationwide analytics, heatmaps, and system health (heartbeats, camera status, queue depth).\n- Inspect audit logs / the decision ledger.\n\n<a name=\"dispatcher\"></a>\n## Dispatcher / Station Operator\n\nThe operational core of the system, working from the **Station / Dispatcher Console** (web). There are two console identities:\n\n- **`police_station_web`** — the institutional station account, a **supervisory** view that sees the full queue but does not take ownership of individual incidents.\n- **`station_user_web`** — an individual person (e.g. a dispatcher or station manager) who can **claim**, **release**, dispatch, and reject incidents under their own identity.\n\n**Can:**\n- Review the live incident queue and triage by priority.\n- **Claim / release** incidents (individual users) and dispatch (create crime + assign officer) or reject as a false alarm.\n- Create **manual incidents** (e.g. from a phone call or beat report).\n- Triage **citizen tips** — promote to incident, dismiss, or reply (SMS).\n- Monitor cameras live (HLS/WebRTC grid) and the station heatmap.\n- Broadcast **BOLO** alerts, manage officers/cameras, and chat with officers.\n\n<a name=\"officer\"></a>\n## Field Officer\n\nWorks exclusively from the **Officer mobile app**. Officers receive work; they never manage stations or move cameras.\n\n**Can:**\n- Receive assigned incidents/crimes (push + in-app), with map, evidence, and a pre-arrival brief.\n- **Accept**, mark **arrived**, **resolve**, or decline (`no-visit` with a reason).\n- Stream their GPS location (every ~100 m or 30 s) and toggle availability status.\n- Trigger a **panic / SOS** button and request backup.\n- Chat with their station and capture body-cam evidence.\n\n<a name=\"ai\"></a>\n## AI Detection Service\n\nA machine client (Python) that watches camera streams and **reports** what it sees. It is the most tightly constrained actor.\n\n**Can:**\n- Authenticate (email + password, **verified against a whitelisted IP**).\n- Fetch its assigned cameras with **encrypted** credentials (AES-256, per-session key).\n- Send periodic **heartbeats**.\n- Raise an **alert** (suspicious activity) or a confirmed **crime detection** (with timestamps and confidence).\n\n**Cannot:**\n- Create a crime directly, dispatch an officer, or command a camera. It only feeds the [incident layer](/{{route}}/{{version}}/dispatch).\n\n<a name=\"camera\"></a>\n## Camera Device\n\nTapo / ONVIF street cameras. A camera streams RTSP and accepts control commands (PTZ, alarm, privacy) **only from the backend**, never from the AI or the officer. The [Camera Gateway](/{{route}}/{{version}}/cameras) brokers the stream into the formats each consumer needs.\n\n<a name=\"citizen\"></a>\n## Citizen (External Reporter)\n\nNot an authenticated actor, but a source. Members of the public submit tips through a public web form or by SMS. Tips land in a triage queue for a dispatcher to review — see [Citizen Tips](/{{route}}/{{version}}/citizen-tips).\n\n<a name=\"authority-matrix\"></a>\n## Authority Matrix\n\n| Capability | Admin | Dispatcher | Officer | AI | Camera |\n|------------|:----:|:----------:|:-------:|:--:|:------:|\n| Configure system / stations | ✅ | — | — | — | — |\n| Review & dispatch incidents | — | ✅ | — | — | — |\n| Create manual incident | — | ✅ | — | — | — |\n| Report detection (alert/crime) | — | — | — | ✅ | — |\n| Accept / resolve assigned crime | — | — | ✅ | — | — |\n| Command camera (PTZ/alarm) | via backend | via backend | — | ❌ | receives |\n| Stream GPS / panic | — | — | ✅ | — | — |\n| Submit a tip | — | — | — | — | — |\n\nContinue to [End-to-End Workflow](/{{route}}/{{version}}/workflow).\n", "html": "<h1 id=\"actors-roles\"><a class=\"anchor\" href=\"#actors-roles\">#</a>Actors &amp; Roles</h1>\n<hr>\n<p>CrimeLens has five actors — three human, two machine. Each is confined to the surface and authority appropriate to its role. The golden rule: <strong>only the dispatcher commits a dispatch decision, and only the backend commands a camera.</strong></p>\n<ul>\n<li><a href=\"#admin\">System Administrator</a></li>\n<li><a href=\"#dispatcher\">Dispatcher / Station Operator</a></li>\n<li><a href=\"#officer\">Field Officer</a></li>\n<li><a href=\"#ai\">AI Detection Service</a></li>\n<li><a href=\"#camera\">Camera Device</a></li>\n<li><a href=\"#authority-matrix\">Authority Matrix</a></li>\n</ul>\n<h2 id=\"admin\"><a class=\"anchor\" href=\"#admin\">#</a>System Administrator</h2>\n<p>Governs the platform from the <strong>Admin Console</strong> (web). Administrators do not handle live incidents; they configure the system the dispatchers operate.</p>\n<p><strong>Can:</strong></p>\n<ul>\n<li>Manage police stations (CRUD, Excel import, password resets).</li>\n<li>Register and manage AI detection services (IP whitelist, camera assignment, activation).</li>\n<li>Configure system settings and preview/persist priority-weight proposals. Live incident scoring currently still uses fixed service constants.</li>\n<li>View nationwide analytics, heatmaps, and system health (heartbeats, camera status, queue depth).</li>\n<li>Inspect audit logs / the decision ledger.</li>\n</ul>\n<h2 id=\"dispatcher\"><a class=\"anchor\" href=\"#dispatcher\">#</a>Dispatcher / Station Operator</h2>\n<p>The operational core of the system, working from the <strong>Station / Dispatcher Console</strong> (web). There are two console identities:</p>\n<ul>\n<li><strong><code>police_station_web</code></strong> — the institutional station account, a <strong>supervisory</strong> view that sees the full queue but does not take ownership of individual incidents.</li>\n<li><strong><code>station_user_web</code></strong> — an individual person (e.g. a dispatcher or station manager) who can <strong>claim</strong>, <strong>release</strong>, dispatch, and reject incidents under their own identity.</li>\n</ul>\n<p><strong>Can:</strong></p>\n<ul>\n<li>Review the live incident queue and triage by priority.</li>\n<li><strong>Claim / release</strong> incidents (individual users) and dispatch (create crime + assign officer) or reject as a false alarm.</li>\n<li>Create <strong>manual incidents</strong> (e.g. from a phone call or beat report).</li>\n<li>Triage <strong>citizen tips</strong> — promote to incident, dismiss, or reply (SMS).</li>\n<li>Monitor cameras live (HLS/WebRTC grid) and the station heatmap.</li>\n<li>Broadcast <strong>BOLO</strong> alerts, manage officers/cameras, and chat with officers.</li>\n</ul>\n<h2 id=\"officer\"><a class=\"anchor\" href=\"#officer\">#</a>Field Officer</h2>\n<p>Works exclusively from the <strong>Officer mobile app</strong>. Officers receive work; they never manage stations or move cameras.</p>\n<p><strong>Can:</strong></p>\n<ul>\n<li>Receive assigned incidents/crimes (push + in-app), with map, evidence, and a pre-arrival brief.</li>\n<li><strong>Accept</strong>, mark <strong>arrived</strong>, <strong>resolve</strong>, or decline (<code>no-visit</code> with a reason).</li>\n<li>Stream their GPS location (every ~100 m or 30 s) and toggle availability status.</li>\n<li>Trigger a <strong>panic / SOS</strong> button and request backup.</li>\n<li>Chat with their station and capture body-cam evidence.</li>\n</ul>\n<h2 id=\"ai\"><a class=\"anchor\" href=\"#ai\">#</a>AI Detection Service</h2>\n<p>A machine client (Python) that watches camera streams and <strong>reports</strong> what it sees. It is the most tightly constrained actor.</p>\n<p><strong>Can:</strong></p>\n<ul>\n<li>Authenticate (email + password, <strong>verified against a whitelisted IP</strong>).</li>\n<li>Fetch its assigned cameras with <strong>encrypted</strong> credentials (AES-256, per-session key).</li>\n<li>Send periodic <strong>heartbeats</strong>.</li>\n<li>Raise an <strong>alert</strong> (suspicious activity) or a confirmed <strong>crime detection</strong> (with timestamps and confidence).</li>\n</ul>\n<p><strong>Cannot:</strong></p>\n<ul>\n<li>Create a crime directly, dispatch an officer, or command a camera. It only feeds the <a href=\"#/docs/dispatch\">incident layer</a>.</li>\n</ul>\n<h2 id=\"camera\"><a class=\"anchor\" href=\"#camera\">#</a>Camera Device</h2>\n<p>Tapo / ONVIF street cameras. A camera streams RTSP and accepts control commands (PTZ, alarm, privacy) <strong>only from the backend</strong>, never from the AI or the officer. The <a href=\"#/docs/cameras\">Camera Gateway</a> brokers the stream into the formats each consumer needs.</p>\n<h2 id=\"citizen\"><a class=\"anchor\" href=\"#citizen\">#</a>Citizen (External Reporter)</h2>\n<p>Not an authenticated actor, but a source. Members of the public submit tips through a public web form or by SMS. Tips land in a triage queue for a dispatcher to review — see <a href=\"#/docs/citizen-tips\">Citizen Tips</a>.</p>\n<h2 id=\"authority-matrix\"><a class=\"anchor\" href=\"#authority-matrix\">#</a>Authority Matrix</h2>\n<p>| Capability | Admin | Dispatcher | Officer | AI | Camera | |------------|:----:|:----------:|:-------:|:--:|:------:| | Configure system / stations | ✅ | — | — | — | — | | Review &amp; dispatch incidents | — | ✅ | — | — | — | | Create manual incident | — | ✅ | — | — | — | | Report detection (alert/crime) | — | — | — | ✅ | — | | Accept / resolve assigned crime | — | — | ✅ | — | — | | Command camera (PTZ/alarm) | via backend | via backend | — | ❌ | receives | | Stream GPS / panic | — | — | ✅ | — | — | | Submit a tip | — | — | — | — | — |</p>\n<p>Continue to <a href=\"#/docs/workflow\">End-to-End Workflow</a>.</p>", "headings": [{"level": 1, "title": "Actors & Roles", "id": "actors-roles"}, {"level": 2, "title": "System Administrator", "id": "admin"}, {"level": 2, "title": "Dispatcher / Station Operator", "id": "dispatcher"}, {"level": 2, "title": "Field Officer", "id": "officer"}, {"level": 2, "title": "AI Detection Service", "id": "ai"}, {"level": 2, "title": "Camera Device", "id": "camera"}, {"level": 2, "title": "Citizen (External Reporter)", "id": "citizen"}, {"level": 2, "title": "Authority Matrix", "id": "authority-matrix"}]}, {"slug": "workflow", "title": "End-to-End Workflow", "section": "Concepts", "description": "End to End Workflow This is the spine of CrimeLens: how a pixel on a camera becomes a closed case, with a human dispatcher and a full audit trail in between. The CAD Pipeline pipeline Stage …", "raw": "# End-to-End Workflow\n\n---\n\nThis is the spine of CrimeLens: how a pixel on a camera becomes a closed case, with a human dispatcher and a full audit trail in between.\n\n- [The CAD Pipeline](#pipeline)\n- [Stage 1 — Detection](#detection)\n- [Stage 2 — Incident & Scoring](#incident)\n- [Stage 3 — Dispatcher Review](#review)\n- [Stage 4 — Crime & Assignment](#assignment)\n- [Stage 5 — Field Response](#response)\n- [Stage 6 — Escalation](#escalation)\n- [Alternative Entry Points](#entry-points)\n\n<a name=\"pipeline\"></a>\n## The CAD Pipeline\n\n```\n ┌────────────┐   alert/crime   ┌──────────────────────┐\n │ AI Service │ ───────────────▶│  Incident            │\n └────────────┘                 │  status=pending_review│\n                                └──────────┬────────────┘\n                                           │ Priority Engine scores it\n                                           ▼\n                                ┌──────────────────────┐\n                       ┌────────│   Dispatcher Console  │────────┐\n                       │        └──────────────────────┘        │\n                  reject│                                        │dispatch\n                       ▼                                         ▼\n            ┌────────────────────┐                  ┌────────────────────────┐\n            │ rejected_false_alarm│                 │  Crime (status=assigned)│\n            └────────────────────┘                  │  + nearest officer      │\n                                                     └───────────┬────────────┘\n                                                                 │ FCM + Echo\n                                                                 ▼\n                                        accept → in_progress → arrived → resolved\n                                                                 │\n                                            no response in N min │\n                                                                 ▼\n                                                      escalate → next officer\n```\n\n<a name=\"detection\"></a>\n## Stage 1 — Detection\n\nThe AI service watches the gateway RTSP streams of its assigned cameras. As confidence rises it raises a **suspicious-activity alert** (`POST /api/v1/model/alert`); when it confirms an event it posts a **crime detection** (`POST /api/v1/model/crime`) with `camera_id`, `description`, a start/end time window, and `confidence_score`. The current request schema does not include `crime_type`.\n\nThe AI never touches a crime record or a camera command — it only hands the backend a report. See [AI Model Integration](/{{route}}/{{version}}/ai-integration).\n\n<a name=\"incident\"></a>\n## Stage 2 — Incident & Scoring\n\nThe backend creates an **`Incident`** in `pending_review` status, tagged with its source (`ai_alert`, `ai_crime`, `citizen`, `manual`), camera, station, and location. The [Priority Engine](/{{route}}/{{version}}/priority-engine) immediately computes a `priority_score` in `[0,1]` from a transparent weighted formula and assigns a tier:\n\n| Tier | Score | Meaning |\n|------|-------|---------|\n| **Critical** | ≥ 0.85 | Eligible for auto-dispatch when the weapon + confidence gates pass |\n| **High** | 0.65 – 0.85 | Top of the queue |\n| **Medium** | 0.45 – 0.65 | Standard review |\n| **Low** | < 0.45 | Background |\n\nThe full factor breakdown is stored as JSON (`priority_factors`) so any score can be explained and audited later.\n\n<a name=\"review\"></a>\n## Stage 3 — Dispatcher Review\n\nThe incident appears **in real time** on the Dispatcher Console queue (broadcast via the `station.{id}` channel). A dispatcher:\n\n1. **Claims** the incident under their own identity (so colleagues see it is being handled).\n2. Reviews the available snapshot/camera context, map, priority factors, and pre-arrival brief.\n3. **Dispatches** it (→ Stage 4) or **rejects** it as a false alarm.\n\nA narrow **auto-dispatch policy** can skip manual review only for the highest-confidence, weapon-bearing critical incidents; everything else waits for a human. Claim/release and review actions are broadcast so every operator's screen stays consistent.\n\n<a name=\"assignment\"></a>\n## Stage 4 — Crime & Assignment\n\nOn dispatch the backend creates a **`Crime`** record (linked to its originating incident), then finds the **nearest available officer** — same station, on shift, within the configured radius — using live GPS from Redis and a Haversine distance. The officer is assigned (`status = assigned`) and notified via **Firebase push** and an **Echo** event to the station.\n\n<a name=\"response\"></a>\n## Stage 5 — Field Response\n\nThe officer receives the crime with its location and brief, plus evidence when a Scene is available, and drives the lifecycle from the mobile app:\n\n```\nassigned → accept → in_progress → arrive → visited → resolve → resolved\n                 └─ decline → not_visited (reason required)\n```\n\nStatus changes broadcast back to the station console and update the live map and timeline.\n\n<a name=\"escalation\"></a>\n## Stage 6 — Escalation\n\nThe **escalation service** can reassign a stale Crime to another officer, increment the escalation counter, and notify the station. Its Artisan command exists, but it is not currently registered in the Laravel scheduler. Stale **dispatcher claims**, by contrast, are actively auto-released every minute.\n\n<a name=\"entry-points\"></a>\n## Alternative Entry Points\n\nNot every incident starts with the AI. The same pipeline absorbs:\n\n- **Citizen tips** — a public web/SMS report becomes a tip; a dispatcher promotes it into an incident. See [Citizen Tips](/{{route}}/{{version}}/citizen-tips).\n- **Manual incidents** — a dispatcher creates an incident directly from a phone call or beat report.\n\nBoth sources join the dispatcher queue and follow the same dispatcher → crime → officer path. Manual and citizen-originated incidents currently do not run the same automatic priority calculation as AI incidents. Continue to [Incident & Dispatch](/{{route}}/{{version}}/dispatch).\n", "html": "<h1 id=\"end-to-end-workflow\"><a class=\"anchor\" href=\"#end-to-end-workflow\">#</a>End-to-End Workflow</h1>\n<hr>\n<p>This is the spine of CrimeLens: how a pixel on a camera becomes a closed case, with a human dispatcher and a full audit trail in between.</p>\n<ul>\n<li><a href=\"#pipeline\">The CAD Pipeline</a></li>\n<li><a href=\"#detection\">Stage 1 — Detection</a></li>\n<li><a href=\"#incident\">Stage 2 — Incident &amp;amp; Scoring</a></li>\n<li><a href=\"#review\">Stage 3 — Dispatcher Review</a></li>\n<li><a href=\"#assignment\">Stage 4 — Crime &amp;amp; Assignment</a></li>\n<li><a href=\"#response\">Stage 5 — Field Response</a></li>\n<li><a href=\"#escalation\">Stage 6 — Escalation</a></li>\n<li><a href=\"#entry-points\">Alternative Entry Points</a></li>\n</ul>\n<h2 id=\"pipeline\"><a class=\"anchor\" href=\"#pipeline\">#</a>The CAD Pipeline</h2>\n<div class=\"code-card\"><div class=\"code-toolbar\"><span>text</span><button class=\"copy-btn\" type=\"button\">Copy</button></div><pre><code class=\"language-text\"> ┌────────────┐   alert/crime   ┌──────────────────────┐\n │ AI Service │ ───────────────▶│  Incident            │\n └────────────┘                 │  status=pending_review│\n                                └──────────┬────────────┘\n                                           │ Priority Engine scores it\n                                           ▼\n                                ┌──────────────────────┐\n                       ┌────────│   Dispatcher Console  │────────┐\n                       │        └──────────────────────┘        │\n                  reject│                                        │dispatch\n                       ▼                                         ▼\n            ┌────────────────────┐                  ┌────────────────────────┐\n            │ rejected_false_alarm│                 │  Crime (status=assigned)│\n            └────────────────────┘                  │  + nearest officer      │\n                                                     └───────────┬────────────┘\n                                                                 │ FCM + Echo\n                                                                 ▼\n                                        accept → in_progress → arrived → resolved\n                                                                 │\n                                            no response in N min │\n                                                                 ▼\n                                                      escalate → next officer</code></pre></div>\n<h2 id=\"detection\"><a class=\"anchor\" href=\"#detection\">#</a>Stage 1 — Detection</h2>\n<p>The AI service watches the gateway RTSP streams of its assigned cameras. As confidence rises it raises a <strong>suspicious-activity alert</strong> (<code>POST /api/v1/model/alert</code>); when it confirms an event it posts a <strong>crime detection</strong> (<code>POST /api/v1/model/crime</code>) with <code>camera_id</code>, <code>description</code>, a start/end time window, and <code>confidence_score</code>. The current request schema does not include <code>crime_type</code>.</p>\n<p>The AI never touches a crime record or a camera command — it only hands the backend a report. See <a href=\"#/docs/ai-integration\">AI Model Integration</a>.</p>\n<h2 id=\"incident\"><a class=\"anchor\" href=\"#incident\">#</a>Stage 2 — Incident &amp; Scoring</h2>\n<p>The backend creates an <strong><code>Incident</code></strong> in <code>pending_review</code> status, tagged with its source (<code>ai_alert</code>, <code>ai_crime</code>, <code>citizen</code>, <code>manual</code>), camera, station, and location. The <a href=\"#/docs/priority-engine\">Priority Engine</a> immediately computes a <code>priority_score</code> in <code>[0,1]</code> from a transparent weighted formula and assigns a tier:</p>\n<div class=\"table-shell\"><table><thead><tr><th>Tier</th><th>Score</th><th>Meaning</th></tr></thead><tbody><tr><td><strong>Critical</strong></td><td>≥ 0.85</td><td>Eligible for auto-dispatch when the weapon + confidence gates pass</td></tr><tr><td><strong>High</strong></td><td>0.65 – 0.85</td><td>Top of the queue</td></tr><tr><td><strong>Medium</strong></td><td>0.45 – 0.65</td><td>Standard review</td></tr><tr><td><strong>Low</strong></td><td>&lt; 0.45</td><td>Background</td></tr></tbody></table></div>\n<p>The full factor breakdown is stored as JSON (<code>priority_factors</code>) so any score can be explained and audited later.</p>\n<h2 id=\"review\"><a class=\"anchor\" href=\"#review\">#</a>Stage 3 — Dispatcher Review</h2>\n<p>The incident appears <strong>in real time</strong> on the Dispatcher Console queue (broadcast via the <code>station.{id}</code> channel). A dispatcher:</p>\n<ol>\n<li><strong>Claims</strong> the incident under their own identity (so colleagues see it is being handled).</li>\n<li>Reviews the available snapshot/camera context, map, priority factors, and pre-arrival brief.</li>\n<li><strong>Dispatches</strong> it (→ Stage 4) or <strong>rejects</strong> it as a false alarm.</li>\n</ol>\n<p>A narrow <strong>auto-dispatch policy</strong> can skip manual review only for the highest-confidence, weapon-bearing critical incidents; everything else waits for a human. Claim/release and review actions are broadcast so every operator's screen stays consistent.</p>\n<h2 id=\"assignment\"><a class=\"anchor\" href=\"#assignment\">#</a>Stage 4 — Crime &amp; Assignment</h2>\n<p>On dispatch the backend creates a <strong><code>Crime</code></strong> record (linked to its originating incident), then finds the <strong>nearest available officer</strong> — same station, on shift, within the configured radius — using live GPS from Redis and a Haversine distance. The officer is assigned (<code>status = assigned</code>) and notified via <strong>Firebase push</strong> and an <strong>Echo</strong> event to the station.</p>\n<h2 id=\"response\"><a class=\"anchor\" href=\"#response\">#</a>Stage 5 — Field Response</h2>\n<p>The officer receives the crime with its location and brief, plus evidence when a Scene is available, and drives the lifecycle from the mobile app:</p>\n<div class=\"code-card\"><div class=\"code-toolbar\"><span>text</span><button class=\"copy-btn\" type=\"button\">Copy</button></div><pre><code class=\"language-text\">assigned → accept → in_progress → arrive → visited → resolve → resolved\n                 └─ decline → not_visited (reason required)</code></pre></div>\n<p>Status changes broadcast back to the station console and update the live map and timeline.</p>\n<h2 id=\"escalation\"><a class=\"anchor\" href=\"#escalation\">#</a>Stage 6 — Escalation</h2>\n<p>The <strong>escalation service</strong> can reassign a stale Crime to another officer, increment the escalation counter, and notify the station. Its Artisan command exists, but it is not currently registered in the Laravel scheduler. Stale <strong>dispatcher claims</strong>, by contrast, are actively auto-released every minute.</p>\n<h2 id=\"entry-points\"><a class=\"anchor\" href=\"#entry-points\">#</a>Alternative Entry Points</h2>\n<p>Not every incident starts with the AI. The same pipeline absorbs:</p>\n<ul>\n<li><strong>Citizen tips</strong> — a public web/SMS report becomes a tip; a dispatcher promotes it into an incident. See <a href=\"#/docs/citizen-tips\">Citizen Tips</a>.</li>\n<li><strong>Manual incidents</strong> — a dispatcher creates an incident directly from a phone call or beat report.</li>\n</ul>\n<p>Both sources join the dispatcher queue and follow the same dispatcher → crime → officer path. Manual and citizen-originated incidents currently do not run the same automatic priority calculation as AI incidents. Continue to <a href=\"#/docs/dispatch\">Incident &amp;amp; Dispatch</a>.</p>", "headings": [{"level": 1, "title": "End-to-End Workflow", "id": "end-to-end-workflow"}, {"level": 2, "title": "The CAD Pipeline", "id": "pipeline"}, {"level": 2, "title": "Stage 1 — Detection", "id": "detection"}, {"level": 2, "title": "Stage 2 — Incident & Scoring", "id": "incident"}, {"level": 2, "title": "Stage 3 — Dispatcher Review", "id": "review"}, {"level": 2, "title": "Stage 4 — Crime & Assignment", "id": "assignment"}, {"level": 2, "title": "Stage 5 — Field Response", "id": "response"}, {"level": 2, "title": "Stage 6 — Escalation", "id": "escalation"}, {"level": 2, "title": "Alternative Entry Points", "id": "entry-points"}]}, {"slug": "security", "title": "Authentication & Security", "section": "Concepts", "description": "Authentication & Security CrimeLens handles sensitive law enforcement data and can command physical devices. Its security posture is defense in depth — no single control is trusted alone. Mu…", "raw": "# Authentication & Security\n\n---\n\nCrimeLens handles sensitive law-enforcement data and can command physical devices. Its security posture is **defense in depth** — no single control is trusted alone.\n\n- [Multi-Guard Authentication](#guards)\n- [Machine-Client Hardening](#machine)\n- [Encryption](#encryption)\n- [Signed Media URLs](#signed)\n- [Multi-Tenant Isolation](#tenancy)\n- [Role-Based Access Control](#rbac)\n- [The Decision Ledger](#ledger)\n- [Rate Limiting & Forced Rotation](#rate)\n\n<a name=\"guards\"></a>\n## Multi-Guard Authentication\n\nEight Laravel guards run off one application, each scoped to a user type and transport:\n\n| Guard | Driver | For |\n|-------|--------|-----|\n| `admin` | session | Admin console |\n| `police_station_web` | session | Station console (institutional) |\n| `station_user_web` | session | Station console (individual dispatcher) |\n| `police_station` | sanctum | Station companion mobile |\n| `officer` | sanctum | Officer mobile |\n| `ai_model` | sanctum | AI detection service |\n| `api` | sanctum | Default API |\n| `web` | session | Framework default |\n\nSession guards protect the web consoles (CSRF-protected); Sanctum bearer guards protect mobile and machine clients. Tokens are issued at login with a bounded TTL and revoked on logout.\n\n<a name=\"machine\"></a>\n## Machine-Client Hardening\n\nThe two non-human actors get extra layers:\n\n**AI detection service**\n- **IP allow-listing** — every request (including login) must originate from the model's whitelisted `ip_address` (`hash_equals`, **403** on mismatch).\n- **HMAC request signing** — `/alert` and `/crime` carry an `X-Signature` (SHA-256 over the body with the model's `signing_secret`), an `X-Timestamp` (±60 s), and an `X-Nonce` (Redis-cached 5 min to block replays).\n\n**Camera gateway**\n- All `/api/camera/*` control routes and the tamper webhook require gateway **IP allow-listing** plus HMAC headers (`X-Signature`, `X-Timestamp`, `X-Nonce`) and are rate-limited.\n- Laravel-to-Python gateway requests use `X-Gateway-Token`. This authenticates the internal service call but does not encrypt the network, so credential-bearing traffic must stay on localhost/private networking/VPN or HTTPS.\n\n<a name=\"encryption\"></a>\n## Encryption\n\n- **Per-session credential encryption** — camera stream URLs/credentials returned to the AI are encrypted with **AES-256-CBC + HMAC** using a key regenerated on every login, so an intercepted response expires at the next login.\n- **Encryption at rest** — camera credentials (`user_name`, `password`, `tapo_*`) and the model `signing_secret` use Laravel's `encrypted` cast.\n- Passwords are `hashed`; encryption keys are hidden from serialization.\n\n<a name=\"signed\"></a>\n## Signed Media URLs\n\nEvidence and media are private and served only through **signed URLs** with bounded TTLs — HLS playlists/segments, crime scene clips, tamper sample frames, body-cam footage, chat voice notes, and citizen-tip media. Citizen-tip media uses a **relative** signature so an `<img>`/`<video>` works across hosts (localhost vs a tunnelled public URL) without leaking the domain into the signature. Streaming routes deliberately bypass session/CSRF middleware so players can fetch them statelessly.\n\n<a name=\"tenancy\"></a>\n## Multi-Tenant Isolation\n\nA station can only ever see its own data. Isolation is enforced at three layers: query scoping (every station query is filtered by `police_station_id`), explicit ownership checks (cross-station access returns **404**), and **channel authorization** — private broadcast channels (`station.{id}`, `officer.{id}`, `panic.*`) authorise only the owning tenant/person, so isolation holds even on the WebSocket layer.\n\n<a name=\"rbac\"></a>\n## Role-Based Access Control\n\nWithin a station, `spatie/laravel-permission` grants fine-grained roles/permissions to `station_users` (e.g. `incidents.dispatch`, `chat.send`, `incidents.create_manual`). The institutional account is supervisory; individual users act under their own permissioned identity.\n\n<a name=\"ledger\"></a>\n## The Decision Ledger\n\nEvery consequential action — dispatch, reject, claim, escalation, tip promotion — is written to an **append-only, hash-chained** `ledger_entries` table: each row stores the `previous_hash` and its own `hash`, forming a tamper-evident chain. Database triggers enforce immutability, and the scheduled `ledger:verify` command re-walks the chain to detect any break. This is the \"defensible audit trail\" that lets CrimeLens answer *who decided what, when, and why* — a differentiator over phone-call-first CAD systems.\n\n<a name=\"rate\"></a>\n## Rate Limiting & Forced Rotation\n\nSensitive endpoints are throttled (login `5/min`, dispatch/reject `30/min`, panic `1/30 s`, body-cam `5/min`, …). New station/officer accounts carry `must_change_password`, and a `force_password_change` middleware blocks the console until the password is rotated.\n\nContinue to [Background Jobs & Scheduling](/{{route}}/{{version}}/jobs).\n", "html": "<h1 id=\"authentication-security\"><a class=\"anchor\" href=\"#authentication-security\">#</a>Authentication &amp; Security</h1>\n<hr>\n<p>CrimeLens handles sensitive law-enforcement data and can command physical devices. Its security posture is <strong>defense in depth</strong> — no single control is trusted alone.</p>\n<ul>\n<li><a href=\"#guards\">Multi-Guard Authentication</a></li>\n<li><a href=\"#machine\">Machine-Client Hardening</a></li>\n<li><a href=\"#encryption\">Encryption</a></li>\n<li><a href=\"#signed\">Signed Media URLs</a></li>\n<li><a href=\"#tenancy\">Multi-Tenant Isolation</a></li>\n<li><a href=\"#rbac\">Role-Based Access Control</a></li>\n<li><a href=\"#ledger\">The Decision Ledger</a></li>\n<li><a href=\"#rate\">Rate Limiting &amp;amp; Forced Rotation</a></li>\n</ul>\n<h2 id=\"guards\"><a class=\"anchor\" href=\"#guards\">#</a>Multi-Guard Authentication</h2>\n<p>Eight Laravel guards run off one application, each scoped to a user type and transport:</p>\n<div class=\"table-shell\"><table><thead><tr><th>Guard</th><th>Driver</th><th>For</th></tr></thead><tbody><tr><td><code>admin</code></td><td>session</td><td>Admin console</td></tr><tr><td><code>police_station_web</code></td><td>session</td><td>Station console (institutional)</td></tr><tr><td><code>station_user_web</code></td><td>session</td><td>Station console (individual dispatcher)</td></tr><tr><td><code>police_station</code></td><td>sanctum</td><td>Station companion mobile</td></tr><tr><td><code>officer</code></td><td>sanctum</td><td>Officer mobile</td></tr><tr><td><code>ai_model</code></td><td>sanctum</td><td>AI detection service</td></tr><tr><td><code>api</code></td><td>sanctum</td><td>Default API</td></tr><tr><td><code>web</code></td><td>session</td><td>Framework default</td></tr></tbody></table></div>\n<p>Session guards protect the web consoles (CSRF-protected); Sanctum bearer guards protect mobile and machine clients. Tokens are issued at login with a bounded TTL and revoked on logout.</p>\n<h2 id=\"machine\"><a class=\"anchor\" href=\"#machine\">#</a>Machine-Client Hardening</h2>\n<p>The two non-human actors get extra layers:</p>\n<p><strong>AI detection service</strong></p>\n<ul>\n<li><strong>IP allow-listing</strong> — every request (including login) must originate from the model's whitelisted <code>ip_address</code> (<code>hash_equals</code>, <strong>403</strong> on mismatch).</li>\n<li><strong>HMAC request signing</strong> — <code>/alert</code> and <code>/crime</code> carry an <code>X-Signature</code> (SHA-256 over the body with the model's <code>signing_secret</code>), an <code>X-Timestamp</code> (±60 s), and an <code>X-Nonce</code> (Redis-cached 5 min to block replays).</li>\n</ul>\n<p><strong>Camera gateway</strong></p>\n<ul>\n<li>All <code>/api/camera/*</code> control routes and the tamper webhook require gateway <strong>IP allow-listing</strong> plus HMAC headers (<code>X-Signature</code>, <code>X-Timestamp</code>, <code>X-Nonce</code>) and are rate-limited.</li>\n<li>Laravel-to-Python gateway requests use <code>X-Gateway-Token</code>. This authenticates the internal service call but does not encrypt the network, so credential-bearing traffic must stay on localhost/private networking/VPN or HTTPS.</li>\n</ul>\n<h2 id=\"encryption\"><a class=\"anchor\" href=\"#encryption\">#</a>Encryption</h2>\n<ul>\n<li><strong>Per-session credential encryption</strong> — camera stream URLs/credentials returned to the AI are encrypted with <strong>AES-256-CBC + HMAC</strong> using a key regenerated on every login, so an intercepted response expires at the next login.</li>\n<li><strong>Encryption at rest</strong> — camera credentials (<code>user_name</code>, <code>password</code>, <code>tapo_*</code>) and the model <code>signing_secret</code> use Laravel's <code>encrypted</code> cast.</li>\n<li>Passwords are <code>hashed</code>; encryption keys are hidden from serialization.</li>\n</ul>\n<h2 id=\"signed\"><a class=\"anchor\" href=\"#signed\">#</a>Signed Media URLs</h2>\n<p>Evidence and media are private and served only through <strong>signed URLs</strong> with bounded TTLs — HLS playlists/segments, crime scene clips, tamper sample frames, body-cam footage, chat voice notes, and citizen-tip media. Citizen-tip media uses a <strong>relative</strong> signature so an <code>&amp;lt;img&amp;gt;</code>/<code>&amp;lt;video&amp;gt;</code> works across hosts (localhost vs a tunnelled public URL) without leaking the domain into the signature. Streaming routes deliberately bypass session/CSRF middleware so players can fetch them statelessly.</p>\n<h2 id=\"tenancy\"><a class=\"anchor\" href=\"#tenancy\">#</a>Multi-Tenant Isolation</h2>\n<p>A station can only ever see its own data. Isolation is enforced at three layers: query scoping (every station query is filtered by <code>police_station_id</code>), explicit ownership checks (cross-station access returns <strong>404</strong>), and <strong>channel authorization</strong> — private broadcast channels (<code>station.{id}</code>, <code>officer.{id}</code>, <code>panic.*</code>) authorise only the owning tenant/person, so isolation holds even on the WebSocket layer.</p>\n<h2 id=\"rbac\"><a class=\"anchor\" href=\"#rbac\">#</a>Role-Based Access Control</h2>\n<p>Within a station, <code>spatie/laravel-permission</code> grants fine-grained roles/permissions to <code>station_users</code> (e.g. <code>incidents.dispatch</code>, <code>chat.send</code>, <code>incidents.create_manual</code>). The institutional account is supervisory; individual users act under their own permissioned identity.</p>\n<h2 id=\"ledger\"><a class=\"anchor\" href=\"#ledger\">#</a>The Decision Ledger</h2>\n<p>Every consequential action — dispatch, reject, claim, escalation, tip promotion — is written to an <strong>append-only, hash-chained</strong> <code>ledger_entries</code> table: each row stores the <code>previous_hash</code> and its own <code>hash</code>, forming a tamper-evident chain. Database triggers enforce immutability, and the scheduled <code>ledger:verify</code> command re-walks the chain to detect any break. This is the \"defensible audit trail\" that lets CrimeLens answer <em>who decided what, when, and why</em> — a differentiator over phone-call-first CAD systems.</p>\n<h2 id=\"rate\"><a class=\"anchor\" href=\"#rate\">#</a>Rate Limiting &amp; Forced Rotation</h2>\n<p>Sensitive endpoints are throttled (login <code>5/min</code>, dispatch/reject <code>30/min</code>, panic <code>1/30 s</code>, body-cam <code>5/min</code>, …). New station/officer accounts carry <code>must_change_password</code>, and a <code>force_password_change</code> middleware blocks the console until the password is rotated.</p>\n<p>Continue to <a href=\"#/docs/jobs\">Background Jobs &amp;amp; Scheduling</a>.</p>", "headings": [{"level": 1, "title": "Authentication & Security", "id": "authentication-security"}, {"level": 2, "title": "Multi-Guard Authentication", "id": "guards"}, {"level": 2, "title": "Machine-Client Hardening", "id": "machine"}, {"level": 2, "title": "Encryption", "id": "encryption"}, {"level": 2, "title": "Signed Media URLs", "id": "signed"}, {"level": 2, "title": "Multi-Tenant Isolation", "id": "tenancy"}, {"level": 2, "title": "Role-Based Access Control", "id": "rbac"}, {"level": 2, "title": "The Decision Ledger", "id": "ledger"}, {"level": 2, "title": "Rate Limiting & Forced Rotation", "id": "rate"}]}, {"slug": "data-model", "title": "Data Model", "section": "Concepts", "description": "Data Model CrimeLens persists to PostgreSQL + PostGIS across roughly 35 tables. This page groups them by domain, lists the columns that matter, and calls out the enums and spatial columns th…", "raw": "# Data Model\n\n---\n\nCrimeLens persists to **PostgreSQL + PostGIS** across roughly 35 tables. This page groups them by domain, lists the columns that matter, and calls out the enums and spatial columns that make the system work.\n\n- [Spatial & Enum Conventions](#conventions)\n- [Incident & Dispatch](#incident)\n- [Cameras & AI Detection](#cameras)\n- [Officers & Field Ops](#officers)\n- [Stations, Users & Admins](#stations)\n- [Citizen Engagement](#citizen)\n- [Evidence & Media](#evidence)\n- [Communications](#comms)\n- [Audit & Ledger](#audit)\n- [Configuration](#config)\n\n<a name=\"conventions\"></a>\n## Spatial & Enum Conventions\n\n- **Spatial** — `cameras.location` and `officers.current_location` are PostGIS `geography(Point, 4326)` columns with GiST indexes, kept in sync with their `lat`/`lang` pairs. They power nearest-officer/nearest-camera and heatmap queries.\n- **Enums** — every status/type column is a PHP backed enum cast on the model (e.g. `IncidentStatus`, `CrimeStatus`, `CrimeSeverity`, `OfficerStatus`, `StorageType`).\n- **Polymorphism** — `created_by`, `loggable`, `notifiable`, `tokenable`, `resettable`, and chat `sender`/`receiver` are polymorphic so a single table serves admins, stations, officers, and AI models.\n\n<a name=\"incident\"></a>\n## Incident & Dispatch\n\n| Table | Key columns | Notes |\n|-------|-------------|-------|\n| `incidents` | source, status, priority_score, priority_tier, priority_factors (json), confidence_score, camera_id, ai_model_id, police_station_id, crime_id, assigned_dispatcher_id, created_by (morph) | The review queue. Source `ai_alert`/`ai_crime`/`manual`/`citizen`; status `pending_review`/`dispatched`/`rejected_false_alarm`/`expired`. |\n| `crimes` | officer_id, camera_id, incident_id, scene_id, status, severity, confidence_score, priority_score_snapshot, accepted_at, officer_arrive_time, resolved_at, response_time_minutes, escalation_count | The committed response. Status `pending`/`assigned`/`in_progress`/`not_visited`/`resolved`/`escalated`/`false_alarm`. |\n| `crime_types` | name, slug, severity_weight | Seeded catalogue + the per-type weight used by the priority engine. |\n| `incident_links` | incident_a_id, incident_b_id, link_type | Dispatcher-drawn links between related incidents. |\n| `pattern_alerts` | station_id, rule_key, geohash, incident_ids (jsonb), status | Output of cross-incident pattern detection (e.g. weapon clusters). |\n\n<a name=\"cameras\"></a>\n## Cameras & AI Detection\n\n| Table | Key columns | Notes |\n|-------|-------------|-------|\n| `cameras` | police_station_id, name, ip_address, gateway_stream_key, encrypted credentials, stream_path (json), storage_type, coverage_* , lat/lang, `location` (PostGIS), is_active, last_seen_at | The device. |\n| `camera_ai_model` | camera_id, ai_model_id | Assignment pivot (unique pair). |\n| `ai_models` | name, email, ip_address, encryption_key, signing_secret (encrypted), is_active, last_heartbeat_at | The detection service identity. |\n| `camera_detection_filters` | camera_id, crime_type_id, min_confidence, is_enabled | Per-camera detection thresholds. |\n| `camera_tamper_events` | camera_id, signal_type, status, started_at, ended_at, detection_metric, sample_frame_path, acknowledged_by_admin_id | Tamper signals (`blackout`/`variance_collapse`/`fov_shift`/`static_stream`/`heartbeat_gap`). |\n| `ai_detection_logs` | camera_id, crime_type_id, ai_model_id, confidence, decision, detected_at | Every detection + the filter decision — feeds model analytics. |\n\n<a name=\"officers\"></a>\n## Officers & Field Ops\n\n| Table | Key columns | Notes |\n|-------|-------------|-------|\n| `officers` | police_station_id, name, email, rank, badge_number, status, is_on_shift, encryption_key, `current_location` (PostGIS) | The responder. |\n| `officer_shifts` | officer_id, shift_start, shift_end, patrol_polygon (json), zone_name, is_active | Shift windows + patrol zones. |\n| `officer_status_logs` | officer_id, status, reason, started_at, ended_at | Status-change accountability. |\n| `officer_activity_logs` | officer_id, date, encoded_route, total_distance_km, location_updates_count | Daily polyline-encoded patrol history. |\n| `panic_events` | officer_id, station_id, lat, lng, status, started_at, ended_at, audio_recording_path | Officer-safety SOS. |\n| `bolos` | police_station_id, subject_type, description, photo_path, area_lat/lng, radius_meters, severity, expires_at, is_active, created_by (morph) | Be-On-the-Look-Out broadcasts. |\n\n<a name=\"stations\"></a>\n## Stations, Users & Admins\n\n| Table | Key columns | Notes |\n|-------|-------------|-------|\n| `police_stations` | name, email, phone, address, city, governorate, latitude, longitude, must_change_password | The tenant + institutional login. |\n| `station_users` | police_station_id, name, email, is_active, last_login_at, created_by_police_station_id | Individual dispatchers within a station. |\n| `admins` | name, email, phone | System administrators. |\n| `admin_station` | admin_id, police_station_id | Optional admin→station scoping. |\n\n<a name=\"citizen\"></a>\n## Citizen Engagement\n\n| Table | Key columns | Notes |\n|-------|-------------|-------|\n| `citizen_tips` | station_id, source, sender_identifier, message, media_paths (jsonb), lat/lng, reported_address, status, promoted_incident_id, reviewed_by_station_user_id, expires_at | Public intake + triage. Status `pending`/`in_review`/`promoted`/`dismissed`/`expired`. |\n\n<a name=\"evidence\"></a>\n## Evidence & Media\n\n| Table | Key columns | Notes |\n|-------|-------------|-------|\n| `scenses` | crime_id, sence_path, start_date, end_date | Extracted camera evidence clip per crime. |\n| `body_cam_uploads` | crime_id, officer_id, media_path, duration_seconds, file_size_bytes, mime_type | Officer body-cam footage. |\n\n<a name=\"comms\"></a>\n## Communications\n\n| Table | Key columns | Notes |\n|-------|-------------|-------|\n| `chat_messages` | conversation_key, sender (morph), receiver (morph), message, media_path, message_type, media_type, is_read | Hierarchical chat (`text`/`image`/`voice`/`quick_reply`). |\n| `notifications` | (uuid) type, notifiable (morph), data, notification_type, is_read | Laravel database notifications. |\n| `firebase_tokens` | token, tokenable (morph), device_type | FCM device registration. |\n\n<a name=\"audit\"></a>\n## Audit & Ledger\n\n| Table | Key columns | Notes |\n|-------|-------------|-------|\n| `ledger_entries` | event_type, subject (type/id), actor (type/id), payload (jsonb), previous_hash, hash, recorded_at | **Append-only, hash-chained** decision ledger; immutability enforced at the DB level and verified by `ledger:verify`. |\n| `activity_logs` | loggable (morph), action, description, properties (json), ip_address | General audit trail. |\n\n<a name=\"config\"></a>\n## Configuration\n\n| Table | Key columns | Notes |\n|-------|-------------|-------|\n| `settings` | key, value, group | Key/value system config, Redis-cached. |\n| `password_reset_codes` | resettable (morph), code, token, is_verified, expires_at | Multi-guard password-reset codes. |\n\n> {info} The relationships above form the spine: `Camera → Incident → Crime → Officer`, with `Scene`/`BodyCamUpload` hanging off the crime, `CitizenTip`/`PatternAlert` feeding the incident queue, and `LedgerEntry`/`ActivityLog` recording every consequential step.\n\n> {warning} The current `incidents` table does not persist the AI report's `start_time` and `end_time`. Also, persisted priority factors use the key `weapon`, while one pattern-detection path currently reads `weapon_detected`.\n\nContinue to the [API Reference](/{{route}}/{{version}}/api).\n", "html": "<h1 id=\"data-model\"><a class=\"anchor\" href=\"#data-model\">#</a>Data Model</h1>\n<hr>\n<p>CrimeLens persists to <strong>PostgreSQL + PostGIS</strong> across roughly 35 tables. This page groups them by domain, lists the columns that matter, and calls out the enums and spatial columns that make the system work.</p>\n<ul>\n<li><a href=\"#conventions\">Spatial &amp;amp; Enum Conventions</a></li>\n<li><a href=\"#incident\">Incident &amp;amp; Dispatch</a></li>\n<li><a href=\"#cameras\">Cameras &amp;amp; AI Detection</a></li>\n<li><a href=\"#officers\">Officers &amp;amp; Field Ops</a></li>\n<li><a href=\"#stations\">Stations, Users &amp;amp; Admins</a></li>\n<li><a href=\"#citizen\">Citizen Engagement</a></li>\n<li><a href=\"#evidence\">Evidence &amp;amp; Media</a></li>\n<li><a href=\"#comms\">Communications</a></li>\n<li><a href=\"#audit\">Audit &amp;amp; Ledger</a></li>\n<li><a href=\"#config\">Configuration</a></li>\n</ul>\n<h2 id=\"conventions\"><a class=\"anchor\" href=\"#conventions\">#</a>Spatial &amp; Enum Conventions</h2>\n<ul>\n<li><strong>Spatial</strong> — <code>cameras.location</code> and <code>officers.current_location</code> are PostGIS <code>geography(Point, 4326)</code> columns with GiST indexes, kept in sync with their <code>lat</code>/<code>lang</code> pairs. They power nearest-officer/nearest-camera and heatmap queries.</li>\n<li><strong>Enums</strong> — every status/type column is a PHP backed enum cast on the model (e.g. <code>IncidentStatus</code>, <code>CrimeStatus</code>, <code>CrimeSeverity</code>, <code>OfficerStatus</code>, <code>StorageType</code>).</li>\n<li><strong>Polymorphism</strong> — <code>created_by</code>, <code>loggable</code>, <code>notifiable</code>, <code>tokenable</code>, <code>resettable</code>, and chat <code>sender</code>/<code>receiver</code> are polymorphic so a single table serves admins, stations, officers, and AI models.</li>\n</ul>\n<h2 id=\"incident\"><a class=\"anchor\" href=\"#incident\">#</a>Incident &amp; Dispatch</h2>\n<div class=\"table-shell\"><table><thead><tr><th>Table</th><th>Key columns</th><th>Notes</th></tr></thead><tbody><tr><td><code>incidents</code></td><td>source, status, priority_score, priority_tier, priority_factors (json), confidence_score, camera_id, ai_model_id, police_station_id, crime_id, assigned_dispatcher_id, created_by (morph)</td><td>The review queue. Source <code>ai_alert</code>/<code>ai_crime</code>/<code>manual</code>/<code>citizen</code>; status <code>pending_review</code>/<code>dispatched</code>/<code>rejected_false_alarm</code>/<code>expired</code>.</td></tr><tr><td><code>crimes</code></td><td>officer_id, camera_id, incident_id, scene_id, status, severity, confidence_score, priority_score_snapshot, accepted_at, officer_arrive_time, resolved_at, response_time_minutes, escalation_count</td><td>The committed response. Status <code>pending</code>/<code>assigned</code>/<code>in_progress</code>/<code>not_visited</code>/<code>resolved</code>/<code>escalated</code>/<code>false_alarm</code>.</td></tr><tr><td><code>crime_types</code></td><td>name, slug, severity_weight</td><td>Seeded catalogue + the per-type weight used by the priority engine.</td></tr><tr><td><code>incident_links</code></td><td>incident_a_id, incident_b_id, link_type</td><td>Dispatcher-drawn links between related incidents.</td></tr><tr><td><code>pattern_alerts</code></td><td>station_id, rule_key, geohash, incident_ids (jsonb), status</td><td>Output of cross-incident pattern detection (e.g. weapon clusters).</td></tr></tbody></table></div>\n<h2 id=\"cameras\"><a class=\"anchor\" href=\"#cameras\">#</a>Cameras &amp; AI Detection</h2>\n<div class=\"table-shell\"><table><thead><tr><th>Table</th><th>Key columns</th><th>Notes</th></tr></thead><tbody><tr><td><code>cameras</code></td><td>police_station_id, name, ip_address, gateway_stream_key, encrypted credentials, stream_path (json), storage_type, coverage_* , lat/lang, <code>location</code> (PostGIS), is_active, last_seen_at</td><td>The device.</td></tr><tr><td><code>camera_ai_model</code></td><td>camera_id, ai_model_id</td><td>Assignment pivot (unique pair).</td></tr><tr><td><code>ai_models</code></td><td>name, email, ip_address, encryption_key, signing_secret (encrypted), is_active, last_heartbeat_at</td><td>The detection service identity.</td></tr><tr><td><code>camera_detection_filters</code></td><td>camera_id, crime_type_id, min_confidence, is_enabled</td><td>Per-camera detection thresholds.</td></tr><tr><td><code>camera_tamper_events</code></td><td>camera_id, signal_type, status, started_at, ended_at, detection_metric, sample_frame_path, acknowledged_by_admin_id</td><td>Tamper signals (<code>blackout</code>/<code>variance_collapse</code>/<code>fov_shift</code>/<code>static_stream</code>/<code>heartbeat_gap</code>).</td></tr><tr><td><code>ai_detection_logs</code></td><td>camera_id, crime_type_id, ai_model_id, confidence, decision, detected_at</td><td>Every detection + the filter decision — feeds model analytics.</td></tr></tbody></table></div>\n<h2 id=\"officers\"><a class=\"anchor\" href=\"#officers\">#</a>Officers &amp; Field Ops</h2>\n<div class=\"table-shell\"><table><thead><tr><th>Table</th><th>Key columns</th><th>Notes</th></tr></thead><tbody><tr><td><code>officers</code></td><td>police_station_id, name, email, rank, badge_number, status, is_on_shift, encryption_key, <code>current_location</code> (PostGIS)</td><td>The responder.</td></tr><tr><td><code>officer_shifts</code></td><td>officer_id, shift_start, shift_end, patrol_polygon (json), zone_name, is_active</td><td>Shift windows + patrol zones.</td></tr><tr><td><code>officer_status_logs</code></td><td>officer_id, status, reason, started_at, ended_at</td><td>Status-change accountability.</td></tr><tr><td><code>officer_activity_logs</code></td><td>officer_id, date, encoded_route, total_distance_km, location_updates_count</td><td>Daily polyline-encoded patrol history.</td></tr><tr><td><code>panic_events</code></td><td>officer_id, station_id, lat, lng, status, started_at, ended_at, audio_recording_path</td><td>Officer-safety SOS.</td></tr><tr><td><code>bolos</code></td><td>police_station_id, subject_type, description, photo_path, area_lat/lng, radius_meters, severity, expires_at, is_active, created_by (morph)</td><td>Be-On-the-Look-Out broadcasts.</td></tr></tbody></table></div>\n<h2 id=\"stations\"><a class=\"anchor\" href=\"#stations\">#</a>Stations, Users &amp; Admins</h2>\n<div class=\"table-shell\"><table><thead><tr><th>Table</th><th>Key columns</th><th>Notes</th></tr></thead><tbody><tr><td><code>police_stations</code></td><td>name, email, phone, address, city, governorate, latitude, longitude, must_change_password</td><td>The tenant + institutional login.</td></tr><tr><td><code>station_users</code></td><td>police_station_id, name, email, is_active, last_login_at, created_by_police_station_id</td><td>Individual dispatchers within a station.</td></tr><tr><td><code>admins</code></td><td>name, email, phone</td><td>System administrators.</td></tr><tr><td><code>admin_station</code></td><td>admin_id, police_station_id</td><td>Optional admin→station scoping.</td></tr></tbody></table></div>\n<h2 id=\"citizen\"><a class=\"anchor\" href=\"#citizen\">#</a>Citizen Engagement</h2>\n<div class=\"table-shell\"><table><thead><tr><th>Table</th><th>Key columns</th><th>Notes</th></tr></thead><tbody><tr><td><code>citizen_tips</code></td><td>station_id, source, sender_identifier, message, media_paths (jsonb), lat/lng, reported_address, status, promoted_incident_id, reviewed_by_station_user_id, expires_at</td><td>Public intake + triage. Status <code>pending</code>/<code>in_review</code>/<code>promoted</code>/<code>dismissed</code>/<code>expired</code>.</td></tr></tbody></table></div>\n<h2 id=\"evidence\"><a class=\"anchor\" href=\"#evidence\">#</a>Evidence &amp; Media</h2>\n<div class=\"table-shell\"><table><thead><tr><th>Table</th><th>Key columns</th><th>Notes</th></tr></thead><tbody><tr><td><code>scenses</code></td><td>crime_id, sence_path, start_date, end_date</td><td>Extracted camera evidence clip per crime.</td></tr><tr><td><code>body_cam_uploads</code></td><td>crime_id, officer_id, media_path, duration_seconds, file_size_bytes, mime_type</td><td>Officer body-cam footage.</td></tr></tbody></table></div>\n<h2 id=\"comms\"><a class=\"anchor\" href=\"#comms\">#</a>Communications</h2>\n<div class=\"table-shell\"><table><thead><tr><th>Table</th><th>Key columns</th><th>Notes</th></tr></thead><tbody><tr><td><code>chat_messages</code></td><td>conversation_key, sender (morph), receiver (morph), message, media_path, message_type, media_type, is_read</td><td>Hierarchical chat (<code>text</code>/<code>image</code>/<code>voice</code>/<code>quick_reply</code>).</td></tr><tr><td><code>notifications</code></td><td>(uuid) type, notifiable (morph), data, notification_type, is_read</td><td>Laravel database notifications.</td></tr><tr><td><code>firebase_tokens</code></td><td>token, tokenable (morph), device_type</td><td>FCM device registration.</td></tr></tbody></table></div>\n<h2 id=\"audit\"><a class=\"anchor\" href=\"#audit\">#</a>Audit &amp; Ledger</h2>\n<div class=\"table-shell\"><table><thead><tr><th>Table</th><th>Key columns</th><th>Notes</th></tr></thead><tbody><tr><td><code>ledger_entries</code></td><td>event_type, subject (type/id), actor (type/id), payload (jsonb), previous_hash, hash, recorded_at</td><td><strong>Append-only, hash-chained</strong> decision ledger; immutability enforced at the DB level and verified by <code>ledger:verify</code>.</td></tr><tr><td><code>activity_logs</code></td><td>loggable (morph), action, description, properties (json), ip_address</td><td>General audit trail.</td></tr></tbody></table></div>\n<h2 id=\"config\"><a class=\"anchor\" href=\"#config\">#</a>Configuration</h2>\n<div class=\"table-shell\"><table><thead><tr><th>Table</th><th>Key columns</th><th>Notes</th></tr></thead><tbody><tr><td><code>settings</code></td><td>key, value, group</td><td>Key/value system config, Redis-cached.</td></tr><tr><td><code>password_reset_codes</code></td><td>resettable (morph), code, token, is_verified, expires_at</td><td>Multi-guard password-reset codes.</td></tr></tbody></table></div>\n<aside class=\"callout info\"><div class=\"callout-icon\">ℹ</div><div><strong>Info</strong><p>The relationships above form the spine: <code>Camera → Incident → Crime → Officer</code>, with <code>Scene</code>/<code>BodyCamUpload</code> hanging off the crime, <code>CitizenTip</code>/<code>PatternAlert</code> feeding the incident queue, and <code>LedgerEntry</code>/<code>ActivityLog</code> recording every consequential step.</p></div></aside>\n<aside class=\"callout warning\"><div class=\"callout-icon\">⚠</div><div><strong>Important note</strong><p>The current <code>incidents</code> table does not persist the AI report's <code>start_time</code> and <code>end_time</code>. Also, persisted priority factors use the key <code>weapon</code>, while one pattern-detection path currently reads <code>weapon_detected</code>.</p></div></aside>\n<p>Continue to the <a href=\"#/docs/api\">API Reference</a>.</p>", "headings": [{"level": 1, "title": "Data Model", "id": "data-model"}, {"level": 2, "title": "Spatial & Enum Conventions", "id": "conventions"}, {"level": 2, "title": "Incident & Dispatch", "id": "incident"}, {"level": 2, "title": "Cameras & AI Detection", "id": "cameras"}, {"level": 2, "title": "Officers & Field Ops", "id": "officers"}, {"level": 2, "title": "Stations, Users & Admins", "id": "stations"}, {"level": 2, "title": "Citizen Engagement", "id": "citizen"}, {"level": 2, "title": "Evidence & Media", "id": "evidence"}, {"level": 2, "title": "Communications", "id": "comms"}, {"level": 2, "title": "Audit & Ledger", "id": "audit"}, {"level": 2, "title": "Configuration", "id": "config"}]}, {"slug": "dispatch", "title": "Incident & Dispatch", "section": "Features", "description": "Incident & Dispatch The incident/dispatch layer is the governance core of CrimeLens — the deliberate boundary that turns an AI detection into a reviewable unit of work before any officer is …", "raw": "# Incident & Dispatch\n\n---\n\nThe incident/dispatch layer is the governance core of CrimeLens — the deliberate boundary that turns an AI detection into a *reviewable* unit of work before any officer is committed. This page documents the incident model, the dispatcher console, the queue ownership model, and the concurrency machinery that keeps multiple dispatchers consistent.\n\n- [The Incident Entity](#incident-entity)\n- [Incident Sources & Statuses](#sources-statuses)\n- [The Dispatcher Console](#console)\n- [Queue Ownership: Mine vs Shared](#ownership)\n- [Claim & Release](#claim-release)\n- [Supervisory vs Operator Identity](#identity)\n- [Presence & Load Balancing](#presence)\n- [Auto-Release of Stale Claims](#stale)\n- [Dispatch & Reject](#dispatch-reject)\n- [Real-Time Consistency](#realtime)\n- [Endpoints](#endpoints)\n\n<a name=\"incident-entity\"></a>\n## The Incident Entity\n\nAn **`Incident`** is a candidate event awaiting a dispatch decision. It is deliberately *not* a crime — a crime is only created when a dispatcher (or a narrow auto-dispatch policy) commits to a response.\n\nKey attributes:\n\n| Field | Purpose |\n|-------|---------|\n| `source` | Where it came from — `ai_alert`, `ai_crime`, `citizen`, `manual`. |\n| `police_station_id` | Owning station (tenancy boundary). |\n| `camera_id`, `ai_model_id` | Originating device / detector (nullable for manual & citizen). |\n| `status` | Lifecycle state (see below). |\n| `priority_score` | `[0,1]` score from the [Priority Engine](/{{route}}/{{version}}/priority-engine). |\n| `priority_tier` | `critical` / `high` / `medium` / `low`. |\n| `priority_factors` | JSON breakdown of *why* the score is what it is (auditable). |\n| `confidence_score` | The AI's confidence, when applicable. |\n| `lat` / `lang` | Geolocation. |\n| `assigned_dispatcher_id` | The `station_user` currently handling it (`null` = shared). |\n| `assigned_at` | When it was claimed. |\n| `crime_id` | The crime created on dispatch (set after review). |\n| `reviewed_at`, `dispatched_at`, `rejected_at` | Audit timestamps. |\n\n<a name=\"sources-statuses\"></a>\n## Incident Sources & Statuses\n\nThe status machine is intentionally small:\n\n```\n                ┌──────────────────┐\n   created ────▶│  pending_review  │\n                └────────┬─────────┘\n                  dispatch│  │reject\n                          ▼  ▼\n            ┌──────────────┐ ┌────────────────────────┐\n            │  dispatched  │ │  rejected_false_alarm  │\n            └──────────────┘ └────────────────────────┘\n                          (or) expired — aged out without review\n```\n\n`dispatched` incidents carry a `crime_id`; `rejected_false_alarm` ones carry a reason and feed back into model-quality metrics. The status field is a PHP backed enum (`IncidentStatus`), cast on the model.\n\n<a name=\"console\"></a>\n## The Dispatcher Console\n\nThe console (`Modules/Police/resources/js/Pages/Station/Dispatcher.tsx`, served by `DispatcherController`) is a **tri-pane operations screen**:\n\n```\n┌──────────────┬───────────────────────────┬────────────────────┐\n│ Pending Queue│         Live Map          │   Side Panels      │\n│  • My tasks  │   incidents · officers ·  │  • Chat preview    │\n│  • Shared    │   cameras (MapLibre)      │  • Citizen tips    │\n│              ├───────────────────────────┤  • Pattern alerts  │\n│  (claim/     │   Selected Incident Panel │  • Active crimes   │\n│   release)   │   (evidence · actions)    │  • On-shift officers│\n└──────────────┴───────────────────────────┴────────────────────┘\n```\n\nThe controller hydrates the page with the station's queue, on-shift officers (with live Redis GPS), cameras, active crimes, citizen tips, and the active-dispatcher count. Heavier panels (`officers`, `cameras`, `activeCrimes`) are streamed via Inertia **deferred props** with skeletons.\n\n<a name=\"ownership\"></a>\n## Queue Ownership: Mine vs Shared\n\nThe pending queue is split into two lists, computed server-side by `DispatcherSnapshotService::pending()` with a single PostgreSQL query:\n\n- **Mine** — incidents where `assigned_dispatcher_id = <current dispatcher>`.\n- **Shared** — unassigned incidents (`assigned_dispatcher_id IS NULL`), capped and ordered by `priority_score DESC, created_at ASC`.\n\nThis split lets several dispatchers work the same station without colliding: each sees their own committed work separately from the open pool.\n\n<a name=\"claim-release\"></a>\n## Claim & Release\n\nTaking ownership is an atomic, race-safe operation in `DispatcherAssignmentService`:\n\n```php\n// claim — succeeds for exactly one dispatcher\nUPDATE incidents\n   SET assigned_dispatcher_id = :dispatcher, assigned_at = now()\n WHERE id = :incident\n   AND police_station_id = :station\n   AND status = 'pending_review'\n   AND assigned_dispatcher_id IS NULL;   // ← the guard that prevents double-claim\n```\n\nIf the conditional `UPDATE` affects zero rows, the incident was already taken and the API returns **409 Conflict**. **Release** is the inverse, guarded by `assigned_dispatcher_id = :dispatcher` so only the owner can return an incident to the shared pool. Both actions broadcast an `IncidentAssigned` event so every console updates instantly.\n\n<a name=\"identity\"></a>\n## Supervisory vs Operator Identity\n\nA station has two console identities, and they are treated differently on purpose:\n\n- **`station_user_web`** (an individual person) **has** a dispatcher identity. They can claim, release, dispatch, and reject under their own name. Their \"Mine\" lane reflects their personal workload.\n- **`police_station_web`** (the institutional account) is **supervisory**. It has *no* dispatcher identity (`currentDispatcherId` is `null`), so it never shows a false \"Mine\" lane and the claim/release controls are hidden. It is a read-and-oversee view.\n\nThis prevents the institutional account from impersonating an arbitrary \"default\" user and corrupting ownership — claiming is strictly a per-person action (`IncidentClaimController` rejects a claim from the institutional account with **403**).\n\n<a name=\"presence\"></a>\n## Presence & Load Balancing\n\nActive dispatchers are tracked in a **Redis sorted set** keyed by station, scored by timestamp. Each console pings a presence endpoint; entries older than the TTL are trimmed, giving a live \"who is online\" count and feeding the **rebalance** routine.\n\n`DispatcherAssignmentService::rebalance()` distributes unassigned incidents round-robin across currently-active dispatchers, respecting each dispatcher's `dispatcher_focus_count` cap, and unassigns work from dispatchers who have gone offline. A rebalance broadcasts so consoles re-pull their queues.\n\n<a name=\"stale\"></a>\n## Auto-Release of Stale Claims\n\nA claimed incident must never be silently stuck behind an absent dispatcher. The scheduled **`ReleaseStaleDispatcherAssignmentsJob`** (every minute) returns incidents whose claim has gone stale back to the shared pool, so the queue is self-healing.\n\n<a name=\"dispatch-reject\"></a>\n## Dispatch & Reject\n\nFrom the selected-incident panel a dispatcher commits the decision:\n\n- **Dispatch** — creates a `Crime` linked to the incident, runs nearest-officer assignment (see [Crime Lifecycle](/{{route}}/{{version}}/crime-lifecycle)), marks the incident `dispatched`, and notifies the officer (FCM + Echo).\n- **Reject** — marks the incident `rejected_false_alarm` with a reason code/note; the rejection is recorded in the decision ledger and contributes to AI-quality analytics.\n\nBoth transitions are guarded against races so a single incident can never produce two crimes.\n\n<a name=\"realtime\"></a>\n## Real-Time Consistency\n\nEvery consequential action broadcasts on the private `station.{id}` channel and is consumed by the console:\n\n| Event | Effect on the console |\n|-------|----------------------|\n| `IncidentCreated` | New incident drops into the shared queue (+ siren for critical/high). |\n| `IncidentAssigned` (`claimed`/`released`/`rebalanced`) | Incident moves between Mine/Shared lanes. |\n| `IncidentReviewed` (`dispatched`/`rejected`) | Incident leaves the queue. |\n\nBroadcasts use `toOthers()` so the acting dispatcher — who already updated their own UI optimistically and saw a personal success toast — does not receive a redundant third-person echo, while every *other* operator does. This is what makes claim/release/dispatch feel instant and consistent across screens.\n\n<a name=\"endpoints\"></a>\n## Endpoints\n\n| Method | Path | Purpose |\n|--------|------|---------|\n| `GET` | `/station` | Dispatcher console (Inertia page). |\n| `POST` | `/station/api/incidents/{incident}/claim` | Claim an incident (per-person). |\n| `POST` | `/station/api/incidents/{incident}/release` | Release a claimed incident. |\n| `POST` | `/station/api/incidents/{incident}/dispatch` | Dispatch → create crime + assign. |\n| `POST` | `/station/api/incidents/{incident}/reject` | Reject as false alarm. |\n| `POST` | `/station/api/incidents` | Create a manual incident. |\n| `POST` | `/station/api/dispatcher-presence/ping` | Heartbeat presence. |\n\nAll are behind the station web guards (`police_station_web` / `station_user_web`). Continue to the [Priority Engine](/{{route}}/{{version}}/priority-engine).\n", "html": "<h1 id=\"incident-dispatch\"><a class=\"anchor\" href=\"#incident-dispatch\">#</a>Incident &amp; Dispatch</h1>\n<hr>\n<p>The incident/dispatch layer is the governance core of CrimeLens — the deliberate boundary that turns an AI detection into a <em>reviewable</em> unit of work before any officer is committed. This page documents the incident model, the dispatcher console, the queue ownership model, and the concurrency machinery that keeps multiple dispatchers consistent.</p>\n<ul>\n<li><a href=\"#incident-entity\">The Incident Entity</a></li>\n<li><a href=\"#sources-statuses\">Incident Sources &amp;amp; Statuses</a></li>\n<li><a href=\"#console\">The Dispatcher Console</a></li>\n<li><a href=\"#ownership\">Queue Ownership: Mine vs Shared</a></li>\n<li><a href=\"#claim-release\">Claim &amp;amp; Release</a></li>\n<li><a href=\"#identity\">Supervisory vs Operator Identity</a></li>\n<li><a href=\"#presence\">Presence &amp;amp; Load Balancing</a></li>\n<li><a href=\"#stale\">Auto-Release of Stale Claims</a></li>\n<li><a href=\"#dispatch-reject\">Dispatch &amp;amp; Reject</a></li>\n<li><a href=\"#realtime\">Real-Time Consistency</a></li>\n<li><a href=\"#endpoints\">Endpoints</a></li>\n</ul>\n<h2 id=\"incident-entity\"><a class=\"anchor\" href=\"#incident-entity\">#</a>The Incident Entity</h2>\n<p>An <strong><code>Incident</code></strong> is a candidate event awaiting a dispatch decision. It is deliberately <em>not</em> a crime — a crime is only created when a dispatcher (or a narrow auto-dispatch policy) commits to a response.</p>\n<p>Key attributes:</p>\n<div class=\"table-shell\"><table><thead><tr><th>Field</th><th>Purpose</th></tr></thead><tbody><tr><td><code>source</code></td><td>Where it came from — <code>ai_alert</code>, <code>ai_crime</code>, <code>citizen</code>, <code>manual</code>.</td></tr><tr><td><code>police_station_id</code></td><td>Owning station (tenancy boundary).</td></tr><tr><td><code>camera_id</code>, <code>ai_model_id</code></td><td>Originating device / detector (nullable for manual &amp; citizen).</td></tr><tr><td><code>status</code></td><td>Lifecycle state (see below).</td></tr><tr><td><code>priority_score</code></td><td><code>[0,1]</code> score from the <a href=\"#/docs/priority-engine\">Priority Engine</a>.</td></tr><tr><td><code>priority_tier</code></td><td><code>critical</code> / <code>high</code> / <code>medium</code> / <code>low</code>.</td></tr><tr><td><code>priority_factors</code></td><td>JSON breakdown of <em>why</em> the score is what it is (auditable).</td></tr><tr><td><code>confidence_score</code></td><td>The AI's confidence, when applicable.</td></tr><tr><td><code>lat</code> / <code>lang</code></td><td>Geolocation.</td></tr><tr><td><code>assigned_dispatcher_id</code></td><td>The <code>station_user</code> currently handling it (<code>null</code> = shared).</td></tr><tr><td><code>assigned_at</code></td><td>When it was claimed.</td></tr><tr><td><code>crime_id</code></td><td>The crime created on dispatch (set after review).</td></tr><tr><td><code>reviewed_at</code>, <code>dispatched_at</code>, <code>rejected_at</code></td><td>Audit timestamps.</td></tr></tbody></table></div>\n<h2 id=\"sources-statuses\"><a class=\"anchor\" href=\"#sources-statuses\">#</a>Incident Sources &amp; Statuses</h2>\n<p>The status machine is intentionally small:</p>\n<div class=\"code-card\"><div class=\"code-toolbar\"><span>text</span><button class=\"copy-btn\" type=\"button\">Copy</button></div><pre><code class=\"language-text\">                ┌──────────────────┐\n   created ────▶│  pending_review  │\n                └────────┬─────────┘\n                  dispatch│  │reject\n                          ▼  ▼\n            ┌──────────────┐ ┌────────────────────────┐\n            │  dispatched  │ │  rejected_false_alarm  │\n            └──────────────┘ └────────────────────────┘\n                          (or) expired — aged out without review</code></pre></div>\n<p><code>dispatched</code> incidents carry a <code>crime_id</code>; <code>rejected_false_alarm</code> ones carry a reason and feed back into model-quality metrics. The status field is a PHP backed enum (<code>IncidentStatus</code>), cast on the model.</p>\n<h2 id=\"console\"><a class=\"anchor\" href=\"#console\">#</a>The Dispatcher Console</h2>\n<p>The console (<code>Modules/Police/resources/js/Pages/Station/Dispatcher.tsx</code>, served by <code>DispatcherController</code>) is a <strong>tri-pane operations screen</strong>:</p>\n<div class=\"code-card\"><div class=\"code-toolbar\"><span>text</span><button class=\"copy-btn\" type=\"button\">Copy</button></div><pre><code class=\"language-text\">┌──────────────┬───────────────────────────┬────────────────────┐\n│ Pending Queue│         Live Map          │   Side Panels      │\n│  • My tasks  │   incidents · officers ·  │  • Chat preview    │\n│  • Shared    │   cameras (MapLibre)      │  • Citizen tips    │\n│              ├───────────────────────────┤  • Pattern alerts  │\n│  (claim/     │   Selected Incident Panel │  • Active crimes   │\n│   release)   │   (evidence · actions)    │  • On-shift officers│\n└──────────────┴───────────────────────────┴────────────────────┘</code></pre></div>\n<p>The controller hydrates the page with the station's queue, on-shift officers (with live Redis GPS), cameras, active crimes, citizen tips, and the active-dispatcher count. Heavier panels (<code>officers</code>, <code>cameras</code>, <code>activeCrimes</code>) are streamed via Inertia <strong>deferred props</strong> with skeletons.</p>\n<h2 id=\"ownership\"><a class=\"anchor\" href=\"#ownership\">#</a>Queue Ownership: Mine vs Shared</h2>\n<p>The pending queue is split into two lists, computed server-side by <code>DispatcherSnapshotService::pending()</code> with a single PostgreSQL query:</p>\n<ul>\n<li><strong>Mine</strong> — incidents where <code>assigned_dispatcher_id = &amp;lt;current dispatcher&amp;gt;</code>.</li>\n<li><strong>Shared</strong> — unassigned incidents (<code>assigned_dispatcher_id IS NULL</code>), capped and ordered by <code>priority_score DESC, created_at ASC</code>.</li>\n</ul>\n<p>This split lets several dispatchers work the same station without colliding: each sees their own committed work separately from the open pool.</p>\n<h2 id=\"claim-release\"><a class=\"anchor\" href=\"#claim-release\">#</a>Claim &amp; Release</h2>\n<p>Taking ownership is an atomic, race-safe operation in <code>DispatcherAssignmentService</code>:</p>\n<div class=\"code-card\"><div class=\"code-toolbar\"><span>php</span><button class=\"copy-btn\" type=\"button\">Copy</button></div><pre><code class=\"language-php\">// claim — succeeds for exactly one dispatcher\nUPDATE incidents\n   SET assigned_dispatcher_id = :dispatcher, assigned_at = now()\n WHERE id = :incident\n   AND police_station_id = :station\n   AND status = &#x27;pending_review&#x27;\n   AND assigned_dispatcher_id IS NULL;   // ← the guard that prevents double-claim</code></pre></div>\n<p>If the conditional <code>UPDATE</code> affects zero rows, the incident was already taken and the API returns <strong>409 Conflict</strong>. <strong>Release</strong> is the inverse, guarded by <code>assigned_dispatcher_id = :dispatcher</code> so only the owner can return an incident to the shared pool. Both actions broadcast an <code>IncidentAssigned</code> event so every console updates instantly.</p>\n<h2 id=\"identity\"><a class=\"anchor\" href=\"#identity\">#</a>Supervisory vs Operator Identity</h2>\n<p>A station has two console identities, and they are treated differently on purpose:</p>\n<ul>\n<li><strong><code>station_user_web</code></strong> (an individual person) <strong>has</strong> a dispatcher identity. They can claim, release, dispatch, and reject under their own name. Their \"Mine\" lane reflects their personal workload.</li>\n<li><strong><code>police_station_web</code></strong> (the institutional account) is <strong>supervisory</strong>. It has <em>no</em> dispatcher identity (<code>currentDispatcherId</code> is <code>null</code>), so it never shows a false \"Mine\" lane and the claim/release controls are hidden. It is a read-and-oversee view.</li>\n</ul>\n<p>This prevents the institutional account from impersonating an arbitrary \"default\" user and corrupting ownership — claiming is strictly a per-person action (<code>IncidentClaimController</code> rejects a claim from the institutional account with <strong>403</strong>).</p>\n<h2 id=\"presence\"><a class=\"anchor\" href=\"#presence\">#</a>Presence &amp; Load Balancing</h2>\n<p>Active dispatchers are tracked in a <strong>Redis sorted set</strong> keyed by station, scored by timestamp. Each console pings a presence endpoint; entries older than the TTL are trimmed, giving a live \"who is online\" count and feeding the <strong>rebalance</strong> routine.</p>\n<p><code>DispatcherAssignmentService::rebalance()</code> distributes unassigned incidents round-robin across currently-active dispatchers, respecting each dispatcher's <code>dispatcher_focus_count</code> cap, and unassigns work from dispatchers who have gone offline. A rebalance broadcasts so consoles re-pull their queues.</p>\n<h2 id=\"stale\"><a class=\"anchor\" href=\"#stale\">#</a>Auto-Release of Stale Claims</h2>\n<p>A claimed incident must never be silently stuck behind an absent dispatcher. The scheduled <strong><code>ReleaseStaleDispatcherAssignmentsJob</code></strong> (every minute) returns incidents whose claim has gone stale back to the shared pool, so the queue is self-healing.</p>\n<h2 id=\"dispatch-reject\"><a class=\"anchor\" href=\"#dispatch-reject\">#</a>Dispatch &amp; Reject</h2>\n<p>From the selected-incident panel a dispatcher commits the decision:</p>\n<ul>\n<li><strong>Dispatch</strong> — creates a <code>Crime</code> linked to the incident, runs nearest-officer assignment (see <a href=\"#/docs/crime-lifecycle\">Crime Lifecycle</a>), marks the incident <code>dispatched</code>, and notifies the officer (FCM + Echo).</li>\n<li><strong>Reject</strong> — marks the incident <code>rejected_false_alarm</code> with a reason code/note; the rejection is recorded in the decision ledger and contributes to AI-quality analytics.</li>\n</ul>\n<p>Both transitions are guarded against races so a single incident can never produce two crimes.</p>\n<h2 id=\"realtime\"><a class=\"anchor\" href=\"#realtime\">#</a>Real-Time Consistency</h2>\n<p>Every consequential action broadcasts on the private <code>station.{id}</code> channel and is consumed by the console:</p>\n<div class=\"table-shell\"><table><thead><tr><th>Event</th><th>Effect on the console</th></tr></thead><tbody><tr><td><code>IncidentCreated</code></td><td>New incident drops into the shared queue (+ siren for critical/high).</td></tr><tr><td><code>IncidentAssigned</code> (<code>claimed</code>/<code>released</code>/<code>rebalanced</code>)</td><td>Incident moves between Mine/Shared lanes.</td></tr><tr><td><code>IncidentReviewed</code> (<code>dispatched</code>/<code>rejected</code>)</td><td>Incident leaves the queue.</td></tr></tbody></table></div>\n<p>Broadcasts use <code>toOthers()</code> so the acting dispatcher — who already updated their own UI optimistically and saw a personal success toast — does not receive a redundant third-person echo, while every <em>other</em> operator does. This is what makes claim/release/dispatch feel instant and consistent across screens.</p>\n<h2 id=\"endpoints\"><a class=\"anchor\" href=\"#endpoints\">#</a>Endpoints</h2>\n<div class=\"table-shell\"><table><thead><tr><th>Method</th><th>Path</th><th>Purpose</th></tr></thead><tbody><tr><td><code>GET</code></td><td><code>/station</code></td><td>Dispatcher console (Inertia page).</td></tr><tr><td><code>POST</code></td><td><code>/station/api/incidents/{incident}/claim</code></td><td>Claim an incident (per-person).</td></tr><tr><td><code>POST</code></td><td><code>/station/api/incidents/{incident}/release</code></td><td>Release a claimed incident.</td></tr><tr><td><code>POST</code></td><td><code>/station/api/incidents/{incident}/dispatch</code></td><td>Dispatch → create crime + assign.</td></tr><tr><td><code>POST</code></td><td><code>/station/api/incidents/{incident}/reject</code></td><td>Reject as false alarm.</td></tr><tr><td><code>POST</code></td><td><code>/station/api/incidents</code></td><td>Create a manual incident.</td></tr><tr><td><code>POST</code></td><td><code>/station/api/dispatcher-presence/ping</code></td><td>Heartbeat presence.</td></tr></tbody></table></div>\n<p>All are behind the station web guards (<code>police_station_web</code> / <code>station_user_web</code>). Continue to the <a href=\"#/docs/priority-engine\">Priority Engine</a>.</p>", "headings": [{"level": 1, "title": "Incident & Dispatch", "id": "incident-dispatch"}, {"level": 2, "title": "The Incident Entity", "id": "incident-entity"}, {"level": 2, "title": "Incident Sources & Statuses", "id": "sources-statuses"}, {"level": 2, "title": "The Dispatcher Console", "id": "console"}, {"level": 2, "title": "Queue Ownership: Mine vs Shared", "id": "ownership"}, {"level": 2, "title": "Claim & Release", "id": "claim-release"}, {"level": 2, "title": "Supervisory vs Operator Identity", "id": "identity"}, {"level": 2, "title": "Presence & Load Balancing", "id": "presence"}, {"level": 2, "title": "Auto-Release of Stale Claims", "id": "stale"}, {"level": 2, "title": "Dispatch & Reject", "id": "dispatch-reject"}, {"level": 2, "title": "Real-Time Consistency", "id": "realtime"}, {"level": 2, "title": "Endpoints", "id": "endpoints"}]}, {"slug": "priority-engine", "title": "Priority Engine", "section": "Features", "description": "Priority Engine The Priority Engine is what makes CrimeLens's dispatch decisions explainable . Instead of a black box \"AI says urgent\", every incident gets a transparent, weighted score that…", "raw": "# Priority Engine\n\n---\n\nThe Priority Engine is what makes CrimeLens's dispatch decisions **explainable**. Instead of a black-box \"AI says urgent\", every incident gets a transparent, weighted score that a dispatcher — and later an auditor — can decompose into its contributing factors.\n\n- [Why Explainable Scoring](#why)\n- [The Formula](#formula)\n- [The Factors](#factors)\n- [Tiers](#tiers)\n- [Auditability](#audit)\n- [Auto-Dispatch Policy](#auto-dispatch)\n- [Tunability & Safety](#tuning)\n\n<a name=\"why\"></a>\n## Why Explainable Scoring\n\nSending armed officers is a consequential act. A score of \"0.91\" is meaningless on its own; a score that breaks down into *\"high model confidence, a weapon was detected, it's a repeat location, and it's the middle of the night\"* is something a dispatcher can trust and a review board can defend. Explainability is a first-class requirement, not a nice-to-have.\n\n<a name=\"formula\"></a>\n## The Formula\n\n`IncidentPriorityService` computes a score in `[0, 1]` as a weighted sum of six normalised factors:\n\n```\nscore = clamp(0, 1,\n      0.35 × confidence\n    + 0.25 × crime_type_weight\n    + 0.15 × weapon\n    + 0.10 × repeat_in_area\n    + 0.10 × time_of_day\n    + 0.05 × crowd_density )\n```\n\n| Weight | Factor |\n|:------:|--------|\n| **0.35** | `confidence` |\n| **0.25** | `crime_type_weight` |\n| **0.15** | `weapon` |\n| **0.10** | `repeat_in_area` |\n| **0.10** | `time_of_day` |\n| **0.05** | `crowd_density` |\n\n<a name=\"factors\"></a>\n## The Factors\n\nEach factor is normalised to `[0, 1]` before weighting:\n\n- **`confidence`** — the AI's own confidence in the detection (the strongest single signal).\n- **`crime_type_weight`** — the context weight supplied to the scorer. The current model intake uses a default base of `0.50` for alerts and `0.75` for confirmed reports; it does not currently resolve a `crime_type` from the request.\n- **`weapon`** — whether a weapon was detected; a strong escalator.\n- **`repeat_in_area`** — recent crimes within a small radius and time window (e.g. ~500 m / 24 h) — captures developing hot-spots.\n- **`time_of_day`** — night-time weighting, when risk is higher and witnesses fewer.\n- **`crowd_density`** — how many people are in frame; a minor modifier.\n\nAll six are persisted on the incident as `priority_factors` (JSON) alongside the final `priority_score`.\n\n<a name=\"tiers\"></a>\n## Tiers\n\nThe continuous score is bucketed into an operational tier that drives queue ordering and UI colour:\n\n| Tier | Score band | Meaning |\n|------|-----------|---------|\n| **Critical** | ≥ 0.85 | Top of the queue; auto-dispatch eligible (gated). |\n| **High** | 0.65 – 0.85 | Handle next. |\n| **Medium** | 0.45 – 0.65 | Standard review. |\n| **Low** | < 0.45 | Background / informational. |\n\nThe dispatcher queue orders by `priority_score DESC, created_at ASC`, so the most urgent, oldest work always surfaces first.\n\n<a name=\"audit\"></a>\n## Auditability\n\nBecause `priority_factors` is stored as structured JSON, any incident's score can be reconstructed and explained months later — *which* factors drove it and by how much. Combined with the [decision ledger](/{{route}}/{{version}}/security), this gives CrimeLens a defensible answer to the question every real dispatch system must answer: **\"why was this prioritised the way it was?\"**\n\n<a name=\"auto-dispatch\"></a>\n## Auto-Dispatch Policy\n\n`AutoDispatchPolicy` is intentionally conservative. Auto-dispatch — skipping the human reviewer — is allowed only when **all** of the strictest gates pass simultaneously (a critical-tier score, very high confidence, and a detected weapon). Everything else, by design, waits for a dispatcher. The policy never *lowers* scrutiny; it only fast-tracks the rare, unambiguous case where waiting is the bigger risk.\n\n<a name=\"tuning\"></a>\n## Tunability & Safety\n\nThe admin console exposes a **priority-weights** screen with dry-run and apply endpoints. It can replay proposed weights against recent incidents, persist them in settings, and record the change in the ledger.\n\n> {warning} `IncidentPriorityService` currently computes live incidents using its `WEIGHTS` constants rather than `SettingsService::priorityWeights()`. Applied admin values therefore affect the sandbox/stored policy but do **not yet change live incident scoring**. Wire the service to the settings provider before describing the weights as runtime-tunable.\n\nContinue to [Crime Lifecycle & Field Ops](/{{route}}/{{version}}/crime-lifecycle).\n", "html": "<h1 id=\"priority-engine\"><a class=\"anchor\" href=\"#priority-engine\">#</a>Priority Engine</h1>\n<hr>\n<p>The Priority Engine is what makes CrimeLens's dispatch decisions <strong>explainable</strong>. Instead of a black-box \"AI says urgent\", every incident gets a transparent, weighted score that a dispatcher — and later an auditor — can decompose into its contributing factors.</p>\n<ul>\n<li><a href=\"#why\">Why Explainable Scoring</a></li>\n<li><a href=\"#formula\">The Formula</a></li>\n<li><a href=\"#factors\">The Factors</a></li>\n<li><a href=\"#tiers\">Tiers</a></li>\n<li><a href=\"#audit\">Auditability</a></li>\n<li><a href=\"#auto-dispatch\">Auto-Dispatch Policy</a></li>\n<li><a href=\"#tuning\">Tunability &amp;amp; Safety</a></li>\n</ul>\n<h2 id=\"why\"><a class=\"anchor\" href=\"#why\">#</a>Why Explainable Scoring</h2>\n<p>Sending armed officers is a consequential act. A score of \"0.91\" is meaningless on its own; a score that breaks down into <em>\"high model confidence, a weapon was detected, it's a repeat location, and it's the middle of the night\"</em> is something a dispatcher can trust and a review board can defend. Explainability is a first-class requirement, not a nice-to-have.</p>\n<h2 id=\"formula\"><a class=\"anchor\" href=\"#formula\">#</a>The Formula</h2>\n<p><code>IncidentPriorityService</code> computes a score in <code>[0, 1]</code> as a weighted sum of six normalised factors:</p>\n<div class=\"code-card\"><div class=\"code-toolbar\"><span>text</span><button class=\"copy-btn\" type=\"button\">Copy</button></div><pre><code class=\"language-text\">score = clamp(0, 1,\n      0.35 × confidence\n    + 0.25 × crime_type_weight\n    + 0.15 × weapon\n    + 0.10 × repeat_in_area\n    + 0.10 × time_of_day\n    + 0.05 × crowd_density )</code></pre></div>\n<div class=\"table-shell\"><table><thead><tr><th>Weight</th><th>Factor</th></tr></thead><tbody><tr><td><strong>0.35</strong></td><td><code>confidence</code></td></tr><tr><td><strong>0.25</strong></td><td><code>crime_type_weight</code></td></tr><tr><td><strong>0.15</strong></td><td><code>weapon</code></td></tr><tr><td><strong>0.10</strong></td><td><code>repeat_in_area</code></td></tr><tr><td><strong>0.10</strong></td><td><code>time_of_day</code></td></tr><tr><td><strong>0.05</strong></td><td><code>crowd_density</code></td></tr></tbody></table></div>\n<h2 id=\"factors\"><a class=\"anchor\" href=\"#factors\">#</a>The Factors</h2>\n<p>Each factor is normalised to <code>[0, 1]</code> before weighting:</p>\n<ul>\n<li><strong><code>confidence</code></strong> — the AI's own confidence in the detection (the strongest single signal).</li>\n<li><strong><code>crime_type_weight</code></strong> — the context weight supplied to the scorer. The current model intake uses a default base of <code>0.50</code> for alerts and <code>0.75</code> for confirmed reports; it does not currently resolve a <code>crime_type</code> from the request.</li>\n<li><strong><code>weapon</code></strong> — whether a weapon was detected; a strong escalator.</li>\n<li><strong><code>repeat_in_area</code></strong> — recent crimes within a small radius and time window (e.g. ~500 m / 24 h) — captures developing hot-spots.</li>\n<li><strong><code>time_of_day</code></strong> — night-time weighting, when risk is higher and witnesses fewer.</li>\n<li><strong><code>crowd_density</code></strong> — how many people are in frame; a minor modifier.</li>\n</ul>\n<p>All six are persisted on the incident as <code>priority_factors</code> (JSON) alongside the final <code>priority_score</code>.</p>\n<h2 id=\"tiers\"><a class=\"anchor\" href=\"#tiers\">#</a>Tiers</h2>\n<p>The continuous score is bucketed into an operational tier that drives queue ordering and UI colour:</p>\n<div class=\"table-shell\"><table><thead><tr><th>Tier</th><th>Score band</th><th>Meaning</th></tr></thead><tbody><tr><td><strong>Critical</strong></td><td>≥ 0.85</td><td>Top of the queue; auto-dispatch eligible (gated).</td></tr><tr><td><strong>High</strong></td><td>0.65 – 0.85</td><td>Handle next.</td></tr><tr><td><strong>Medium</strong></td><td>0.45 – 0.65</td><td>Standard review.</td></tr><tr><td><strong>Low</strong></td><td>&lt; 0.45</td><td>Background / informational.</td></tr></tbody></table></div>\n<p>The dispatcher queue orders by <code>priority_score DESC, created_at ASC</code>, so the most urgent, oldest work always surfaces first.</p>\n<h2 id=\"audit\"><a class=\"anchor\" href=\"#audit\">#</a>Auditability</h2>\n<p>Because <code>priority_factors</code> is stored as structured JSON, any incident's score can be reconstructed and explained months later — <em>which</em> factors drove it and by how much. Combined with the <a href=\"#/docs/security\">decision ledger</a>, this gives CrimeLens a defensible answer to the question every real dispatch system must answer: <strong>\"why was this prioritised the way it was?\"</strong></p>\n<h2 id=\"auto-dispatch\"><a class=\"anchor\" href=\"#auto-dispatch\">#</a>Auto-Dispatch Policy</h2>\n<p><code>AutoDispatchPolicy</code> is intentionally conservative. Auto-dispatch — skipping the human reviewer — is allowed only when <strong>all</strong> of the strictest gates pass simultaneously (a critical-tier score, very high confidence, and a detected weapon). Everything else, by design, waits for a dispatcher. The policy never <em>lowers</em> scrutiny; it only fast-tracks the rare, unambiguous case where waiting is the bigger risk.</p>\n<h2 id=\"tuning\"><a class=\"anchor\" href=\"#tuning\">#</a>Tunability &amp; Safety</h2>\n<p>The admin console exposes a <strong>priority-weights</strong> screen with dry-run and apply endpoints. It can replay proposed weights against recent incidents, persist them in settings, and record the change in the ledger.</p>\n<aside class=\"callout warning\"><div class=\"callout-icon\">⚠</div><div><strong>Important note</strong><p><code>IncidentPriorityService</code> currently computes live incidents using its <code>WEIGHTS</code> constants rather than <code>SettingsService::priorityWeights()</code>. Applied admin values therefore affect the sandbox/stored policy but do <strong>not yet change live incident scoring</strong>. Wire the service to the settings provider before describing the weights as runtime-tunable.</p></div></aside>\n<p>Continue to <a href=\"#/docs/crime-lifecycle\">Crime Lifecycle &amp;amp; Field Ops</a>.</p>", "headings": [{"level": 1, "title": "Priority Engine", "id": "priority-engine"}, {"level": 2, "title": "Why Explainable Scoring", "id": "why"}, {"level": 2, "title": "The Formula", "id": "formula"}, {"level": 2, "title": "The Factors", "id": "factors"}, {"level": 2, "title": "Tiers", "id": "tiers"}, {"level": 2, "title": "Auditability", "id": "audit"}, {"level": 2, "title": "Auto-Dispatch Policy", "id": "auto-dispatch"}, {"level": 2, "title": "Tunability & Safety", "id": "tuning"}]}, {"slug": "citizen-tips", "title": "Citizen Tips", "section": "Features", "description": "Citizen Tips Citizen Tips is CrimeLens's public intake lane: a way for members of the public to report incidents by web form or SMS, with a dispatcher triage workflow that promotes a credibl…", "raw": "# Citizen Tips\n\n---\n\nCitizen Tips is CrimeLens's public-intake lane: a way for members of the public to report incidents by web form or SMS, with a dispatcher triage workflow that promotes a credible tip into a first-class incident. It mirrors the \"NG9-1-1-lite\" capability of commercial CAD systems.\n\n- [Why a Triage Lane](#why)\n- [The Tip Entity](#tip-entity)\n- [Intake Channels](#intake)\n- [Geolocation & Routing](#routing)\n- [The Triage Inbox](#inbox)\n- [Promote, Dismiss, Reply](#actions)\n- [Media Handling](#media)\n- [Retention & Privacy](#retention)\n- [Real-Time](#realtime)\n- [Endpoints](#endpoints)\n\n<a name=\"why\"></a>\n## Why a Triage Lane\n\nA citizen tip is **not** automatically an incident — most public reports are noise, duplicates, or non-emergencies. Tips therefore land in a separate triage queue where a dispatcher reviews them and decides whether to **promote** a tip into the main incident pipeline (where it inherits the same [priority scoring](/{{route}}/{{version}}/priority-engine) and dispatcher → crime → officer flow) or **dismiss** it. This keeps the operational queue clean while still capturing the public as a sensor.\n\n<a name=\"tip-entity\"></a>\n## The Tip Entity\n\nA **`CitizenTip`** captures one public report:\n\n| Field | Purpose |\n|-------|---------|\n| `station_id` | The station that owns/handles the tip. |\n| `source` | `web`, `sms`, `whatsapp`, or `phone_logged`. |\n| `sender_identifier` | The contact handle (phone/anon token). |\n| `sender_display_name` | Optional friendly name. |\n| `message` | The free-text report. |\n| `media_paths` | JSON list of uploaded photos/videos (`path`, `mime`, `size`, `original_name`). |\n| `lat` / `lng` / `reported_address` | Location (raw + reverse-geocoded). |\n| `status` | `pending` → `in_review` → `promoted` / `dismissed` / `expired`. |\n| `promoted_incident_id` | Set when promoted into an incident. |\n| `reviewed_by_station_user_id`, `reviewed_at` | Triage audit. |\n| `dismissal_reason` | Why a tip was dismissed. |\n| `expires_at` | Auto-purge horizon. |\n\n<a name=\"intake\"></a>\n## Intake Channels\n\nAll channels funnel through a single **`TipIntakeService::intake()`**, which normalises the sender, stores media, sets the retention window, and broadcasts a real-time notification.\n\n**Web portal** — a public, unauthenticated form per station:\n\n```\nGET  /tip/{stationCode}        → the public submission form\nPOST /tip/{stationCode}        → submit (description, contact, lat/lng, address, media[])\nGET  /tip/{tipId}/thanks       → confirmation with a TIP-XXXXXXXX reference\n```\n\nSubmissions are rate-limited per sender IP (configurable hourly cap) and require at least a description or one media file. Anonymous submitters get a random `anon_…` identifier.\n\n**SMS / WhatsApp (Twilio)** — an inbound webhook per station:\n\n```\nPOST /api/v1/webhooks/twilio/inbound/{stationCode}\n```\n\n`TwilioInboundController` resolves the station from the URL, downloads any MMS media, and calls the same intake service with `source = sms`.\n\n<a name=\"routing\"></a>\n## Geolocation & Routing\n\nThe receiving station is determined by the **station code in the URL** (the link/QR a citizen uses, or the Twilio number's mapping) — explicit routing rather than guesswork. A geospatial `findNearestStation()` (Haversine over station coordinates) exists for location-based routing where a station code is not available.\n\nAfter intake, **`GeocodeCitizenTipJob`** reverse-geocodes the coordinates into a human-readable `reported_address` off the request cycle.\n\n<a name=\"inbox\"></a>\n## The Triage Inbox\n\nDispatchers work tips in two places:\n\n1. **The console side-panel** — recent unresolved tips surface live on the Dispatcher Console; clicking one opens a detail dialog. The console seeds its list from the server (`pending` + `in_review`) so tips survive a page refresh, then keeps it live over WebSockets.\n2. **The dedicated \"Citizen Tips\" page** (`/station/citizen-tips`) — a full, filterable, paginated inbox with per-status tabs (All / Pending / In review / Promoted / Dismissed) and counts, for working through the backlog.\n\nBoth open the same review dialog, which loads full tip details (including signed media URLs) on demand.\n\n<a name=\"actions\"></a>\n## Promote, Dismiss, Reply\n\nFrom the review dialog a dispatcher can:\n\n- **Promote** — opens a form (severity, crime type, notes) and creates a real `Incident` (`source = citizen`) linked back via `promoted_incident_id`. The new incident is broadcast (`IncidentCreated`) so it appears on every console's queue in real time, and the promoter's own queue reloads immediately. Promotion requires a location on the tip.\n- **Dismiss** — records a reason and closes the tip; logged to the decision ledger.\n- **Reply** (SMS tips only) — sends an SMS back to the reporter via `SendTwilioReplyJob`, gated on chat permission.\n\nEvery triage action is permission-checked and station-scoped (a dispatcher can only act on their own station's tips — cross-station access returns **404**).\n\n<a name=\"media\"></a>\n## Media Handling\n\nUploaded photos and videos are stored privately and served through a **relative signed URL** (`signed:relative` middleware), so an `<img>`/`<video>` tag works regardless of the host the console is opened on (localhost vs a tunnelled public URL) — the signature covers only the path, not the domain. The review dialog renders `<video controls>` for video MIME types and `<img>` for images, with a click-to-zoom lightbox.\n\n<a name=\"retention\"></a>\n## Retention & Privacy\n\nTips carry an `expires_at` set from a configurable retention window. The scheduled **`PurgeExpiredCitizenTipsJob`** (daily) deletes expired tips and their media. Sender identifiers are masked in list views and only revealed to operators with the appropriate chat permission.\n\n<a name=\"realtime\"></a>\n## Real-Time\n\nOn intake, `TipIntakeService` broadcasts a **`CitizenTipReceived`** event on the `station.{id}` channel with a masked-sender preview, media count, and message snippet. The console adds it to the side-panel live (with an audible cue), and the promote action broadcasts `IncidentCreated` to surface the resulting incident everywhere at once.\n\n<a name=\"endpoints\"></a>\n## Endpoints\n\n**Public intake**\n\n| Method | Path | Purpose |\n|--------|------|---------|\n| `GET` | `/tip/{stationCode}` | Public form |\n| `POST` | `/tip/{stationCode}` | Submit a tip |\n| `GET` | `/tip/{tipId}/thanks` | Confirmation |\n| `POST` | `/api/v1/webhooks/twilio/inbound/{stationCode}` | Twilio inbound webhook |\n\n**Dispatcher triage** (station web guards)\n\n| Method | Path | Purpose |\n|--------|------|---------|\n| `GET` | `/station/citizen-tips` | Inbox page (filterable, paginated) |\n| `GET` | `/station/api/citizen-tips` | Tips JSON list |\n| `GET` | `/station/api/citizen-tips/{tip}` | Full tip detail (+ signed media URLs) |\n| `POST` | `/station/api/citizen-tips/{tip}/promote` | Promote → incident |\n| `POST` | `/station/api/citizen-tips/{tip}/dismiss` | Dismiss with reason |\n| `POST` | `/station/api/citizen-tips/{tip}/reply` | Reply via SMS |\n| `GET` | `/station/api/citizen-tips/{tip}/media/{index}` | Serve media (relative-signed) |\n\nContinue to [Cameras & Streaming](/{{route}}/{{version}}/cameras).\n", "html": "<h1 id=\"citizen-tips\"><a class=\"anchor\" href=\"#citizen-tips\">#</a>Citizen Tips</h1>\n<hr>\n<p>Citizen Tips is CrimeLens's public-intake lane: a way for members of the public to report incidents by web form or SMS, with a dispatcher triage workflow that promotes a credible tip into a first-class incident. It mirrors the \"NG9-1-1-lite\" capability of commercial CAD systems.</p>\n<ul>\n<li><a href=\"#why\">Why a Triage Lane</a></li>\n<li><a href=\"#tip-entity\">The Tip Entity</a></li>\n<li><a href=\"#intake\">Intake Channels</a></li>\n<li><a href=\"#routing\">Geolocation &amp;amp; Routing</a></li>\n<li><a href=\"#inbox\">The Triage Inbox</a></li>\n<li><a href=\"#actions\">Promote, Dismiss, Reply</a></li>\n<li><a href=\"#media\">Media Handling</a></li>\n<li><a href=\"#retention\">Retention &amp;amp; Privacy</a></li>\n<li><a href=\"#realtime\">Real-Time</a></li>\n<li><a href=\"#endpoints\">Endpoints</a></li>\n</ul>\n<h2 id=\"why\"><a class=\"anchor\" href=\"#why\">#</a>Why a Triage Lane</h2>\n<p>A citizen tip is <strong>not</strong> automatically an incident — most public reports are noise, duplicates, or non-emergencies. Tips therefore land in a separate triage queue where a dispatcher reviews them and decides whether to <strong>promote</strong> a tip into the main incident pipeline (where it inherits the same <a href=\"#/docs/priority-engine\">priority scoring</a> and dispatcher → crime → officer flow) or <strong>dismiss</strong> it. This keeps the operational queue clean while still capturing the public as a sensor.</p>\n<h2 id=\"tip-entity\"><a class=\"anchor\" href=\"#tip-entity\">#</a>The Tip Entity</h2>\n<p>A <strong><code>CitizenTip</code></strong> captures one public report:</p>\n<div class=\"table-shell\"><table><thead><tr><th>Field</th><th>Purpose</th></tr></thead><tbody><tr><td><code>station_id</code></td><td>The station that owns/handles the tip.</td></tr><tr><td><code>source</code></td><td><code>web</code>, <code>sms</code>, <code>whatsapp</code>, or <code>phone_logged</code>.</td></tr><tr><td><code>sender_identifier</code></td><td>The contact handle (phone/anon token).</td></tr><tr><td><code>sender_display_name</code></td><td>Optional friendly name.</td></tr><tr><td><code>message</code></td><td>The free-text report.</td></tr><tr><td><code>media_paths</code></td><td>JSON list of uploaded photos/videos (<code>path</code>, <code>mime</code>, <code>size</code>, <code>original_name</code>).</td></tr><tr><td><code>lat</code> / <code>lng</code> / <code>reported_address</code></td><td>Location (raw + reverse-geocoded).</td></tr><tr><td><code>status</code></td><td><code>pending</code> → <code>in_review</code> → <code>promoted</code> / <code>dismissed</code> / <code>expired</code>.</td></tr><tr><td><code>promoted_incident_id</code></td><td>Set when promoted into an incident.</td></tr><tr><td><code>reviewed_by_station_user_id</code>, <code>reviewed_at</code></td><td>Triage audit.</td></tr><tr><td><code>dismissal_reason</code></td><td>Why a tip was dismissed.</td></tr><tr><td><code>expires_at</code></td><td>Auto-purge horizon.</td></tr></tbody></table></div>\n<h2 id=\"intake\"><a class=\"anchor\" href=\"#intake\">#</a>Intake Channels</h2>\n<p>All channels funnel through a single <strong><code>TipIntakeService::intake()</code></strong>, which normalises the sender, stores media, sets the retention window, and broadcasts a real-time notification.</p>\n<p><strong>Web portal</strong> — a public, unauthenticated form per station:</p>\n<div class=\"code-card\"><div class=\"code-toolbar\"><span>text</span><button class=\"copy-btn\" type=\"button\">Copy</button></div><pre><code class=\"language-text\">GET  /tip/{stationCode}        → the public submission form\nPOST /tip/{stationCode}        → submit (description, contact, lat/lng, address, media[])\nGET  /tip/{tipId}/thanks       → confirmation with a TIP-XXXXXXXX reference</code></pre></div>\n<p>Submissions are rate-limited per sender IP (configurable hourly cap) and require at least a description or one media file. Anonymous submitters get a random <code>anon_…</code> identifier.</p>\n<p><strong>SMS / WhatsApp (Twilio)</strong> — an inbound webhook per station:</p>\n<div class=\"code-card\"><div class=\"code-toolbar\"><span>text</span><button class=\"copy-btn\" type=\"button\">Copy</button></div><pre><code class=\"language-text\">POST /api/v1/webhooks/twilio/inbound/{stationCode}</code></pre></div>\n<p><code>TwilioInboundController</code> resolves the station from the URL, downloads any MMS media, and calls the same intake service with <code>source = sms</code>.</p>\n<h2 id=\"routing\"><a class=\"anchor\" href=\"#routing\">#</a>Geolocation &amp; Routing</h2>\n<p>The receiving station is determined by the <strong>station code in the URL</strong> (the link/QR a citizen uses, or the Twilio number's mapping) — explicit routing rather than guesswork. A geospatial <code>findNearestStation()</code> (Haversine over station coordinates) exists for location-based routing where a station code is not available.</p>\n<p>After intake, <strong><code>GeocodeCitizenTipJob</code></strong> reverse-geocodes the coordinates into a human-readable <code>reported_address</code> off the request cycle.</p>\n<h2 id=\"inbox\"><a class=\"anchor\" href=\"#inbox\">#</a>The Triage Inbox</h2>\n<p>Dispatchers work tips in two places:</p>\n<ol>\n<li><strong>The console side-panel</strong> — recent unresolved tips surface live on the Dispatcher Console; clicking one opens a detail dialog. The console seeds its list from the server (<code>pending</code> + <code>in_review</code>) so tips survive a page refresh, then keeps it live over WebSockets.</li>\n<li><strong>The dedicated \"Citizen Tips\" page</strong> (<code>/station/citizen-tips</code>) — a full, filterable, paginated inbox with per-status tabs (All / Pending / In review / Promoted / Dismissed) and counts, for working through the backlog.</li>\n</ol>\n<p>Both open the same review dialog, which loads full tip details (including signed media URLs) on demand.</p>\n<h2 id=\"actions\"><a class=\"anchor\" href=\"#actions\">#</a>Promote, Dismiss, Reply</h2>\n<p>From the review dialog a dispatcher can:</p>\n<ul>\n<li><strong>Promote</strong> — opens a form (severity, crime type, notes) and creates a real <code>Incident</code> (<code>source = citizen</code>) linked back via <code>promoted_incident_id</code>. The new incident is broadcast (<code>IncidentCreated</code>) so it appears on every console's queue in real time, and the promoter's own queue reloads immediately. Promotion requires a location on the tip.</li>\n<li><strong>Dismiss</strong> — records a reason and closes the tip; logged to the decision ledger.</li>\n<li><strong>Reply</strong> (SMS tips only) — sends an SMS back to the reporter via <code>SendTwilioReplyJob</code>, gated on chat permission.</li>\n</ul>\n<p>Every triage action is permission-checked and station-scoped (a dispatcher can only act on their own station's tips — cross-station access returns <strong>404</strong>).</p>\n<h2 id=\"media\"><a class=\"anchor\" href=\"#media\">#</a>Media Handling</h2>\n<p>Uploaded photos and videos are stored privately and served through a <strong>relative signed URL</strong> (<code>signed:relative</code> middleware), so an <code>&amp;lt;img&amp;gt;</code>/<code>&amp;lt;video&amp;gt;</code> tag works regardless of the host the console is opened on (localhost vs a tunnelled public URL) — the signature covers only the path, not the domain. The review dialog renders <code>&amp;lt;video controls&amp;gt;</code> for video MIME types and <code>&amp;lt;img&amp;gt;</code> for images, with a click-to-zoom lightbox.</p>\n<h2 id=\"retention\"><a class=\"anchor\" href=\"#retention\">#</a>Retention &amp; Privacy</h2>\n<p>Tips carry an <code>expires_at</code> set from a configurable retention window. The scheduled <strong><code>PurgeExpiredCitizenTipsJob</code></strong> (daily) deletes expired tips and their media. Sender identifiers are masked in list views and only revealed to operators with the appropriate chat permission.</p>\n<h2 id=\"realtime\"><a class=\"anchor\" href=\"#realtime\">#</a>Real-Time</h2>\n<p>On intake, <code>TipIntakeService</code> broadcasts a <strong><code>CitizenTipReceived</code></strong> event on the <code>station.{id}</code> channel with a masked-sender preview, media count, and message snippet. The console adds it to the side-panel live (with an audible cue), and the promote action broadcasts <code>IncidentCreated</code> to surface the resulting incident everywhere at once.</p>\n<h2 id=\"endpoints\"><a class=\"anchor\" href=\"#endpoints\">#</a>Endpoints</h2>\n<p><strong>Public intake</strong></p>\n<div class=\"table-shell\"><table><thead><tr><th>Method</th><th>Path</th><th>Purpose</th></tr></thead><tbody><tr><td><code>GET</code></td><td><code>/tip/{stationCode}</code></td><td>Public form</td></tr><tr><td><code>POST</code></td><td><code>/tip/{stationCode}</code></td><td>Submit a tip</td></tr><tr><td><code>GET</code></td><td><code>/tip/{tipId}/thanks</code></td><td>Confirmation</td></tr><tr><td><code>POST</code></td><td><code>/api/v1/webhooks/twilio/inbound/{stationCode}</code></td><td>Twilio inbound webhook</td></tr></tbody></table></div>\n<p><strong>Dispatcher triage</strong> (station web guards)</p>\n<div class=\"table-shell\"><table><thead><tr><th>Method</th><th>Path</th><th>Purpose</th></tr></thead><tbody><tr><td><code>GET</code></td><td><code>/station/citizen-tips</code></td><td>Inbox page (filterable, paginated)</td></tr><tr><td><code>GET</code></td><td><code>/station/api/citizen-tips</code></td><td>Tips JSON list</td></tr><tr><td><code>GET</code></td><td><code>/station/api/citizen-tips/{tip}</code></td><td>Full tip detail (+ signed media URLs)</td></tr><tr><td><code>POST</code></td><td><code>/station/api/citizen-tips/{tip}/promote</code></td><td>Promote → incident</td></tr><tr><td><code>POST</code></td><td><code>/station/api/citizen-tips/{tip}/dismiss</code></td><td>Dismiss with reason</td></tr><tr><td><code>POST</code></td><td><code>/station/api/citizen-tips/{tip}/reply</code></td><td>Reply via SMS</td></tr><tr><td><code>GET</code></td><td><code>/station/api/citizen-tips/{tip}/media/{index}</code></td><td>Serve media (relative-signed)</td></tr></tbody></table></div>\n<p>Continue to <a href=\"#/docs/cameras\">Cameras &amp;amp; Streaming</a>.</p>", "headings": [{"level": 1, "title": "Citizen Tips", "id": "citizen-tips"}, {"level": 2, "title": "Why a Triage Lane", "id": "why"}, {"level": 2, "title": "The Tip Entity", "id": "tip-entity"}, {"level": 2, "title": "Intake Channels", "id": "intake"}, {"level": 2, "title": "Geolocation & Routing", "id": "routing"}, {"level": 2, "title": "The Triage Inbox", "id": "inbox"}, {"level": 2, "title": "Promote, Dismiss, Reply", "id": "actions"}, {"level": 2, "title": "Media Handling", "id": "media"}, {"level": 2, "title": "Retention & Privacy", "id": "retention"}, {"level": 2, "title": "Real-Time", "id": "realtime"}, {"level": 2, "title": "Endpoints", "id": "endpoints"}]}, {"slug": "cameras", "title": "Cameras & Streaming", "section": "Features", "description": "Cameras & Streaming CrimeLens treats a camera as both a sensor for the AI and an evidence source for the dispatcher and the courtroom . This page covers the camera model, device control, the…", "raw": "# Cameras & Streaming\n\n---\n\nCrimeLens treats a camera as both a **sensor** (for the AI) and an **evidence source** (for the dispatcher and the courtroom). This page covers the camera model, device control, the streaming gateway, and the recording/evidence pipeline.\n\n- [The Camera Model](#model)\n- [Coverage Cones](#coverage)\n- [Device Control (Tapo / ONVIF)](#control)\n- [The Streaming Gateway](#gateway)\n- [Stream Formats](#formats)\n- [Continuous Recording](#recording)\n- [Scene Extraction (Evidence)](#scene)\n- [Tamper Detection](#tamper)\n- [Detection Filters](#filters)\n- [Health & Cleanup](#health)\n\n<a name=\"model\"></a>\n## The Camera Model\n\nA `Camera` belongs to a police station and carries everything needed to reach, stream, and place it on a map:\n\n| Field | Purpose |\n|-------|---------|\n| `gateway_stream_key` | Unique key the streaming gateway uses to route this camera. |\n| `ip_address`, `rtsp_port` | Network address (RTSP default 554). |\n| `user_name`, `password`, `tapo_email`, `tapo_password` | **Encrypted-at-rest** credentials (Laravel `encrypted` casts). |\n| `stream_path` | JSON map of stream IDs (e.g. `stream1` HD, `stream2` SD). |\n| `storage_type` | `none` / `sd_card` / `cloud` — selects the recording strategy. |\n| `lat`, `lang`, `location` | Coordinates + a **PostGIS** `geography(Point, 4326)` for spatial queries. |\n| `coverage_radius_meters`, `coverage_angle_degrees`, `coverage_bearing_degrees` | The camera's field-of-view cone. |\n| `is_active`, `last_seen_at`, `offline_notified_at` | Health state. |\n\nCredentials are never returned raw — the model exposes **gateway URLs** instead (`gateway_rtsp_url`, `gateway_hls_url`, `gateway_webrtc_url`), and the HLS accessor returns a short-lived **signed route** (`/gateway/hls/{key}/index.m3u8`, ~5-minute TTL).\n\n<a name=\"coverage\"></a>\n## Coverage Cones\n\nEach camera stores a directional field-of-view (radius + angle + bearing). The dispatcher map renders these as **coverage cones**, so operators can see exactly what is and isn't observable — and where the blind spots are. The PostGIS `location` point (kept in sync with `lat`/`lang`) powers nearest-camera and heatmap queries.\n\n<a name=\"control\"></a>\n## Device Control (Tapo / ONVIF)\n\nThe backend — and only the backend — can command a camera. Control flows through the `TapoCameraController` (`/api/camera/*`), every route protected by gateway IP allow-listing + **HMAC** (`gateway.ip`, `gateway.hmac`) and rate-limited. The surface is broad (40+ actions), grouped as:\n\n| Group | Actions |\n|-------|---------|\n| **Privacy / safety** | `privacy`, `alarm` (siren + light), `whitelamp`, `cover`, `led` |\n| **PTZ & tracking** | `motor` (move/calibrate), `preset` (list/set/save/delete), `cruise`, `autotrack`, `smarttrack` |\n| **Detection toggles** | `motion`, `person`, `babycry`, `tamper` (sensitivity-tunable) |\n| **Image** | `daynight`, `flip`, `ldc`, `osd`, `light-freq`, `video-qualities` |\n| **Audio** | `microphone`, `speaker`, `audio-config`, `record-audio` |\n| **Recording / storage** | `recordings`, `record-plan`, `circular-recording`, `sdcard`, `media-encrypt` |\n| **Device** | `status`, `basic-info`, `time`, `timezone`, `firmware`, `reboot`, `diagnose`, `stream-url`, `events` |\n\nEach action takes an `action` discriminator (e.g. `on`/`off`/`status`) plus action-specific params. The controller forwards the command to the Python gateway, which talks to the camera over Tapo's API.\n\n<a name=\"gateway\"></a>\n## The Streaming Gateway\n\nA camera emits one RTSP feed, but three consumers need three formats. The **Python Camera Gateway** (`Modules/Gateway/tapo_server.py`, a persistent Flask service) solves the fan-out. Laravel drives it through `CameraGatewayService`:\n\n| Operation | Gateway endpoint |\n|-----------|------------------|\n| Register camera | `POST /gateway/cameras/register` |\n| Start stream | `POST /gateway/cameras/{key}/stream/start` |\n| Stop stream | `POST /gateway/cameras/{key}/stream/stop` |\n| Stream status | `GET /gateway/cameras/{key}/stream/status` |\n| Execute command | `POST /gateway/cameras/{key}/command` |\n| Health | `GET /gateway/health` |\n\nLaravel-to-Python requests carry an `X-Gateway-Token` shared secret. The gateway keeps **persistent, pooled `pytapo` connections** (cached by `ip:user`, 5-minute TTL) so it doesn't re-handshake per request, and tracks one `ffmpeg` process per active stream. The token authenticates the service call; use localhost/private networking/VPN or HTTPS to protect credential-bearing traffic.\n\nCamera state is kept in sync with the gateway by jobs — `SyncCameraGatewayStreamJob` (register + start / stop), `SyncCameraRecordingJob`, and `SyncCameraPrivacyJob` (privacy follows `is_active`) — and the scheduled `camera:gateway:boot` command re-establishes all active streams after a restart.\n\n<a name=\"formats\"></a>\n## Stream Formats\n\n| Format | Produced by | Consumer | Why |\n|--------|-------------|----------|-----|\n| **HLS** | gateway ffmpeg | mobile + lightweight web | Robust over poor networks; served via a signed route. |\n| **RTSP relay** | gateway → MediaMTX | internal | Re-publishes the feed for WebRTC. |\n| **WebRTC** | MediaMTX | dispatcher monitoring grid | Sub-second latency for live ops. |\n| **RTSP relay** | gateway / MediaMTX | AI service + recording | Stable internal source for detection and recording. |\n\nThe gateway transcodes RTSP → HLS with tunable, low-latency `ffmpeg` settings (1-second segments, `zerolatency`, configurable codec/bitrate/GOP), all driven by environment variables.\n\n<a name=\"recording\"></a>\n## Continuous Recording\n\nFor cameras with `storage_type = none` (no on-device storage), `CameraRecordingService` runs a continuous server-side recording: one `ffmpeg` process per camera writing **60-second MKV segments**:\n\n```bash\nffmpeg -rtsp_transport tcp -use_wallclock_as_timestamps 1 \\\n  -i {rtsp_url} -map 0:v -c:v copy -map 0:a? -c:a copy \\\n  -f segment -segment_time 60 -reset_timestamps 1 -strftime 1 \\\n  storage/recordings/{camera_id}/%Y-%m-%d_%H-%M-%S.mkv\n```\n\nProcess PIDs are tracked in Redis (`camera_recording:{id}`) so the service can `start` / `stop` / `restart` / `isRecording` reliably. Recording starts automatically when such a camera is activated.\n\n<a name=\"scene\"></a>\n## Scene Extraction (Evidence)\n\nWhen `ExtractCrimeScene` is dispatched, `SceneExtractorService` on the `scenes` queue produces the evidence clip:\n\n- **SD-card / cloud cameras** — pull the clip directly from the camera for the incident window.\n- **Storage-less cameras** — find the recorded segments overlapping `[start − 30s, end + 30s]`, concat-merge them with `ffmpeg`, and transcode to a single faststart MP4.\n\nIt also extracts a **thumbnail** (first frame) for quick preview, and writes both to `crimes/{crime_id}/`. The result is persisted as a `Scene` record linked to the crime.\n\n> {warning} The legacy direct model-report flow dispatches this job. The current AI → Incident → Crime flow accepts a time window but does not persist it on the Incident, so automatic extraction is not yet guaranteed for every AI-originated Crime.\n\n<a name=\"tamper\"></a>\n## Tamper Detection\n\nCameras can be blinded — covered, sprayed, re-aimed, or cut off. The gateway analyses the feed and reports tamper signals to a webhook (`POST /v1/webhooks/camera-tamper`), creating a `CameraTamperEvent`:\n\n- **Signal types** — `blackout`, `variance_collapse`, `fov_shift`, `static_stream`, `heartbeat_gap`.\n- **Status** — `active` → `acknowledged` / `resolved` / `false_alarm`, with an admin acknowledgement note and an optional evidence frame (served via a 30-minute signed URL).\n\nA `CameraTamperDetected` event broadcasts on the private `admin.camera-health` channel so operators see blind-spots forming in real time.\n\n<a name=\"filters\"></a>\n## Detection Filters\n\n`CameraDetectionFilter` lets a camera be tuned per crime type: a `min_confidence` threshold (default `0.700`) and an enabled flag, keyed uniquely by `(camera, crime_type)`. When the AI reports a detection, the filter decides whether it is **accepted** or **filtered** (by type or confidence) — and that decision is recorded in `ai_detection_logs`, giving a measurable view of model precision per camera.\n\n<a name=\"health\"></a>\n## Health & Cleanup\n\n- **Health checks** — the `camera:health-check` schedule pings cameras; unreachable cameras flip to offline (`CameraOffline` event) and notify their station.\n- **Cleanup** — the `CleanupOldRecordings` job (maintenance queue) deletes recording segments past the retention window (default 7 days) and crime evidence past its window (default 90 days).\n\nContinue to [AI Model Integration](/{{route}}/{{version}}/ai-integration).\n", "html": "<h1 id=\"cameras-streaming\"><a class=\"anchor\" href=\"#cameras-streaming\">#</a>Cameras &amp; Streaming</h1>\n<hr>\n<p>CrimeLens treats a camera as both a <strong>sensor</strong> (for the AI) and an <strong>evidence source</strong> (for the dispatcher and the courtroom). This page covers the camera model, device control, the streaming gateway, and the recording/evidence pipeline.</p>\n<ul>\n<li><a href=\"#model\">The Camera Model</a></li>\n<li><a href=\"#coverage\">Coverage Cones</a></li>\n<li><a href=\"#control\">Device Control (Tapo / ONVIF)</a></li>\n<li><a href=\"#gateway\">The Streaming Gateway</a></li>\n<li><a href=\"#formats\">Stream Formats</a></li>\n<li><a href=\"#recording\">Continuous Recording</a></li>\n<li><a href=\"#scene\">Scene Extraction (Evidence)</a></li>\n<li><a href=\"#tamper\">Tamper Detection</a></li>\n<li><a href=\"#filters\">Detection Filters</a></li>\n<li><a href=\"#health\">Health &amp;amp; Cleanup</a></li>\n</ul>\n<h2 id=\"model\"><a class=\"anchor\" href=\"#model\">#</a>The Camera Model</h2>\n<p>A <code>Camera</code> belongs to a police station and carries everything needed to reach, stream, and place it on a map:</p>\n<div class=\"table-shell\"><table><thead><tr><th>Field</th><th>Purpose</th></tr></thead><tbody><tr><td><code>gateway_stream_key</code></td><td>Unique key the streaming gateway uses to route this camera.</td></tr><tr><td><code>ip_address</code>, <code>rtsp_port</code></td><td>Network address (RTSP default 554).</td></tr><tr><td><code>user_name</code>, <code>password</code>, <code>tapo_email</code>, <code>tapo_password</code></td><td><strong>Encrypted-at-rest</strong> credentials (Laravel <code>encrypted</code> casts).</td></tr><tr><td><code>stream_path</code></td><td>JSON map of stream IDs (e.g. <code>stream1</code> HD, <code>stream2</code> SD).</td></tr><tr><td><code>storage_type</code></td><td><code>none</code> / <code>sd_card</code> / <code>cloud</code> — selects the recording strategy.</td></tr><tr><td><code>lat</code>, <code>lang</code>, <code>location</code></td><td>Coordinates + a <strong>PostGIS</strong> <code>geography(Point, 4326)</code> for spatial queries.</td></tr><tr><td><code>coverage_radius_meters</code>, <code>coverage_angle_degrees</code>, <code>coverage_bearing_degrees</code></td><td>The camera's field-of-view cone.</td></tr><tr><td><code>is_active</code>, <code>last_seen_at</code>, <code>offline_notified_at</code></td><td>Health state.</td></tr></tbody></table></div>\n<p>Credentials are never returned raw — the model exposes <strong>gateway URLs</strong> instead (<code>gateway_rtsp_url</code>, <code>gateway_hls_url</code>, <code>gateway_webrtc_url</code>), and the HLS accessor returns a short-lived <strong>signed route</strong> (<code>/gateway/hls/{key}/index.m3u8</code>, ~5-minute TTL).</p>\n<h2 id=\"coverage\"><a class=\"anchor\" href=\"#coverage\">#</a>Coverage Cones</h2>\n<p>Each camera stores a directional field-of-view (radius + angle + bearing). The dispatcher map renders these as <strong>coverage cones</strong>, so operators can see exactly what is and isn't observable — and where the blind spots are. The PostGIS <code>location</code> point (kept in sync with <code>lat</code>/<code>lang</code>) powers nearest-camera and heatmap queries.</p>\n<h2 id=\"control\"><a class=\"anchor\" href=\"#control\">#</a>Device Control (Tapo / ONVIF)</h2>\n<p>The backend — and only the backend — can command a camera. Control flows through the <code>TapoCameraController</code> (<code>/api/camera/*</code>), every route protected by gateway IP allow-listing + <strong>HMAC</strong> (<code>gateway.ip</code>, <code>gateway.hmac</code>) and rate-limited. The surface is broad (40+ actions), grouped as:</p>\n<div class=\"table-shell\"><table><thead><tr><th>Group</th><th>Actions</th></tr></thead><tbody><tr><td><strong>Privacy / safety</strong></td><td><code>privacy</code>, <code>alarm</code> (siren + light), <code>whitelamp</code>, <code>cover</code>, <code>led</code></td></tr><tr><td><strong>PTZ &amp; tracking</strong></td><td><code>motor</code> (move/calibrate), <code>preset</code> (list/set/save/delete), <code>cruise</code>, <code>autotrack</code>, <code>smarttrack</code></td></tr><tr><td><strong>Detection toggles</strong></td><td><code>motion</code>, <code>person</code>, <code>babycry</code>, <code>tamper</code> (sensitivity-tunable)</td></tr><tr><td><strong>Image</strong></td><td><code>daynight</code>, <code>flip</code>, <code>ldc</code>, <code>osd</code>, <code>light-freq</code>, <code>video-qualities</code></td></tr><tr><td><strong>Audio</strong></td><td><code>microphone</code>, <code>speaker</code>, <code>audio-config</code>, <code>record-audio</code></td></tr><tr><td><strong>Recording / storage</strong></td><td><code>recordings</code>, <code>record-plan</code>, <code>circular-recording</code>, <code>sdcard</code>, <code>media-encrypt</code></td></tr><tr><td><strong>Device</strong></td><td><code>status</code>, <code>basic-info</code>, <code>time</code>, <code>timezone</code>, <code>firmware</code>, <code>reboot</code>, <code>diagnose</code>, <code>stream-url</code>, <code>events</code></td></tr></tbody></table></div>\n<p>Each action takes an <code>action</code> discriminator (e.g. <code>on</code>/<code>off</code>/<code>status</code>) plus action-specific params. The controller forwards the command to the Python gateway, which talks to the camera over Tapo's API.</p>\n<h2 id=\"gateway\"><a class=\"anchor\" href=\"#gateway\">#</a>The Streaming Gateway</h2>\n<p>A camera emits one RTSP feed, but three consumers need three formats. The <strong>Python Camera Gateway</strong> (<code>Modules/Gateway/tapo_server.py</code>, a persistent Flask service) solves the fan-out. Laravel drives it through <code>CameraGatewayService</code>:</p>\n<div class=\"table-shell\"><table><thead><tr><th>Operation</th><th>Gateway endpoint</th></tr></thead><tbody><tr><td>Register camera</td><td><code>POST /gateway/cameras/register</code></td></tr><tr><td>Start stream</td><td><code>POST /gateway/cameras/{key}/stream/start</code></td></tr><tr><td>Stop stream</td><td><code>POST /gateway/cameras/{key}/stream/stop</code></td></tr><tr><td>Stream status</td><td><code>GET /gateway/cameras/{key}/stream/status</code></td></tr><tr><td>Execute command</td><td><code>POST /gateway/cameras/{key}/command</code></td></tr><tr><td>Health</td><td><code>GET /gateway/health</code></td></tr></tbody></table></div>\n<p>Laravel-to-Python requests carry an <code>X-Gateway-Token</code> shared secret. The gateway keeps <strong>persistent, pooled <code>pytapo</code> connections</strong> (cached by <code>ip:user</code>, 5-minute TTL) so it doesn't re-handshake per request, and tracks one <code>ffmpeg</code> process per active stream. The token authenticates the service call; use localhost/private networking/VPN or HTTPS to protect credential-bearing traffic.</p>\n<p>Camera state is kept in sync with the gateway by jobs — <code>SyncCameraGatewayStreamJob</code> (register + start / stop), <code>SyncCameraRecordingJob</code>, and <code>SyncCameraPrivacyJob</code> (privacy follows <code>is_active</code>) — and the scheduled <code>camera:gateway:boot</code> command re-establishes all active streams after a restart.</p>\n<h2 id=\"formats\"><a class=\"anchor\" href=\"#formats\">#</a>Stream Formats</h2>\n<div class=\"table-shell\"><table><thead><tr><th>Format</th><th>Produced by</th><th>Consumer</th><th>Why</th></tr></thead><tbody><tr><td><strong>HLS</strong></td><td>gateway ffmpeg</td><td>mobile + lightweight web</td><td>Robust over poor networks; served via a signed route.</td></tr><tr><td><strong>RTSP relay</strong></td><td>gateway → MediaMTX</td><td>internal</td><td>Re-publishes the feed for WebRTC.</td></tr><tr><td><strong>WebRTC</strong></td><td>MediaMTX</td><td>dispatcher monitoring grid</td><td>Sub-second latency for live ops.</td></tr><tr><td><strong>RTSP relay</strong></td><td>gateway / MediaMTX</td><td>AI service + recording</td><td>Stable internal source for detection and recording.</td></tr></tbody></table></div>\n<p>The gateway transcodes RTSP → HLS with tunable, low-latency <code>ffmpeg</code> settings (1-second segments, <code>zerolatency</code>, configurable codec/bitrate/GOP), all driven by environment variables.</p>\n<h2 id=\"recording\"><a class=\"anchor\" href=\"#recording\">#</a>Continuous Recording</h2>\n<p>For cameras with <code>storage_type = none</code> (no on-device storage), <code>CameraRecordingService</code> runs a continuous server-side recording: one <code>ffmpeg</code> process per camera writing <strong>60-second MKV segments</strong>:</p>\n<div class=\"code-card\"><div class=\"code-toolbar\"><span>bash</span><button class=\"copy-btn\" type=\"button\">Copy</button></div><pre><code class=\"language-bash\">ffmpeg -rtsp_transport tcp -use_wallclock_as_timestamps 1 \\\n  -i {rtsp_url} -map 0:v -c:v copy -map 0:a? -c:a copy \\\n  -f segment -segment_time 60 -reset_timestamps 1 -strftime 1 \\\n  storage/recordings/{camera_id}/%Y-%m-%d_%H-%M-%S.mkv</code></pre></div>\n<p>Process PIDs are tracked in Redis (<code>camera_recording:{id}</code>) so the service can <code>start</code> / <code>stop</code> / <code>restart</code> / <code>isRecording</code> reliably. Recording starts automatically when such a camera is activated.</p>\n<h2 id=\"scene\"><a class=\"anchor\" href=\"#scene\">#</a>Scene Extraction (Evidence)</h2>\n<p>When <code>ExtractCrimeScene</code> is dispatched, <code>SceneExtractorService</code> on the <code>scenes</code> queue produces the evidence clip:</p>\n<ul>\n<li><strong>SD-card / cloud cameras</strong> — pull the clip directly from the camera for the incident window.</li>\n<li><strong>Storage-less cameras</strong> — find the recorded segments overlapping <code>[start − 30s, end + 30s]</code>, concat-merge them with <code>ffmpeg</code>, and transcode to a single faststart MP4.</li>\n</ul>\n<p>It also extracts a <strong>thumbnail</strong> (first frame) for quick preview, and writes both to <code>crimes/{crime_id}/</code>. The result is persisted as a <code>Scene</code> record linked to the crime.</p>\n<aside class=\"callout warning\"><div class=\"callout-icon\">⚠</div><div><strong>Important note</strong><p>The legacy direct model-report flow dispatches this job. The current AI → Incident → Crime flow accepts a time window but does not persist it on the Incident, so automatic extraction is not yet guaranteed for every AI-originated Crime.</p></div></aside>\n<h2 id=\"tamper\"><a class=\"anchor\" href=\"#tamper\">#</a>Tamper Detection</h2>\n<p>Cameras can be blinded — covered, sprayed, re-aimed, or cut off. The gateway analyses the feed and reports tamper signals to a webhook (<code>POST /v1/webhooks/camera-tamper</code>), creating a <code>CameraTamperEvent</code>:</p>\n<ul>\n<li><strong>Signal types</strong> — <code>blackout</code>, <code>variance_collapse</code>, <code>fov_shift</code>, <code>static_stream</code>, <code>heartbeat_gap</code>.</li>\n<li><strong>Status</strong> — <code>active</code> → <code>acknowledged</code> / <code>resolved</code> / <code>false_alarm</code>, with an admin acknowledgement note and an optional evidence frame (served via a 30-minute signed URL).</li>\n</ul>\n<p>A <code>CameraTamperDetected</code> event broadcasts on the private <code>admin.camera-health</code> channel so operators see blind-spots forming in real time.</p>\n<h2 id=\"filters\"><a class=\"anchor\" href=\"#filters\">#</a>Detection Filters</h2>\n<p><code>CameraDetectionFilter</code> lets a camera be tuned per crime type: a <code>min_confidence</code> threshold (default <code>0.700</code>) and an enabled flag, keyed uniquely by <code>(camera, crime_type)</code>. When the AI reports a detection, the filter decides whether it is <strong>accepted</strong> or <strong>filtered</strong> (by type or confidence) — and that decision is recorded in <code>ai_detection_logs</code>, giving a measurable view of model precision per camera.</p>\n<h2 id=\"health\"><a class=\"anchor\" href=\"#health\">#</a>Health &amp; Cleanup</h2>\n<ul>\n<li><strong>Health checks</strong> — the <code>camera:health-check</code> schedule pings cameras; unreachable cameras flip to offline (<code>CameraOffline</code> event) and notify their station.</li>\n<li><strong>Cleanup</strong> — the <code>CleanupOldRecordings</code> job (maintenance queue) deletes recording segments past the retention window (default 7 days) and crime evidence past its window (default 90 days).</li>\n</ul>\n<p>Continue to <a href=\"#/docs/ai-integration\">AI Model Integration</a>.</p>", "headings": [{"level": 1, "title": "Cameras & Streaming", "id": "cameras-streaming"}, {"level": 2, "title": "The Camera Model", "id": "model"}, {"level": 2, "title": "Coverage Cones", "id": "coverage"}, {"level": 2, "title": "Device Control (Tapo / ONVIF)", "id": "control"}, {"level": 2, "title": "The Streaming Gateway", "id": "gateway"}, {"level": 2, "title": "Stream Formats", "id": "formats"}, {"level": 2, "title": "Continuous Recording", "id": "recording"}, {"level": 2, "title": "Scene Extraction (Evidence)", "id": "scene"}, {"level": 2, "title": "Tamper Detection", "id": "tamper"}, {"level": 2, "title": "Detection Filters", "id": "filters"}, {"level": 2, "title": "Health & Cleanup", "id": "health"}]}, {"slug": "ai-integration", "title": "AI Model Integration", "section": "Features", "description": "AI Model Integration The AI detection service is a first class but tightly constrained client. It watches camera streams and reports what it sees — and nothing more. This page documents its …", "raw": "# AI Model Integration\n\n---\n\nThe AI detection service is a first-class but **tightly constrained** client. It watches camera streams and reports what it sees — and nothing more. This page documents its identity, the multi-layer security on its API, the encrypted camera handshake, and how its reports become incidents.\n\n- [Trust Model](#trust-model)\n- [The AiModel Identity](#identity)\n- [Security in Depth](#security)\n- [The Encrypted Camera Handshake](#handshake)\n- [The API](#api)\n- [From Report to Incident](#report-to-incident)\n- [Heartbeat & Liveness](#heartbeat)\n- [Detection Logging](#logging)\n\n<a name=\"trust-model\"></a>\n## Trust Model\n\nThe AI is treated as a useful but **untrusted** sensor. Through the normal production API it does not:\n\n- create a crime directly,\n- dispatch an officer,\n- or command a camera (alarm, PTZ).\n\nEverything it sends becomes a **pending incident** that a human dispatcher reviews, unless the optional auto-dispatch policy qualifies it. In the current model endpoint the backend sets the weapon signal to false, so a policy requiring weapon detection is not reached by normal model traffic.\n\n<a name=\"identity\"></a>\n## The AiModel Identity\n\nAn `AiModel` is a machine account registered by an administrator:\n\n| Field | Purpose |\n|-------|---------|\n| `email`, `password` | Login credentials (password hashed). |\n| `ip_address` | The single whitelisted source IP. |\n| `encryption_key` | A 64-char hex key, **regenerated on every login** (per-session). |\n| `signing_secret` | An HMAC secret (encrypted at rest) for request signing. |\n| `is_active`, `last_heartbeat_at`, `offline_notified_at` | Liveness state. |\n\nA model sees only the cameras assigned to it through the `camera_ai_model` pivot — never the whole fleet.\n\n<a name=\"security\"></a>\n## Security in Depth\n\nThree independent layers protect the model API; a request must pass all of them:\n\n1. **Sanctum bearer token** (`ai_model` guard) — issued at login with a 7-day TTL; old tokens are revoked on each login (single active session).\n2. **IP allow-listing** (`VerifyModelIpMiddleware`) — every request's source IP is compared (`hash_equals`) against the model's whitelisted `ip_address`; a mismatch is **403**. Login itself verifies the IP *before* issuing a token, so stolen credentials are useless from another host.\n3. **HMAC request signing** (`VerifyAiSignature`, on `/alert` and `/crime`) — the body is signed `sha256` with the model's `signing_secret`, sent as `X-Signature` with an `X-Timestamp` (±60 s window) and an `X-Nonce` (cached in Redis for 5 minutes to block replays).\n\n<a name=\"handshake\"></a>\n## The Encrypted Camera Handshake\n\nCamera credentials are never sent in the clear, even over TLS. On login the model receives a fresh `encryption_key`; when it fetches its cameras the sensitive fields are encrypted with **AES-256-CBC + HMAC** (via the shared `EncryptionService`):\n\n```\nlogin → { token, encryption_key }     (key regenerated, stored in DB)\nGET /cameras → per-camera { encrypted gateway_rtsp_url, gateway_webrtc_url, ... }\nmodel decrypts locally with encryption_key\n```\n\nThe envelope is a base64 JSON `{ iv, value, mac }`: a random 16-byte IV, the ciphertext, and an HMAC-SHA256 over `IV + ciphertext` that the model verifies before decrypting. Because the key rotates per login, an intercepted response is worthless after the next login.\n\n<a name=\"api\"></a>\n## The API\n\nBase prefix `/api/v1/model`, guard `ai_model`:\n\n| Method | Path | Auth chain | Purpose |\n|--------|------|-----------|---------|\n| `POST` | `/login` | throttle | Authenticate (IP-verified) → token + encryption_key. |\n| `POST` | `/logout` | token + IP | Revoke the current token. |\n| `GET` | `/cameras` | token + IP | Assigned cameras with encrypted stream URLs. |\n| `POST` | `/heartbeat` | token + IP | Liveness ping. |\n| `POST` | `/alert` | token + IP + HMAC | Report suspicious activity (`camera_id`, `confidence_score`). |\n| `POST` | `/crime` | token + IP + HMAC | Report a confirmed crime (`camera_id`, `description`, `confidence_score`, `start_time`, `end_time`). |\n\nModel endpoints return the standard `{ status, data, message }` envelope.\n\n<a name=\"report-to-incident\"></a>\n## From Report to Incident\n\n`AlertService` turns a report into an incident through the [incident layer](/{{route}}/{{version}}/dispatch):\n\n1. **Authorization** — confirms the camera is actually assigned to the calling model.\n2. **Filtering** — applies the camera's enabled detection filter and minimum confidence. The decision is written to `ai_detection_logs`. In the current implementation a camera with no enabled filter returns `filtered_type`; `no_filter_config` is used only when no camera is available to the decision method.\n3. **Incident creation** — `IncidentService::createFromAiAlert()` / `createFromAiCrime()` builds an `Incident` (`source = ai_alert` / `ai_crime`, status `pending_review`), copies the camera's station and location, stores the raw `confidence_score`, and runs the [Priority Engine](/{{route}}/{{version}}/priority-engine) to compute `priority_score`, `priority_tier`, and the explainable `priority_factors`.\n4. **Auto-dispatch check** — `AutoDispatchPolicy` evaluates the incident. The policy is deliberately narrow; however, the real model endpoint currently passes `weapon = false`, so weapon-gated auto-dispatch is effectively unavailable through that route.\n5. **Notification** — a job notifies the station, and the incident broadcasts onto the dispatcher console.\n\n<a name=\"heartbeat\"></a>\n## Heartbeat & Liveness\n\nA model is expected to ping `/heartbeat` periodically. The scheduled `model:check-heartbeats` command reads the configured timeout (default 2 minutes), flags any model whose `last_heartbeat_at` is stale as `is_active = false`, records `offline_notified_at`, fires the `AiModelOffline` broadcast (to the admin channel), and notifies administrators — so a dead detector is never silently trusted.\n\n<a name=\"logging\"></a>\n## Detection Logging\n\nEvery detection — accepted or filtered — is recorded in `ai_detection_logs` with the camera, crime type, model, confidence, decision, and timestamp. This feeds the **AI model analytics** in the admin console: acceptance rate, false-alarm rate, and per-camera precision, which together surface **model drift** before it can cause a bad dispatch.\n\nThe Crime API accepts `start_time` and `end_time`, but the current Incident schema does not retain them. The legacy direct Crime flow supports automatic Scene extraction; the newer Incident flow needs an additional persistence/extraction link before the same guarantee can be made.\n\nContinue to the [Priority Engine](/{{route}}/{{version}}/priority-engine).\n", "html": "<h1 id=\"ai-model-integration\"><a class=\"anchor\" href=\"#ai-model-integration\">#</a>AI Model Integration</h1>\n<hr>\n<p>The AI detection service is a first-class but <strong>tightly constrained</strong> client. It watches camera streams and reports what it sees — and nothing more. This page documents its identity, the multi-layer security on its API, the encrypted camera handshake, and how its reports become incidents.</p>\n<ul>\n<li><a href=\"#trust-model\">Trust Model</a></li>\n<li><a href=\"#identity\">The AiModel Identity</a></li>\n<li><a href=\"#security\">Security in Depth</a></li>\n<li><a href=\"#handshake\">The Encrypted Camera Handshake</a></li>\n<li><a href=\"#api\">The API</a></li>\n<li><a href=\"#report-to-incident\">From Report to Incident</a></li>\n<li><a href=\"#heartbeat\">Heartbeat &amp;amp; Liveness</a></li>\n<li><a href=\"#logging\">Detection Logging</a></li>\n</ul>\n<h2 id=\"trust-model\"><a class=\"anchor\" href=\"#trust-model\">#</a>Trust Model</h2>\n<p>The AI is treated as a useful but <strong>untrusted</strong> sensor. Through the normal production API it does not:</p>\n<ul>\n<li>create a crime directly,</li>\n<li>dispatch an officer,</li>\n<li>or command a camera (alarm, PTZ).</li>\n</ul>\n<p>Everything it sends becomes a <strong>pending incident</strong> that a human dispatcher reviews, unless the optional auto-dispatch policy qualifies it. In the current model endpoint the backend sets the weapon signal to false, so a policy requiring weapon detection is not reached by normal model traffic.</p>\n<h2 id=\"identity\"><a class=\"anchor\" href=\"#identity\">#</a>The AiModel Identity</h2>\n<p>An <code>AiModel</code> is a machine account registered by an administrator:</p>\n<div class=\"table-shell\"><table><thead><tr><th>Field</th><th>Purpose</th></tr></thead><tbody><tr><td><code>email</code>, <code>password</code></td><td>Login credentials (password hashed).</td></tr><tr><td><code>ip_address</code></td><td>The single whitelisted source IP.</td></tr><tr><td><code>encryption_key</code></td><td>A 64-char hex key, <strong>regenerated on every login</strong> (per-session).</td></tr><tr><td><code>signing_secret</code></td><td>An HMAC secret (encrypted at rest) for request signing.</td></tr><tr><td><code>is_active</code>, <code>last_heartbeat_at</code>, <code>offline_notified_at</code></td><td>Liveness state.</td></tr></tbody></table></div>\n<p>A model sees only the cameras assigned to it through the <code>camera_ai_model</code> pivot — never the whole fleet.</p>\n<h2 id=\"security\"><a class=\"anchor\" href=\"#security\">#</a>Security in Depth</h2>\n<p>Three independent layers protect the model API; a request must pass all of them:</p>\n<ol>\n<li><strong>Sanctum bearer token</strong> (<code>ai_model</code> guard) — issued at login with a 7-day TTL; old tokens are revoked on each login (single active session).</li>\n<li><strong>IP allow-listing</strong> (<code>VerifyModelIpMiddleware</code>) — every request's source IP is compared (<code>hash_equals</code>) against the model's whitelisted <code>ip_address</code>; a mismatch is <strong>403</strong>. Login itself verifies the IP <em>before</em> issuing a token, so stolen credentials are useless from another host.</li>\n<li><strong>HMAC request signing</strong> (<code>VerifyAiSignature</code>, on <code>/alert</code> and <code>/crime</code>) — the body is signed <code>sha256</code> with the model's <code>signing_secret</code>, sent as <code>X-Signature</code> with an <code>X-Timestamp</code> (±60 s window) and an <code>X-Nonce</code> (cached in Redis for 5 minutes to block replays).</li>\n</ol>\n<h2 id=\"handshake\"><a class=\"anchor\" href=\"#handshake\">#</a>The Encrypted Camera Handshake</h2>\n<p>Camera credentials are never sent in the clear, even over TLS. On login the model receives a fresh <code>encryption_key</code>; when it fetches its cameras the sensitive fields are encrypted with <strong>AES-256-CBC + HMAC</strong> (via the shared <code>EncryptionService</code>):</p>\n<div class=\"code-card\"><div class=\"code-toolbar\"><span>text</span><button class=\"copy-btn\" type=\"button\">Copy</button></div><pre><code class=\"language-text\">login → { token, encryption_key }     (key regenerated, stored in DB)\nGET /cameras → per-camera { encrypted gateway_rtsp_url, gateway_webrtc_url, ... }\nmodel decrypts locally with encryption_key</code></pre></div>\n<p>The envelope is a base64 JSON <code>{ iv, value, mac }</code>: a random 16-byte IV, the ciphertext, and an HMAC-SHA256 over <code>IV + ciphertext</code> that the model verifies before decrypting. Because the key rotates per login, an intercepted response is worthless after the next login.</p>\n<h2 id=\"api\"><a class=\"anchor\" href=\"#api\">#</a>The API</h2>\n<p>Base prefix <code>/api/v1/model</code>, guard <code>ai_model</code>:</p>\n<div class=\"table-shell\"><table><thead><tr><th>Method</th><th>Path</th><th>Auth chain</th><th>Purpose</th></tr></thead><tbody><tr><td><code>POST</code></td><td><code>/login</code></td><td>throttle</td><td>Authenticate (IP-verified) → token + encryption_key.</td></tr><tr><td><code>POST</code></td><td><code>/logout</code></td><td>token + IP</td><td>Revoke the current token.</td></tr><tr><td><code>GET</code></td><td><code>/cameras</code></td><td>token + IP</td><td>Assigned cameras with encrypted stream URLs.</td></tr><tr><td><code>POST</code></td><td><code>/heartbeat</code></td><td>token + IP</td><td>Liveness ping.</td></tr><tr><td><code>POST</code></td><td><code>/alert</code></td><td>token + IP + HMAC</td><td>Report suspicious activity (<code>camera_id</code>, <code>confidence_score</code>).</td></tr><tr><td><code>POST</code></td><td><code>/crime</code></td><td>token + IP + HMAC</td><td>Report a confirmed crime (<code>camera_id</code>, <code>description</code>, <code>confidence_score</code>, <code>start_time</code>, <code>end_time</code>).</td></tr></tbody></table></div>\n<p>Model endpoints return the standard <code>{ status, data, message }</code> envelope.</p>\n<h2 id=\"report-to-incident\"><a class=\"anchor\" href=\"#report-to-incident\">#</a>From Report to Incident</h2>\n<p><code>AlertService</code> turns a report into an incident through the <a href=\"#/docs/dispatch\">incident layer</a>:</p>\n<ol>\n<li><strong>Authorization</strong> — confirms the camera is actually assigned to the calling model.</li>\n<li><strong>Filtering</strong> — applies the camera's enabled detection filter and minimum confidence. The decision is written to <code>ai_detection_logs</code>. In the current implementation a camera with no enabled filter returns <code>filtered_type</code>; <code>no_filter_config</code> is used only when no camera is available to the decision method.</li>\n<li><strong>Incident creation</strong> — <code>IncidentService::createFromAiAlert()</code> / <code>createFromAiCrime()</code> builds an <code>Incident</code> (<code>source = ai_alert</code> / <code>ai_crime</code>, status <code>pending_review</code>), copies the camera's station and location, stores the raw <code>confidence_score</code>, and runs the <a href=\"#/docs/priority-engine\">Priority Engine</a> to compute <code>priority_score</code>, <code>priority_tier</code>, and the explainable <code>priority_factors</code>.</li>\n<li><strong>Auto-dispatch check</strong> — <code>AutoDispatchPolicy</code> evaluates the incident. The policy is deliberately narrow; however, the real model endpoint currently passes <code>weapon = false</code>, so weapon-gated auto-dispatch is effectively unavailable through that route.</li>\n<li><strong>Notification</strong> — a job notifies the station, and the incident broadcasts onto the dispatcher console.</li>\n</ol>\n<h2 id=\"heartbeat\"><a class=\"anchor\" href=\"#heartbeat\">#</a>Heartbeat &amp; Liveness</h2>\n<p>A model is expected to ping <code>/heartbeat</code> periodically. The scheduled <code>model:check-heartbeats</code> command reads the configured timeout (default 2 minutes), flags any model whose <code>last_heartbeat_at</code> is stale as <code>is_active = false</code>, records <code>offline_notified_at</code>, fires the <code>AiModelOffline</code> broadcast (to the admin channel), and notifies administrators — so a dead detector is never silently trusted.</p>\n<h2 id=\"logging\"><a class=\"anchor\" href=\"#logging\">#</a>Detection Logging</h2>\n<p>Every detection — accepted or filtered — is recorded in <code>ai_detection_logs</code> with the camera, crime type, model, confidence, decision, and timestamp. This feeds the <strong>AI model analytics</strong> in the admin console: acceptance rate, false-alarm rate, and per-camera precision, which together surface <strong>model drift</strong> before it can cause a bad dispatch.</p>\n<p>The Crime API accepts <code>start_time</code> and <code>end_time</code>, but the current Incident schema does not retain them. The legacy direct Crime flow supports automatic Scene extraction; the newer Incident flow needs an additional persistence/extraction link before the same guarantee can be made.</p>\n<p>Continue to the <a href=\"#/docs/priority-engine\">Priority Engine</a>.</p>", "headings": [{"level": 1, "title": "AI Model Integration", "id": "ai-model-integration"}, {"level": 2, "title": "Trust Model", "id": "trust-model"}, {"level": 2, "title": "The AiModel Identity", "id": "identity"}, {"level": 2, "title": "Security in Depth", "id": "security"}, {"level": 2, "title": "The Encrypted Camera Handshake", "id": "handshake"}, {"level": 2, "title": "The API", "id": "api"}, {"level": 2, "title": "From Report to Incident", "id": "report-to-incident"}, {"level": 2, "title": "Heartbeat & Liveness", "id": "heartbeat"}, {"level": 2, "title": "Detection Logging", "id": "logging"}]}, {"slug": "crime-lifecycle", "title": "Crime Lifecycle & Field Ops", "section": "Features", "description": "Crime Lifecycle & Field Ops Once a dispatcher commits a dispatch, an incident becomes a crime and the field operations layer takes over: assignment, navigation, the officer's status machine,…", "raw": "# Crime Lifecycle & Field Ops\n\n---\n\nOnce a dispatcher commits a dispatch, an incident becomes a **crime** and the field-operations layer takes over: assignment, navigation, the officer's status machine, escalation, and officer-safety features.\n\n- [The Crime Entity](#crime-entity)\n- [The Status Machine](#status-machine)\n- [Officer Assignment](#assignment)\n- [Officer Actions](#actions)\n- [Escalation](#escalation)\n- [Location Tracking & Proximity](#location)\n- [Panic / Officer Safety](#panic)\n- [BOLO Broadcasts](#bolo)\n- [Shifts, Status & Daily Routes](#shifts)\n- [Body-Cam Evidence](#bodycam)\n\n<a name=\"crime-entity\"></a>\n## The Crime Entity\n\nA `Crime` is a committed response, linked back to the incident that spawned it:\n\n| Field | Purpose |\n|-------|---------|\n| `incident_id` | The originating incident. |\n| `officer_id` | The assigned responder (nullable until assigned). |\n| `camera_id`, `scene_id` | Source camera and recorded evidence. |\n| `status`, `severity` | Lifecycle state and severity. |\n| `confidence_score`, `priority_score_snapshot` | The AI confidence and the priority at assignment time. |\n| `accepted_at`, `officer_arrive_time`, `resolved_at` | Lifecycle timestamps. |\n| `response_time_minutes` | Computed responsiveness metric. |\n| `escalation_count` | How many times it was reassigned. |\n| `no_visit_reason`, `no_visit_type`, `resolution_note` | Outcome detail. |\n\n<a name=\"status-machine\"></a>\n## The Status Machine\n\n```\n assigned ──accept──▶ in_progress ──arrive──▶ (visited) ──resolve──▶ resolved\n    │                                                                     \n    ├── no-visit (reason required) ─────────────▶ not_visited\n    │\n    └── no response in N min ───────────────────▶ escalated ──▶ reassigned\n```\n\n`status` is a `CrimeStatus` backed enum (`pending`, `assigned`, `in_progress`, `not_visited`, `resolved`, `escalated`, `false_alarm`). Every transition broadcasts back to the station console so the live map and timeline stay current.\n\n<a name=\"assignment\"></a>\n## Officer Assignment\n\n`NearestOfficerService` finds responders with a two-tier strategy:\n\n1. **Redis GEO (hot path)** — officer positions are kept in a Redis geo set per station; a `GEORADIUS` returns nearby officers in microseconds, using the *live* GPS rather than a stale database row.\n2. **PostGIS fallback** — a `ST_DWithin` + KNN (`<->`) query over `officers.current_location` when Redis is cold.\n\nThe Redis hot path filters to `is_on_shift = true`, `status = available`, the same station, and returns nearby candidates ordered by distance. The PostGIS fallback provides durable spatial lookup; its availability filter should be kept aligned with the Redis path. The chosen officer is assigned, notified by FCM + Echo (`CrimeAssigned`), and the crime moves to `assigned`.\n\n<a name=\"actions\"></a>\n## Officer Actions\n\nFrom the mobile app (`auth:officer` guard):\n\n| Action | Endpoint | Effect |\n|--------|----------|--------|\n| **Accept** | `PUT /api/v1/officer/crimes/{crime}/accept` | `accepted_at` set, status → `in_progress`. |\n| **No-visit** | `PUT …/no-visit` | Decline with a reason/type → `not_visited`. |\n| **Resolve** | `PUT …/resolve` | Close with a resolution note → `resolved`. |\n| **Brief** | `GET …/brief` | Pre-arrival tactical brief (crime type, weapon flag, snapshots, location history). |\n\nSupporting endpoints cover task history (`/task-history`) and the pre-arrival brief that gives an officer situational awareness *before* arriving.\n\n<a name=\"escalation\"></a>\n## Escalation\n\n`EscalationService::escalateStaleCrimes()` finds crimes still `assigned` past the timeout with fewer than the maximum attempts, groups them by station, and dispatches `EscalateStationCrimesJob`. Each escalation increments `escalation_count`, attempts reassignment, broadcasts `CrimeEscalated`, and flags the station for manual intervention when required. The command exists but is **not currently registered in the scheduler**.\n\n<a name=\"location\"></a>\n## Location Tracking & Proximity\n\nOfficers stream GPS every ~100 m or 30 s to `POST /api/v1/officer/location`. Redis is the live geospatial path, and the current service also synchronizes the latest location to PostGIS. `OfficerLocationUpdated` broadcasts on the officer's location channel and the station dispatch-map channel.\n\nA proximity detector compares an en-route officer's position to their target crime and emits `OfficerProximityChanged` with a state of `approaching` or `arrived`, so the dispatcher map can show responders converging in real time.\n\n<a name=\"panic\"></a>\n## Panic / Officer Safety\n\nAn officer in danger taps SOS → `POST /api/v1/officer/panic` (rate-limited 1 / 30 s), creating a `PanicEvent` (status `active`, with location and optional audio). This:\n\n- broadcasts `PanicActivated` to the station and the officer's own channel, and\n- dispatches `NotifyNearbyOfficersForBackup`, which finds available officers within ~2 km (Redis geo, PostGIS fallback) and pushes a backup request to them.\n\nCancelling (`POST …/panic/{panic}/cancel`) broadcasts `PanicResolved`. The station console raises a blocking panic modal over everything else — officer safety preempts the queue.\n\n<a name=\"bolo\"></a>\n## BOLO Broadcasts\n\nA **BOLO** (\"Be On the Look-Out\") is a station-wide alert about a person/vehicle/object. A `Bolo` carries a subject type, description, photo, an area (`lat`/`lng` + radius), a severity, and an expiry. Creating one fires `BoloBroadcasted` to all on-shift officers and the station map; officers fetch the active set via `GET /api/v1/officer/bolos/active`.\n\n<a name=\"shifts\"></a>\n## Shifts, Status & Daily Routes\n\n- **Status** — `available` / `busy` / `offline`, changed via `PUT /api/v1/officer/status`; every transition is recorded in `officer_status_logs` for accountability and a daily-activity summary (service minutes, completed tasks, average response time).\n- **Shifts & patrol zones** — `officer_shifts` defines shift windows and an optional patrol polygon/zone, set by the station.\n- **Daily routes** — `ProcessDailyOfficerRoutes` (scheduled, chunked) pulls each officer's GPS trail from Redis for the previous day, encodes it as a polyline, and stores it in `officer_activity_logs` with total distance and update count — a compact, replayable patrol history.\n\n<a name=\"bodycam\"></a>\n## Body-Cam Evidence\n\nOfficers can attach body-cam footage to a crime (`POST /api/v1/officer/crimes/{crime}/body-cam`), stored as a `body_cam_upload` and served through a signed URL. Combined with the camera [scene evidence](/{{route}}/{{version}}/cameras), a crime carries both the detecting camera's clip and the responding officer's footage.\n\nContinue to [Real-Time & Notifications](/{{route}}/{{version}}/realtime).\n", "html": "<h1 id=\"crime-lifecycle-field-ops\"><a class=\"anchor\" href=\"#crime-lifecycle-field-ops\">#</a>Crime Lifecycle &amp; Field Ops</h1>\n<hr>\n<p>Once a dispatcher commits a dispatch, an incident becomes a <strong>crime</strong> and the field-operations layer takes over: assignment, navigation, the officer's status machine, escalation, and officer-safety features.</p>\n<ul>\n<li><a href=\"#crime-entity\">The Crime Entity</a></li>\n<li><a href=\"#status-machine\">The Status Machine</a></li>\n<li><a href=\"#assignment\">Officer Assignment</a></li>\n<li><a href=\"#actions\">Officer Actions</a></li>\n<li><a href=\"#escalation\">Escalation</a></li>\n<li><a href=\"#location\">Location Tracking &amp;amp; Proximity</a></li>\n<li><a href=\"#panic\">Panic / Officer Safety</a></li>\n<li><a href=\"#bolo\">BOLO Broadcasts</a></li>\n<li><a href=\"#shifts\">Shifts, Status &amp;amp; Daily Routes</a></li>\n<li><a href=\"#bodycam\">Body-Cam Evidence</a></li>\n</ul>\n<h2 id=\"crime-entity\"><a class=\"anchor\" href=\"#crime-entity\">#</a>The Crime Entity</h2>\n<p>A <code>Crime</code> is a committed response, linked back to the incident that spawned it:</p>\n<div class=\"table-shell\"><table><thead><tr><th>Field</th><th>Purpose</th></tr></thead><tbody><tr><td><code>incident_id</code></td><td>The originating incident.</td></tr><tr><td><code>officer_id</code></td><td>The assigned responder (nullable until assigned).</td></tr><tr><td><code>camera_id</code>, <code>scene_id</code></td><td>Source camera and recorded evidence.</td></tr><tr><td><code>status</code>, <code>severity</code></td><td>Lifecycle state and severity.</td></tr><tr><td><code>confidence_score</code>, <code>priority_score_snapshot</code></td><td>The AI confidence and the priority at assignment time.</td></tr><tr><td><code>accepted_at</code>, <code>officer_arrive_time</code>, <code>resolved_at</code></td><td>Lifecycle timestamps.</td></tr><tr><td><code>response_time_minutes</code></td><td>Computed responsiveness metric.</td></tr><tr><td><code>escalation_count</code></td><td>How many times it was reassigned.</td></tr><tr><td><code>no_visit_reason</code>, <code>no_visit_type</code>, <code>resolution_note</code></td><td>Outcome detail.</td></tr></tbody></table></div>\n<h2 id=\"status-machine\"><a class=\"anchor\" href=\"#status-machine\">#</a>The Status Machine</h2>\n<div class=\"code-card\"><div class=\"code-toolbar\"><span>text</span><button class=\"copy-btn\" type=\"button\">Copy</button></div><pre><code class=\"language-text\"> assigned ──accept──▶ in_progress ──arrive──▶ (visited) ──resolve──▶ resolved\n    │                                                                     \n    ├── no-visit (reason required) ─────────────▶ not_visited\n    │\n    └── no response in N min ───────────────────▶ escalated ──▶ reassigned</code></pre></div>\n<p><code>status</code> is a <code>CrimeStatus</code> backed enum (<code>pending</code>, <code>assigned</code>, <code>in_progress</code>, <code>not_visited</code>, <code>resolved</code>, <code>escalated</code>, <code>false_alarm</code>). Every transition broadcasts back to the station console so the live map and timeline stay current.</p>\n<h2 id=\"assignment\"><a class=\"anchor\" href=\"#assignment\">#</a>Officer Assignment</h2>\n<p><code>NearestOfficerService</code> finds responders with a two-tier strategy:</p>\n<ol>\n<li><strong>Redis GEO (hot path)</strong> — officer positions are kept in a Redis geo set per station; a <code>GEORADIUS</code> returns nearby officers in microseconds, using the <em>live</em> GPS rather than a stale database row.</li>\n<li><strong>PostGIS fallback</strong> — a <code>ST_DWithin</code> + KNN (<code>&amp;lt;-&amp;gt;</code>) query over <code>officers.current_location</code> when Redis is cold.</li>\n</ol>\n<p>The Redis hot path filters to <code>is_on_shift = true</code>, <code>status = available</code>, the same station, and returns nearby candidates ordered by distance. The PostGIS fallback provides durable spatial lookup; its availability filter should be kept aligned with the Redis path. The chosen officer is assigned, notified by FCM + Echo (<code>CrimeAssigned</code>), and the crime moves to <code>assigned</code>.</p>\n<h2 id=\"actions\"><a class=\"anchor\" href=\"#actions\">#</a>Officer Actions</h2>\n<p>From the mobile app (<code>auth:officer</code> guard):</p>\n<div class=\"table-shell\"><table><thead><tr><th>Action</th><th>Endpoint</th><th>Effect</th></tr></thead><tbody><tr><td><strong>Accept</strong></td><td><code>PUT /api/v1/officer/crimes/{crime}/accept</code></td><td><code>accepted_at</code> set, status → <code>in_progress</code>.</td></tr><tr><td><strong>No-visit</strong></td><td><code>PUT …/no-visit</code></td><td>Decline with a reason/type → <code>not_visited</code>.</td></tr><tr><td><strong>Resolve</strong></td><td><code>PUT …/resolve</code></td><td>Close with a resolution note → <code>resolved</code>.</td></tr><tr><td><strong>Brief</strong></td><td><code>GET …/brief</code></td><td>Pre-arrival tactical brief (crime type, weapon flag, snapshots, location history).</td></tr></tbody></table></div>\n<p>Supporting endpoints cover task history (<code>/task-history</code>) and the pre-arrival brief that gives an officer situational awareness <em>before</em> arriving.</p>\n<h2 id=\"escalation\"><a class=\"anchor\" href=\"#escalation\">#</a>Escalation</h2>\n<p><code>EscalationService::escalateStaleCrimes()</code> finds crimes still <code>assigned</code> past the timeout with fewer than the maximum attempts, groups them by station, and dispatches <code>EscalateStationCrimesJob</code>. Each escalation increments <code>escalation_count</code>, attempts reassignment, broadcasts <code>CrimeEscalated</code>, and flags the station for manual intervention when required. The command exists but is <strong>not currently registered in the scheduler</strong>.</p>\n<h2 id=\"location\"><a class=\"anchor\" href=\"#location\">#</a>Location Tracking &amp; Proximity</h2>\n<p>Officers stream GPS every ~100 m or 30 s to <code>POST /api/v1/officer/location</code>. Redis is the live geospatial path, and the current service also synchronizes the latest location to PostGIS. <code>OfficerLocationUpdated</code> broadcasts on the officer's location channel and the station dispatch-map channel.</p>\n<p>A proximity detector compares an en-route officer's position to their target crime and emits <code>OfficerProximityChanged</code> with a state of <code>approaching</code> or <code>arrived</code>, so the dispatcher map can show responders converging in real time.</p>\n<h2 id=\"panic\"><a class=\"anchor\" href=\"#panic\">#</a>Panic / Officer Safety</h2>\n<p>An officer in danger taps SOS → <code>POST /api/v1/officer/panic</code> (rate-limited 1 / 30 s), creating a <code>PanicEvent</code> (status <code>active</code>, with location and optional audio). This:</p>\n<ul>\n<li>broadcasts <code>PanicActivated</code> to the station and the officer's own channel, and</li>\n<li>dispatches <code>NotifyNearbyOfficersForBackup</code>, which finds available officers within ~2 km (Redis geo, PostGIS fallback) and pushes a backup request to them.</li>\n</ul>\n<p>Cancelling (<code>POST …/panic/{panic}/cancel</code>) broadcasts <code>PanicResolved</code>. The station console raises a blocking panic modal over everything else — officer safety preempts the queue.</p>\n<h2 id=\"bolo\"><a class=\"anchor\" href=\"#bolo\">#</a>BOLO Broadcasts</h2>\n<p>A <strong>BOLO</strong> (\"Be On the Look-Out\") is a station-wide alert about a person/vehicle/object. A <code>Bolo</code> carries a subject type, description, photo, an area (<code>lat</code>/<code>lng</code> + radius), a severity, and an expiry. Creating one fires <code>BoloBroadcasted</code> to all on-shift officers and the station map; officers fetch the active set via <code>GET /api/v1/officer/bolos/active</code>.</p>\n<h2 id=\"shifts\"><a class=\"anchor\" href=\"#shifts\">#</a>Shifts, Status &amp; Daily Routes</h2>\n<ul>\n<li><strong>Status</strong> — <code>available</code> / <code>busy</code> / <code>offline</code>, changed via <code>PUT /api/v1/officer/status</code>; every transition is recorded in <code>officer_status_logs</code> for accountability and a daily-activity summary (service minutes, completed tasks, average response time).</li>\n<li><strong>Shifts &amp; patrol zones</strong> — <code>officer_shifts</code> defines shift windows and an optional patrol polygon/zone, set by the station.</li>\n<li><strong>Daily routes</strong> — <code>ProcessDailyOfficerRoutes</code> (scheduled, chunked) pulls each officer's GPS trail from Redis for the previous day, encodes it as a polyline, and stores it in <code>officer_activity_logs</code> with total distance and update count — a compact, replayable patrol history.</li>\n</ul>\n<h2 id=\"bodycam\"><a class=\"anchor\" href=\"#bodycam\">#</a>Body-Cam Evidence</h2>\n<p>Officers can attach body-cam footage to a crime (<code>POST /api/v1/officer/crimes/{crime}/body-cam</code>), stored as a <code>body_cam_upload</code> and served through a signed URL. Combined with the camera <a href=\"#/docs/cameras\">scene evidence</a>, a crime carries both the detecting camera's clip and the responding officer's footage.</p>\n<p>Continue to <a href=\"#/docs/realtime\">Real-Time &amp;amp; Notifications</a>.</p>", "headings": [{"level": 1, "title": "Crime Lifecycle & Field Ops", "id": "crime-lifecycle-field-ops"}, {"level": 2, "title": "The Crime Entity", "id": "crime-entity"}, {"level": 2, "title": "The Status Machine", "id": "status-machine"}, {"level": 2, "title": "Officer Assignment", "id": "assignment"}, {"level": 2, "title": "Officer Actions", "id": "actions"}, {"level": 2, "title": "Escalation", "id": "escalation"}, {"level": 2, "title": "Location Tracking & Proximity", "id": "location"}, {"level": 2, "title": "Panic / Officer Safety", "id": "panic"}, {"level": 2, "title": "BOLO Broadcasts", "id": "bolo"}, {"level": 2, "title": "Shifts, Status & Daily Routes", "id": "shifts"}, {"level": 2, "title": "Body-Cam Evidence", "id": "bodycam"}]}, {"slug": "realtime", "title": "Real-Time & Notifications", "section": "Features", "description": "Real Time & Notifications CrimeLens is an event driven system: the moment something changes, every screen that cares finds out. This page documents the two delivery rails WebSockets for cons…", "raw": "# Real-Time & Notifications\n\n---\n\nCrimeLens is an event-driven system: the moment something changes, every screen that cares finds out. This page documents the two delivery rails (WebSockets for consoles, FCM for mobile), the channel topology, and the full event catalogue.\n\n- [Two Delivery Rails](#rails)\n- [Channel Topology](#channels)\n- [Channel Authorization](#authorization)\n- [Event Catalogue](#catalogue)\n- [Consistency Patterns](#patterns)\n- [Push Notifications (FCM)](#fcm)\n\n<a name=\"rails\"></a>\n## Two Delivery Rails\n\n| Rail | Transport | Audience | Used for |\n|------|-----------|----------|----------|\n| **Laravel Echo / Pusher** | WebSockets | Web consoles (admin, station) | Live queue, map, toasts, presence. |\n| **Firebase Cloud Messaging** | Push | Mobile (officer, station companion) | Background delivery of assignments, panic, BOLO. |\n\nBroadcast events implement `ShouldBroadcast` and are dispatched onto the Redis queue, processed by a dedicated Horizon supervisor, and delivered through Pusher. Consoles subscribe through the module bootstrap that wires Echo to the correct `/broadcasting/auth` endpoint per guard.\n\n<a name=\"channels\"></a>\n## Channel Topology\n\nChannels are private (or presence) and scoped to a tenant or a person:\n\n| Channel | Subscribers | Carries |\n|---------|-------------|---------|\n| `station.{id}` | station web guards | Incidents, citizen tips, BOLO, proximity, pattern alerts. |\n| `police-station.{id}.officers` | station guards | Officer locations for the dispatch map. |\n| `officer.{id}` | the officer | Their assignments and status. |\n| `officer.location.{id}` | officer, station, admin | Full-fidelity GPS stream. |\n| `panic.station.{id}` / `panic.officer.{id}` | station / officer | Panic activation & resolution. |\n| `admin.camera-health` | admins | Camera tamper events. |\n| `admin.officers-locations.{id}` | admins | Officer positions per station. |\n| `chat.{conversationKey}` (presence) | participants | Chat messages & transcripts. |\n| `admin.*` | admins | System-level alerts (escalations, AI offline). |\n\n<a name=\"authorization\"></a>\n## Channel Authorization\n\nEvery private channel has an authorization callback (`Modules/*/routes/channels.php`). `station.{id}` is authorised for both the institutional `police_station_web` account and the individual `station_user_web` — so an operator only ever subscribes to *their* station. Officer and panic channels are bound to the owning officer; admin channels to administrators. This is the multi-tenant boundary enforced at the socket layer.\n\n<a name=\"catalogue\"></a>\n## Event Catalogue\n\nEighteen domain events drive the real-time experience:\n\n**Incidents & Dispatch**\n\n| Event | Channel · `broadcastAs` | Key payload |\n|-------|------------------------|-------------|\n| `IncidentCreated` | `station.{id}` · `incident.created` | id, camera_id, priority_tier, priority_score, source |\n| `IncidentAssigned` | `station.{id}` · `incident.assigned` | incident_id, assigned_dispatcher_id, dispatcher_name, action (`claimed`/`released`/`rebalanced`), full incident |\n| `IncidentReviewed` | `station.{id}` · `incident.reviewed` | id, status, action (`dispatched`/`rejected`), crime_id |\n\n**Crimes & Officers**\n\n| Event | Channel · `broadcastAs` | Key payload |\n|-------|------------------------|-------------|\n| `CrimeAssigned` | `officer.{id}`, `station.{id}` · `crime.assigned` | crime_id, severity, location, officer_id, station_id |\n| `CrimeEscalated` | `station.{id}`, `admin.*` · `crime.escalated` | crime_id, attempt_number, previous/new officer |\n| `CrimeResolved` | `station.{id}`, `admin.*` · `crime.resolved` | crime_id, officer_id, resolved_at |\n| `OfficerLocationUpdated` | `officer.location.{id}`, `police-station.{id}.officers`, `admin.officers-locations.{id}` · `officer.location-updated` | officer_id, lat, lng, name |\n| `OfficerProximityChanged` | `station.{id}` · `officer.approaching`/`officer.arrived` | crime_id, officer_id, distance_meters, state |\n\n**Safety & Community**\n\n| Event | Channel · `broadcastAs` | Key payload |\n|-------|------------------------|-------------|\n| `PanicActivated` | `panic.station.{id}`, `panic.officer.{id}` · `panic.activated` | id, officer_id, lat, lng, officer_name |\n| `PanicResolved` | `panic.station.{id}`, `panic.officer.{id}` · `panic.resolved` | id, ended_at |\n| `CitizenTipReceived` | `station.{id}` · `citizen-tip.received` | id, source, masked_sender, message_preview, media_count |\n| `BoloBroadcasted` | `police-station.{id}.officers`, `station.{id}` · `bolo.broadcasted` | id, subject_type, description, severity, photo_url |\n| `WeaponClusterDetected` | `station.{id}` · `pattern.weapon-cluster.detected` | alert_id, incident_count, incident_ids, center_lat/lng |\n\n**Infrastructure**\n\n| Event | Channel · `broadcastAs` | Key payload |\n|-------|------------------------|-------------|\n| `CameraOffline` | `station.{id}`, `admin.*` · `camera.offline` | camera_id, name, last_seen_at |\n| `CameraTamperDetected` | `admin.camera-health` · `camera.tamper.detected` | event_id, camera_id, signal_type, status |\n| `AiModelOffline` | `admin.*` · `ai_model.offline` | model_id, name, last_heartbeat_at |\n\n**Communications**\n\n| Event | Channel · `broadcastAs` | Key payload |\n|-------|------------------------|-------------|\n| `ChatMessageSent` | `chat.{key}` (presence) + station/admin · `ChatMessageSent` | id, sender, message, message_type, media URLs, transcript |\n| `ChatTranscriptUpdated` | `chat.{key}` (presence) · `ChatTranscriptUpdated` | id, transcript |\n\n<a name=\"patterns\"></a>\n## Consistency Patterns\n\nTwo patterns keep the consoles correct under concurrency:\n\n- **`toOthers()` on operator actions** — when a dispatcher claims/releases/dispatches, the broadcast excludes the acting socket. The actor's own UI already updated optimistically (and showed a personal success toast); only *other* operators receive the third-person echo. This avoids double toasts and flicker.\n- **Per-callback listener cleanup** — multiple console components subscribe to the same `station.{id}` channel. Each registers and tears down its *own* named callback (never a blanket `stopListening(event)`), so one component unmounting can't silently kill another's live updates.\n\n<a name=\"fcm\"></a>\n## Push Notifications (FCM)\n\nMobile clients register an FCM token (polymorphic `firebase_tokens`, per device type). The backend (via `kreait/laravel-firebase`) pushes notifications for crime assignment, escalation, panic/backup, and chat — the background rail that reaches an officer whose app is closed. Database notifications (`notifications` table) back the in-app notification centre on every surface.\n\nContinue to the [Data Model](/{{route}}/{{version}}/data-model).\n", "html": "<h1 id=\"real-time-notifications\"><a class=\"anchor\" href=\"#real-time-notifications\">#</a>Real-Time &amp; Notifications</h1>\n<hr>\n<p>CrimeLens is an event-driven system: the moment something changes, every screen that cares finds out. This page documents the two delivery rails (WebSockets for consoles, FCM for mobile), the channel topology, and the full event catalogue.</p>\n<ul>\n<li><a href=\"#rails\">Two Delivery Rails</a></li>\n<li><a href=\"#channels\">Channel Topology</a></li>\n<li><a href=\"#authorization\">Channel Authorization</a></li>\n<li><a href=\"#catalogue\">Event Catalogue</a></li>\n<li><a href=\"#patterns\">Consistency Patterns</a></li>\n<li><a href=\"#fcm\">Push Notifications (FCM)</a></li>\n</ul>\n<h2 id=\"rails\"><a class=\"anchor\" href=\"#rails\">#</a>Two Delivery Rails</h2>\n<div class=\"table-shell\"><table><thead><tr><th>Rail</th><th>Transport</th><th>Audience</th><th>Used for</th></tr></thead><tbody><tr><td><strong>Laravel Echo / Pusher</strong></td><td>WebSockets</td><td>Web consoles (admin, station)</td><td>Live queue, map, toasts, presence.</td></tr><tr><td><strong>Firebase Cloud Messaging</strong></td><td>Push</td><td>Mobile (officer, station companion)</td><td>Background delivery of assignments, panic, BOLO.</td></tr></tbody></table></div>\n<p>Broadcast events implement <code>ShouldBroadcast</code> and are dispatched onto the Redis queue, processed by a dedicated Horizon supervisor, and delivered through Pusher. Consoles subscribe through the module bootstrap that wires Echo to the correct <code>/broadcasting/auth</code> endpoint per guard.</p>\n<h2 id=\"channels\"><a class=\"anchor\" href=\"#channels\">#</a>Channel Topology</h2>\n<p>Channels are private (or presence) and scoped to a tenant or a person:</p>\n<div class=\"table-shell\"><table><thead><tr><th>Channel</th><th>Subscribers</th><th>Carries</th></tr></thead><tbody><tr><td><code>station.{id}</code></td><td>station web guards</td><td>Incidents, citizen tips, BOLO, proximity, pattern alerts.</td></tr><tr><td><code>police-station.{id}.officers</code></td><td>station guards</td><td>Officer locations for the dispatch map.</td></tr><tr><td><code>officer.{id}</code></td><td>the officer</td><td>Their assignments and status.</td></tr><tr><td><code>officer.location.{id}</code></td><td>officer, station, admin</td><td>Full-fidelity GPS stream.</td></tr><tr><td><code>panic.station.{id}</code> / <code>panic.officer.{id}</code></td><td>station / officer</td><td>Panic activation &amp; resolution.</td></tr><tr><td><code>admin.camera-health</code></td><td>admins</td><td>Camera tamper events.</td></tr><tr><td><code>admin.officers-locations.{id}</code></td><td>admins</td><td>Officer positions per station.</td></tr><tr><td><code>chat.{conversationKey}</code> (presence)</td><td>participants</td><td>Chat messages &amp; transcripts.</td></tr><tr><td><code>admin.*</code></td><td>admins</td><td>System-level alerts (escalations, AI offline).</td></tr></tbody></table></div>\n<h2 id=\"authorization\"><a class=\"anchor\" href=\"#authorization\">#</a>Channel Authorization</h2>\n<p>Every private channel has an authorization callback (<code>Modules/<em>/routes/channels.php</code>). <code>station.{id}</code> is authorised for both the institutional <code>police_station_web</code> account and the individual <code>station_user_web</code> — so an operator only ever subscribes to </em>their* station. Officer and panic channels are bound to the owning officer; admin channels to administrators. This is the multi-tenant boundary enforced at the socket layer.</p>\n<h2 id=\"catalogue\"><a class=\"anchor\" href=\"#catalogue\">#</a>Event Catalogue</h2>\n<p>Eighteen domain events drive the real-time experience:</p>\n<p><strong>Incidents &amp; Dispatch</strong></p>\n<div class=\"table-shell\"><table><thead><tr><th>Event</th><th>Channel · <code>broadcastAs</code></th><th>Key payload</th></tr></thead><tbody><tr><td><code>IncidentCreated</code></td><td><code>station.{id}</code> · <code>incident.created</code></td><td>id, camera_id, priority_tier, priority_score, source</td></tr><tr><td><code>IncidentAssigned</code></td><td><code>station.{id}</code> · <code>incident.assigned</code></td><td>incident_id, assigned_dispatcher_id, dispatcher_name, action (<code>claimed</code>/<code>released</code>/<code>rebalanced</code>), full incident</td></tr><tr><td><code>IncidentReviewed</code></td><td><code>station.{id}</code> · <code>incident.reviewed</code></td><td>id, status, action (<code>dispatched</code>/<code>rejected</code>), crime_id</td></tr></tbody></table></div>\n<p><strong>Crimes &amp; Officers</strong></p>\n<div class=\"table-shell\"><table><thead><tr><th>Event</th><th>Channel · <code>broadcastAs</code></th><th>Key payload</th></tr></thead><tbody><tr><td><code>CrimeAssigned</code></td><td><code>officer.{id}</code>, <code>station.{id}</code> · <code>crime.assigned</code></td><td>crime_id, severity, location, officer_id, station_id</td></tr><tr><td><code>CrimeEscalated</code></td><td><code>station.{id}</code>, <code>admin.*</code> · <code>crime.escalated</code></td><td>crime_id, attempt_number, previous/new officer</td></tr><tr><td><code>CrimeResolved</code></td><td><code>station.{id}</code>, <code>admin.*</code> · <code>crime.resolved</code></td><td>crime_id, officer_id, resolved_at</td></tr><tr><td><code>OfficerLocationUpdated</code></td><td><code>officer.location.{id}</code>, <code>police-station.{id}.officers</code>, <code>admin.officers-locations.{id}</code> · <code>officer.location-updated</code></td><td>officer_id, lat, lng, name</td></tr><tr><td><code>OfficerProximityChanged</code></td><td><code>station.{id}</code> · <code>officer.approaching</code>/<code>officer.arrived</code></td><td>crime_id, officer_id, distance_meters, state</td></tr></tbody></table></div>\n<p><strong>Safety &amp; Community</strong></p>\n<div class=\"table-shell\"><table><thead><tr><th>Event</th><th>Channel · <code>broadcastAs</code></th><th>Key payload</th></tr></thead><tbody><tr><td><code>PanicActivated</code></td><td><code>panic.station.{id}</code>, <code>panic.officer.{id}</code> · <code>panic.activated</code></td><td>id, officer_id, lat, lng, officer_name</td></tr><tr><td><code>PanicResolved</code></td><td><code>panic.station.{id}</code>, <code>panic.officer.{id}</code> · <code>panic.resolved</code></td><td>id, ended_at</td></tr><tr><td><code>CitizenTipReceived</code></td><td><code>station.{id}</code> · <code>citizen-tip.received</code></td><td>id, source, masked_sender, message_preview, media_count</td></tr><tr><td><code>BoloBroadcasted</code></td><td><code>police-station.{id}.officers</code>, <code>station.{id}</code> · <code>bolo.broadcasted</code></td><td>id, subject_type, description, severity, photo_url</td></tr><tr><td><code>WeaponClusterDetected</code></td><td><code>station.{id}</code> · <code>pattern.weapon-cluster.detected</code></td><td>alert_id, incident_count, incident_ids, center_lat/lng</td></tr></tbody></table></div>\n<p><strong>Infrastructure</strong></p>\n<div class=\"table-shell\"><table><thead><tr><th>Event</th><th>Channel · <code>broadcastAs</code></th><th>Key payload</th></tr></thead><tbody><tr><td><code>CameraOffline</code></td><td><code>station.{id}</code>, <code>admin.*</code> · <code>camera.offline</code></td><td>camera_id, name, last_seen_at</td></tr><tr><td><code>CameraTamperDetected</code></td><td><code>admin.camera-health</code> · <code>camera.tamper.detected</code></td><td>event_id, camera_id, signal_type, status</td></tr><tr><td><code>AiModelOffline</code></td><td><code>admin.*</code> · <code>ai_model.offline</code></td><td>model_id, name, last_heartbeat_at</td></tr></tbody></table></div>\n<p><strong>Communications</strong></p>\n<div class=\"table-shell\"><table><thead><tr><th>Event</th><th>Channel · <code>broadcastAs</code></th><th>Key payload</th></tr></thead><tbody><tr><td><code>ChatMessageSent</code></td><td><code>chat.{key}</code> (presence) + station/admin · <code>ChatMessageSent</code></td><td>id, sender, message, message_type, media URLs, transcript</td></tr><tr><td><code>ChatTranscriptUpdated</code></td><td><code>chat.{key}</code> (presence) · <code>ChatTranscriptUpdated</code></td><td>id, transcript</td></tr></tbody></table></div>\n<h2 id=\"patterns\"><a class=\"anchor\" href=\"#patterns\">#</a>Consistency Patterns</h2>\n<p>Two patterns keep the consoles correct under concurrency:</p>\n<ul>\n<li><strong><code>toOthers()</code> on operator actions</strong> — when a dispatcher claims/releases/dispatches, the broadcast excludes the acting socket. The actor's own UI already updated optimistically (and showed a personal success toast); only <em>other</em> operators receive the third-person echo. This avoids double toasts and flicker.</li>\n<li><strong>Per-callback listener cleanup</strong> — multiple console components subscribe to the same <code>station.{id}</code> channel. Each registers and tears down its <em>own</em> named callback (never a blanket <code>stopListening(event)</code>), so one component unmounting can't silently kill another's live updates.</li>\n</ul>\n<h2 id=\"fcm\"><a class=\"anchor\" href=\"#fcm\">#</a>Push Notifications (FCM)</h2>\n<p>Mobile clients register an FCM token (polymorphic <code>firebase_tokens</code>, per device type). The backend (via <code>kreait/laravel-firebase</code>) pushes notifications for crime assignment, escalation, panic/backup, and chat — the background rail that reaches an officer whose app is closed. Database notifications (<code>notifications</code> table) back the in-app notification centre on every surface.</p>\n<p>Continue to the <a href=\"#/docs/data-model\">Data Model</a>.</p>", "headings": [{"level": 1, "title": "Real-Time & Notifications", "id": "real-time-notifications"}, {"level": 2, "title": "Two Delivery Rails", "id": "rails"}, {"level": 2, "title": "Channel Topology", "id": "channels"}, {"level": 2, "title": "Channel Authorization", "id": "authorization"}, {"level": 2, "title": "Event Catalogue", "id": "catalogue"}, {"level": 2, "title": "Consistency Patterns", "id": "patterns"}, {"level": 2, "title": "Push Notifications (FCM)", "id": "fcm"}]}, {"slug": "api", "title": "API Reference", "section": "Reference", "description": "API Reference CrimeLens exposes 345 non vendor application routes as discovered on 23 June 2026 across six modules. They divide by audience: machine APIs AI, camera gateway , mobile APIs off…", "raw": "# API Reference\n\n---\n\nCrimeLens exposes **345 non-vendor application routes** as discovered on 23 June 2026 across six modules. They divide by audience: machine APIs (AI, camera gateway), mobile APIs (officer, station companion), web consoles (admin, station), and public intake. This page is a navigational map — each group links conceptually to its feature page.\n\n- [Conventions](#conventions)\n- [Shared & Auth (`/api/v1/{guard}`)](#shared)\n- [Officer Mobile API](#officer)\n- [Police Station API](#station-api)\n- [AI Model API](#model)\n- [Camera Control API](#camera)\n- [Station Web Console](#station-web)\n- [Admin Console](#admin)\n- [Public & Webhooks](#public)\n- [Media & Streaming](#media)\n\n<a name=\"conventions\"></a>\n## Conventions\n\n- **Response envelope** — most JSON APIs use `{ status, data, message }`; a few utility endpoints return purpose-specific payloads. Web consoles use Inertia responses.\n- **Versioning** — mobile/machine APIs are under `/api/v1`.\n- **Auth** — bearer (Sanctum) for mobile/machine; session for web consoles; signed URLs for media.\n- **Rate limiting** — sensitive routes are throttled (login `5/min`, dispatch/reject `30/min`, presence/claim `60/min`, body-cam `5/min`, etc.).\n\n<a name=\"shared\"></a>\n## Shared & Auth — `/api/v1/{guard}`\n\nA dynamic-guard group (`police-station` | `officer`) handled by one set of controllers via a `resolve.guard` middleware:\n\n| Method | Path | Purpose |\n|--------|------|---------|\n| `POST` | `/{guard}/login` | Authenticate → token + encryption_key. |\n| `POST` | `/{guard}/password/{forgot,verify-code,reset}` | Password-reset flow. |\n| `GET/PUT` | `/{guard}/profile`, `/{guard}/password/change`, `/{guard}/logout` | Profile & session. |\n| `GET` | `/{guard}/crimes`, `/{guard}/crimes/{crime}` | Crime list/detail. |\n| `GET/PUT/DELETE` | `/{guard}/notifications…` | Notification centre. |\n| `POST` | `/{guard}/firebase-token` | Register FCM token. |\n| `GET/POST` | `/{guard}/chat…` | Chat. |\n| `GET` | `/v1/settings/language` | Public app-language setting. |\n\n<a name=\"officer\"></a>\n## Officer Mobile API — `/api/v1/officer` (`auth:officer`)\n\n| Method | Path | Purpose |\n|--------|------|---------|\n| `PUT` | `/crimes/{crime}/accept` \\| `/no-visit` \\| `/resolve` | Crime lifecycle actions. |\n| `GET` | `/crimes/{crime}/brief` | Pre-arrival tactical brief. |\n| `POST` | `/location` | GPS update (Redis-backed). |\n| `GET/PUT` | `/status`, `/status/daily-activity` | Availability + daily summary. |\n| `POST` | `/panic`, `/panic/{panic}/cancel` | Officer-safety SOS. |\n| `POST` | `/crimes/{crime}/body-cam` | Upload body-cam footage. |\n| `POST` | `/chat/voice`, `/chat/quick-reply` | Voice & quick-reply chat. |\n| `GET` | `/bolos/active`, `/task-history` | Active BOLOs, history. |\n\nSee [Crime Lifecycle & Field Ops](/{{route}}/{{version}}/crime-lifecycle).\n\n<a name=\"station-api\"></a>\n## Police Station API — `/api/v1/police-station` (`auth:police_station`)\n\nThe mobile/companion API for stations: officer CRUD + import + password reset + activity; camera CRUD + import + test; crime assignment (`candidate-officers`, `assign`); incident ops (`dispatch`, `reject`, `link`, `nearest-officers`); and `panic/{panic}/resolve`. Mirrors the web console's capabilities over bearer auth.\n\n<a name=\"model\"></a>\n## AI Model API — `/api/v1/model` (`auth:ai_model` + IP + HMAC)\n\n| Method | Path | Purpose |\n|--------|------|---------|\n| `POST` | `/login`, `/logout` | Session (IP-verified). |\n| `GET` | `/cameras` | Assigned cameras (encrypted URLs). |\n| `POST` | `/heartbeat` | Liveness. |\n| `POST` | `/alert`, `/crime` | Report detection (HMAC-signed). |\n\nSee [AI Model Integration](/{{route}}/{{version}}/ai-integration).\n\n<a name=\"camera\"></a>\n## Camera Control API — `/api/camera` (`gateway.ip` + `gateway.hmac`)\n\n~40 POST actions covering privacy, alarm, LED, PTZ/preset/tracking, detection toggles, image, audio, recording/SD, and device control — plus the `/v1/webhooks/camera-tamper` ingress. Only the backend reaches these; see [Cameras & Streaming](/{{route}}/{{version}}/cameras).\n\n<a name=\"station-web\"></a>\n## Station Web Console — `/station` (`police_station_web` | `station_user_web`)\n\nThe Inertia/React dispatcher surface. Pages: dispatcher dashboard (`/`), monitoring, heatmap, crimes, officers, cameras, users, bolos, citizen-tips, chat, notifications, profile. The `/station/api/*` lane backs them with: incidents (`dispatch`/`reject`/`link`/`claim`/`release`/`nearest-officers`), dispatcher presence (`ping`/`leave`), citizen-tips (`promote`/`dismiss`/`reply`/`media`), pattern-alerts, camera control (`alarm`/`move`/`test-connection`), chat, notifications, and HLS stream URLs. Behind `force_password_change` middleware.\n\nSee [Incident & Dispatch](/{{route}}/{{version}}/dispatch) and [Citizen Tips](/{{route}}/{{version}}/citizen-tips).\n\n<a name=\"admin\"></a>\n## Admin Console — `/admin` (`auth:admin`)\n\nPolice-station CRUD + cities + password reset; AI-model CRUD + camera assignment + per-model analytics + comparison; crimes + tracking; heatmap; reports; settings + **priority-weights (dry-run/apply)**; notifications; system **health**; **audit** (+ CSV export); **simulation** trigger; chat; **camera-health** (tamper acknowledge); **camera-gateway** test console.\n\n<a name=\"public\"></a>\n## Public & Webhooks\n\n| Method | Path | Purpose |\n|--------|------|---------|\n| `GET/POST` | `/tip/{stationCode}` | Citizen tip form & submission. |\n| `GET` | `/tip/{tipId}/thanks` | Confirmation. |\n| `POST` | `/api/v1/webhooks/twilio/inbound/{stationCode}` | Twilio SMS intake. |\n| `POST` | `/v1/webhooks/camera-tamper` | Gateway tamper signal. |\n\n<a name=\"media\"></a>\n## Media & Streaming (signed)\n\n| Method | Path | Purpose |\n|--------|------|---------|\n| `GET` | `/gateway/hls/{cameraKey}/index.m3u8` + segments | HLS playback (signed, sessionless). |\n| `GET` | `/media/private-scene/{path}` | Crime scene evidence. |\n| `GET` | `/media/private-tamper-sample/{path}` | Tamper sample frame. |\n| `GET` | `/station/api/citizen-tips/{tip}/media/{index}` | Tip media (relative-signed). |\n| `GET` | `/api/v1/body-cam/{upload}/serve`, `/api/v1/chat-voice/{message}/serve` | Officer media (signed). |\n\nContinue to [Authentication & Security](/{{route}}/{{version}}/security).\n", "html": "<h1 id=\"api-reference\"><a class=\"anchor\" href=\"#api-reference\">#</a>API Reference</h1>\n<hr>\n<p>CrimeLens exposes <strong>345 non-vendor application routes</strong> as discovered on 23 June 2026 across six modules. They divide by audience: machine APIs (AI, camera gateway), mobile APIs (officer, station companion), web consoles (admin, station), and public intake. This page is a navigational map — each group links conceptually to its feature page.</p>\n<ul>\n<li><a href=\"#conventions\">Conventions</a></li>\n<li><a href=\"#shared\">Shared &amp;amp; Auth (<code>/api/v1/{guard}</code>)</a></li>\n<li><a href=\"#officer\">Officer Mobile API</a></li>\n<li><a href=\"#station-api\">Police Station API</a></li>\n<li><a href=\"#model\">AI Model API</a></li>\n<li><a href=\"#camera\">Camera Control API</a></li>\n<li><a href=\"#station-web\">Station Web Console</a></li>\n<li><a href=\"#admin\">Admin Console</a></li>\n<li><a href=\"#public\">Public &amp;amp; Webhooks</a></li>\n<li><a href=\"#media\">Media &amp;amp; Streaming</a></li>\n</ul>\n<h2 id=\"conventions\"><a class=\"anchor\" href=\"#conventions\">#</a>Conventions</h2>\n<ul>\n<li><strong>Response envelope</strong> — most JSON APIs use <code>{ status, data, message }</code>; a few utility endpoints return purpose-specific payloads. Web consoles use Inertia responses.</li>\n<li><strong>Versioning</strong> — mobile/machine APIs are under <code>/api/v1</code>.</li>\n<li><strong>Auth</strong> — bearer (Sanctum) for mobile/machine; session for web consoles; signed URLs for media.</li>\n<li><strong>Rate limiting</strong> — sensitive routes are throttled (login <code>5/min</code>, dispatch/reject <code>30/min</code>, presence/claim <code>60/min</code>, body-cam <code>5/min</code>, etc.).</li>\n</ul>\n<h2 id=\"shared\"><a class=\"anchor\" href=\"#shared\">#</a>Shared &amp; Auth — <code>/api/v1/{guard}</code></h2>\n<p>A dynamic-guard group (<code>police-station</code> | <code>officer</code>) handled by one set of controllers via a <code>resolve.guard</code> middleware:</p>\n<div class=\"table-shell\"><table><thead><tr><th>Method</th><th>Path</th><th>Purpose</th></tr></thead><tbody><tr><td><code>POST</code></td><td><code>/{guard}/login</code></td><td>Authenticate → token + encryption_key.</td></tr><tr><td><code>POST</code></td><td><code>/{guard}/password/{forgot,verify-code,reset}</code></td><td>Password-reset flow.</td></tr><tr><td><code>GET/PUT</code></td><td><code>/{guard}/profile</code>, <code>/{guard}/password/change</code>, <code>/{guard}/logout</code></td><td>Profile &amp; session.</td></tr><tr><td><code>GET</code></td><td><code>/{guard}/crimes</code>, <code>/{guard}/crimes/{crime}</code></td><td>Crime list/detail.</td></tr><tr><td><code>GET/PUT/DELETE</code></td><td><code>/{guard}/notifications…</code></td><td>Notification centre.</td></tr><tr><td><code>POST</code></td><td><code>/{guard}/firebase-token</code></td><td>Register FCM token.</td></tr><tr><td><code>GET/POST</code></td><td><code>/{guard}/chat…</code></td><td>Chat.</td></tr><tr><td><code>GET</code></td><td><code>/v1/settings/language</code></td><td>Public app-language setting.</td></tr></tbody></table></div>\n<h2 id=\"officer\"><a class=\"anchor\" href=\"#officer\">#</a>Officer Mobile API — <code>/api/v1/officer</code> (<code>auth:officer</code>)</h2>\n<div class=\"table-shell\"><table><thead><tr><th>Method</th><th>Path</th><th>Purpose</th></tr></thead><tbody><tr><td><code>PUT</code></td><td><code>/crimes/{crime}/accept</code> \\</td><td><code>/no-visit</code> \\</td></tr><tr><td><code>GET</code></td><td><code>/crimes/{crime}/brief</code></td><td>Pre-arrival tactical brief.</td></tr><tr><td><code>POST</code></td><td><code>/location</code></td><td>GPS update (Redis-backed).</td></tr><tr><td><code>GET/PUT</code></td><td><code>/status</code>, <code>/status/daily-activity</code></td><td>Availability + daily summary.</td></tr><tr><td><code>POST</code></td><td><code>/panic</code>, <code>/panic/{panic}/cancel</code></td><td>Officer-safety SOS.</td></tr><tr><td><code>POST</code></td><td><code>/crimes/{crime}/body-cam</code></td><td>Upload body-cam footage.</td></tr><tr><td><code>POST</code></td><td><code>/chat/voice</code>, <code>/chat/quick-reply</code></td><td>Voice &amp; quick-reply chat.</td></tr><tr><td><code>GET</code></td><td><code>/bolos/active</code>, <code>/task-history</code></td><td>Active BOLOs, history.</td></tr></tbody></table></div>\n<p>See <a href=\"#/docs/crime-lifecycle\">Crime Lifecycle &amp;amp; Field Ops</a>.</p>\n<h2 id=\"station-api\"><a class=\"anchor\" href=\"#station-api\">#</a>Police Station API — <code>/api/v1/police-station</code> (<code>auth:police_station</code>)</h2>\n<p>The mobile/companion API for stations: officer CRUD + import + password reset + activity; camera CRUD + import + test; crime assignment (<code>candidate-officers</code>, <code>assign</code>); incident ops (<code>dispatch</code>, <code>reject</code>, <code>link</code>, <code>nearest-officers</code>); and <code>panic/{panic}/resolve</code>. Mirrors the web console's capabilities over bearer auth.</p>\n<h2 id=\"model\"><a class=\"anchor\" href=\"#model\">#</a>AI Model API — <code>/api/v1/model</code> (<code>auth:ai_model</code> + IP + HMAC)</h2>\n<div class=\"table-shell\"><table><thead><tr><th>Method</th><th>Path</th><th>Purpose</th></tr></thead><tbody><tr><td><code>POST</code></td><td><code>/login</code>, <code>/logout</code></td><td>Session (IP-verified).</td></tr><tr><td><code>GET</code></td><td><code>/cameras</code></td><td>Assigned cameras (encrypted URLs).</td></tr><tr><td><code>POST</code></td><td><code>/heartbeat</code></td><td>Liveness.</td></tr><tr><td><code>POST</code></td><td><code>/alert</code>, <code>/crime</code></td><td>Report detection (HMAC-signed).</td></tr></tbody></table></div>\n<p>See <a href=\"#/docs/ai-integration\">AI Model Integration</a>.</p>\n<h2 id=\"camera\"><a class=\"anchor\" href=\"#camera\">#</a>Camera Control API — <code>/api/camera</code> (<code>gateway.ip</code> + <code>gateway.hmac</code>)</h2>\n<p>~40 POST actions covering privacy, alarm, LED, PTZ/preset/tracking, detection toggles, image, audio, recording/SD, and device control — plus the <code>/v1/webhooks/camera-tamper</code> ingress. Only the backend reaches these; see <a href=\"#/docs/cameras\">Cameras &amp;amp; Streaming</a>.</p>\n<h2 id=\"station-web\"><a class=\"anchor\" href=\"#station-web\">#</a>Station Web Console — <code>/station</code> (<code>police_station_web</code> | <code>station_user_web</code>)</h2>\n<p>The Inertia/React dispatcher surface. Pages: dispatcher dashboard (<code>/</code>), monitoring, heatmap, crimes, officers, cameras, users, bolos, citizen-tips, chat, notifications, profile. The <code>/station/api/*</code> lane backs them with: incidents (<code>dispatch</code>/<code>reject</code>/<code>link</code>/<code>claim</code>/<code>release</code>/<code>nearest-officers</code>), dispatcher presence (<code>ping</code>/<code>leave</code>), citizen-tips (<code>promote</code>/<code>dismiss</code>/<code>reply</code>/<code>media</code>), pattern-alerts, camera control (<code>alarm</code>/<code>move</code>/<code>test-connection</code>), chat, notifications, and HLS stream URLs. Behind <code>force_password_change</code> middleware.</p>\n<p>See <a href=\"#/docs/dispatch\">Incident &amp;amp; Dispatch</a> and <a href=\"#/docs/citizen-tips\">Citizen Tips</a>.</p>\n<h2 id=\"admin\"><a class=\"anchor\" href=\"#admin\">#</a>Admin Console — <code>/admin</code> (<code>auth:admin</code>)</h2>\n<p>Police-station CRUD + cities + password reset; AI-model CRUD + camera assignment + per-model analytics + comparison; crimes + tracking; heatmap; reports; settings + <strong>priority-weights (dry-run/apply)</strong>; notifications; system <strong>health</strong>; <strong>audit</strong> (+ CSV export); <strong>simulation</strong> trigger; chat; <strong>camera-health</strong> (tamper acknowledge); <strong>camera-gateway</strong> test console.</p>\n<h2 id=\"public\"><a class=\"anchor\" href=\"#public\">#</a>Public &amp; Webhooks</h2>\n<div class=\"table-shell\"><table><thead><tr><th>Method</th><th>Path</th><th>Purpose</th></tr></thead><tbody><tr><td><code>GET/POST</code></td><td><code>/tip/{stationCode}</code></td><td>Citizen tip form &amp; submission.</td></tr><tr><td><code>GET</code></td><td><code>/tip/{tipId}/thanks</code></td><td>Confirmation.</td></tr><tr><td><code>POST</code></td><td><code>/api/v1/webhooks/twilio/inbound/{stationCode}</code></td><td>Twilio SMS intake.</td></tr><tr><td><code>POST</code></td><td><code>/v1/webhooks/camera-tamper</code></td><td>Gateway tamper signal.</td></tr></tbody></table></div>\n<h2 id=\"media\"><a class=\"anchor\" href=\"#media\">#</a>Media &amp; Streaming (signed)</h2>\n<div class=\"table-shell\"><table><thead><tr><th>Method</th><th>Path</th><th>Purpose</th></tr></thead><tbody><tr><td><code>GET</code></td><td><code>/gateway/hls/{cameraKey}/index.m3u8</code> + segments</td><td>HLS playback (signed, sessionless).</td></tr><tr><td><code>GET</code></td><td><code>/media/private-scene/{path}</code></td><td>Crime scene evidence.</td></tr><tr><td><code>GET</code></td><td><code>/media/private-tamper-sample/{path}</code></td><td>Tamper sample frame.</td></tr><tr><td><code>GET</code></td><td><code>/station/api/citizen-tips/{tip}/media/{index}</code></td><td>Tip media (relative-signed).</td></tr><tr><td><code>GET</code></td><td><code>/api/v1/body-cam/{upload}/serve</code>, <code>/api/v1/chat-voice/{message}/serve</code></td><td>Officer media (signed).</td></tr></tbody></table></div>\n<p>Continue to <a href=\"#/docs/security\">Authentication &amp;amp; Security</a>.</p>", "headings": [{"level": 1, "title": "API Reference", "id": "api-reference"}, {"level": 2, "title": "Conventions", "id": "conventions"}, {"level": 2, "title": "Shared & Auth — /api/v1/{guard}", "id": "shared"}, {"level": 2, "title": "Officer Mobile API — /api/v1/officer (auth:officer)", "id": "officer"}, {"level": 2, "title": "Police Station API — /api/v1/police-station (auth:police_station)", "id": "station-api"}, {"level": 2, "title": "AI Model API — /api/v1/model (auth:ai_model + IP + HMAC)", "id": "model"}, {"level": 2, "title": "Camera Control API — /api/camera (gateway.ip + gateway.hmac)", "id": "camera"}, {"level": 2, "title": "Station Web Console — /station (police_station_web | station_user_web)", "id": "station-web"}, {"level": 2, "title": "Admin Console — /admin (auth:admin)", "id": "admin"}, {"level": 2, "title": "Public & Webhooks", "id": "public"}, {"level": 2, "title": "Media & Streaming (signed)", "id": "media"}]}, {"slug": "jobs", "title": "Background Jobs & Scheduling", "section": "Reference", "description": "Background Jobs & Scheduling Anything slow, periodic, or fan out runs off the request cycle on Redis queues under Laravel Horizon . This keeps the dispatcher console snappy and the API respo…", "raw": "# Background Jobs & Scheduling\n\n---\n\nAnything slow, periodic, or fan-out runs off the request cycle on **Redis queues under Laravel Horizon**. This keeps the dispatcher console snappy and the API responsive.\n\n- [Queue Topology](#topology)\n- [Queued Jobs](#jobs)\n- [Scheduled Tasks](#schedule)\n- [Why Queue These](#why)\n\n<a name=\"topology\"></a>\n## Queue Topology\n\nHorizon runs several supervisors, each watching a purpose-named queue so heavy work can't starve latency-sensitive work:\n\n| Queue | Carries |\n|-------|---------|\n| `default` | General jobs. |\n| `notifications` | FCM / database notifications. |\n| `broadcasts`, `fcm` | Real-time broadcasts and push. |\n| `incidents` | Incident-pipeline jobs (auto-dispatch). |\n| `scenes` | Evidence extraction (heavy ffmpeg). |\n| `camera-alarms` | Camera alarm triggering. |\n| `reports` | PDF/Excel generation. |\n| `health` | Camera/model health checks. |\n| `maintenance` | Cleanup & purges. |\n\n<a name=\"jobs\"></a>\n## Queued Jobs\n\n| Job | Purpose |\n|-----|---------|\n| `MaybeAutoDispatchIncident` | Evaluate the auto-dispatch policy for a new incident. |\n| `ExtractCrimeScene` | Cut/merge evidence video for a confirmed crime (`scenes`). |\n| `TriggerCameraAlarmJob` | Fire a camera alarm (`camera-alarms`). |\n| `SyncCameraGatewayStreamJob` / `SyncCameraPrivacyJob` / `SyncCameraRecordingJob` | Keep the streaming gateway in sync with camera state. |\n| `CheckCameraHealth` | Ping a camera and update its status. |\n| `NotifyNearbyOfficersForBackup` | Fan a panic event out to nearby officers. |\n| `EscalateStationCrimesJob` | Reassign stale crimes for a station. |\n| `ReleaseStaleDispatcherAssignmentsJob` | Return stale claims to the shared queue. |\n| `GeocodeCitizenTipJob` | Reverse-geocode a citizen tip. |\n| `SendTwilioReplyJob` | Send an SMS reply to a tipster. |\n| `PurgeExpiredCitizenTipsJob` | Delete expired tips and media. |\n| `ProcessDailyOfficerRoutes` | Encode each officer's daily GPS trail (polyline). |\n| `TranscribeChatVoice` | Transcribe an officer voice note. |\n| `GenerateReport` / `GenerateDailyReportJob` | Build analytics reports. |\n| `ProcessExcelImport` / `ImportPoliceStationsJob` / `ImportAiModelsJob` | Bulk imports. |\n| `ExportPoliceStationsJob` / `ExportAiModelsJob` | Bulk exports. |\n| `CleanupOldRecordings` | Delete aged recording segments and evidence (`maintenance`). |\n\n<a name=\"schedule\"></a>\n## Scheduled Tasks\n\n| Cadence | Task | Purpose |\n|---------|------|---------|\n| Every second | `camera:health-check` | Near-real-time camera liveness. |\n| Every second | `model:check-heartbeats` | Flag AI models that stopped pinging. |\n| Every minute | `ReleaseStaleDispatcherAssignmentsJob` | Self-heal stuck claims. |\n| Every 2 min | `camera:gateway:boot` | Re-establish gateway streams. |\n| Every 5 min | `horizon:snapshot` | Queue metrics. |\n| Daily 00:05 | `ProcessDailyOfficerRoutes` | Yesterday's patrol routes. |\n| Daily 02:00 | `ledger:verify` | Verify the decision-ledger hash chain. |\n| Daily 03:00 | `camera:cleanup-recordings`, `PurgeExpiredCitizenTipsJob` | Storage hygiene. |\n| Daily 06:00 | `GenerateDailyReportJob` | Daily crime summary. |\n| Daily | `telescope:prune` | Trim debug telemetry. |\n\n> {warning} `EscalationService`, `EscalateStationCrimesJob`, and the `crime:escalate-stale` command exist, but the command is not currently registered in the scheduler. The every-minute scheduled task releases stale dispatcher claims; it does not escalate stale Crime assignments.\n\n<a name=\"why\"></a>\n## Why Queue These\n\nThree reasons recur:\n\n1. **Latency** — ffmpeg evidence extraction can take tens of seconds; it must never block a dispatch.\n2. **Fan-out** — one panic event notifies many officers; one crime broadcasts to multiple channels.\n3. **External I/O** — Twilio, Firebase, the camera gateway, and geocoding are network calls with their own failure modes, so they run with retries/backoff off the request path.\n\nContinue to [Testing](/{{route}}/{{version}}/testing).\n", "html": "<h1 id=\"background-jobs-scheduling\"><a class=\"anchor\" href=\"#background-jobs-scheduling\">#</a>Background Jobs &amp; Scheduling</h1>\n<hr>\n<p>Anything slow, periodic, or fan-out runs off the request cycle on <strong>Redis queues under Laravel Horizon</strong>. This keeps the dispatcher console snappy and the API responsive.</p>\n<ul>\n<li><a href=\"#topology\">Queue Topology</a></li>\n<li><a href=\"#jobs\">Queued Jobs</a></li>\n<li><a href=\"#schedule\">Scheduled Tasks</a></li>\n<li><a href=\"#why\">Why Queue These</a></li>\n</ul>\n<h2 id=\"topology\"><a class=\"anchor\" href=\"#topology\">#</a>Queue Topology</h2>\n<p>Horizon runs several supervisors, each watching a purpose-named queue so heavy work can't starve latency-sensitive work:</p>\n<div class=\"table-shell\"><table><thead><tr><th>Queue</th><th>Carries</th></tr></thead><tbody><tr><td><code>default</code></td><td>General jobs.</td></tr><tr><td><code>notifications</code></td><td>FCM / database notifications.</td></tr><tr><td><code>broadcasts</code>, <code>fcm</code></td><td>Real-time broadcasts and push.</td></tr><tr><td><code>incidents</code></td><td>Incident-pipeline jobs (auto-dispatch).</td></tr><tr><td><code>scenes</code></td><td>Evidence extraction (heavy ffmpeg).</td></tr><tr><td><code>camera-alarms</code></td><td>Camera alarm triggering.</td></tr><tr><td><code>reports</code></td><td>PDF/Excel generation.</td></tr><tr><td><code>health</code></td><td>Camera/model health checks.</td></tr><tr><td><code>maintenance</code></td><td>Cleanup &amp; purges.</td></tr></tbody></table></div>\n<h2 id=\"jobs\"><a class=\"anchor\" href=\"#jobs\">#</a>Queued Jobs</h2>\n<div class=\"table-shell\"><table><thead><tr><th>Job</th><th>Purpose</th></tr></thead><tbody><tr><td><code>MaybeAutoDispatchIncident</code></td><td>Evaluate the auto-dispatch policy for a new incident.</td></tr><tr><td><code>ExtractCrimeScene</code></td><td>Cut/merge evidence video for a confirmed crime (<code>scenes</code>).</td></tr><tr><td><code>TriggerCameraAlarmJob</code></td><td>Fire a camera alarm (<code>camera-alarms</code>).</td></tr><tr><td><code>SyncCameraGatewayStreamJob</code> / <code>SyncCameraPrivacyJob</code> / <code>SyncCameraRecordingJob</code></td><td>Keep the streaming gateway in sync with camera state.</td></tr><tr><td><code>CheckCameraHealth</code></td><td>Ping a camera and update its status.</td></tr><tr><td><code>NotifyNearbyOfficersForBackup</code></td><td>Fan a panic event out to nearby officers.</td></tr><tr><td><code>EscalateStationCrimesJob</code></td><td>Reassign stale crimes for a station.</td></tr><tr><td><code>ReleaseStaleDispatcherAssignmentsJob</code></td><td>Return stale claims to the shared queue.</td></tr><tr><td><code>GeocodeCitizenTipJob</code></td><td>Reverse-geocode a citizen tip.</td></tr><tr><td><code>SendTwilioReplyJob</code></td><td>Send an SMS reply to a tipster.</td></tr><tr><td><code>PurgeExpiredCitizenTipsJob</code></td><td>Delete expired tips and media.</td></tr><tr><td><code>ProcessDailyOfficerRoutes</code></td><td>Encode each officer's daily GPS trail (polyline).</td></tr><tr><td><code>TranscribeChatVoice</code></td><td>Transcribe an officer voice note.</td></tr><tr><td><code>GenerateReport</code> / <code>GenerateDailyReportJob</code></td><td>Build analytics reports.</td></tr><tr><td><code>ProcessExcelImport</code> / <code>ImportPoliceStationsJob</code> / <code>ImportAiModelsJob</code></td><td>Bulk imports.</td></tr><tr><td><code>ExportPoliceStationsJob</code> / <code>ExportAiModelsJob</code></td><td>Bulk exports.</td></tr><tr><td><code>CleanupOldRecordings</code></td><td>Delete aged recording segments and evidence (<code>maintenance</code>).</td></tr></tbody></table></div>\n<h2 id=\"schedule\"><a class=\"anchor\" href=\"#schedule\">#</a>Scheduled Tasks</h2>\n<div class=\"table-shell\"><table><thead><tr><th>Cadence</th><th>Task</th><th>Purpose</th></tr></thead><tbody><tr><td>Every second</td><td><code>camera:health-check</code></td><td>Near-real-time camera liveness.</td></tr><tr><td>Every second</td><td><code>model:check-heartbeats</code></td><td>Flag AI models that stopped pinging.</td></tr><tr><td>Every minute</td><td><code>ReleaseStaleDispatcherAssignmentsJob</code></td><td>Self-heal stuck claims.</td></tr><tr><td>Every 2 min</td><td><code>camera:gateway:boot</code></td><td>Re-establish gateway streams.</td></tr><tr><td>Every 5 min</td><td><code>horizon:snapshot</code></td><td>Queue metrics.</td></tr><tr><td>Daily 00:05</td><td><code>ProcessDailyOfficerRoutes</code></td><td>Yesterday's patrol routes.</td></tr><tr><td>Daily 02:00</td><td><code>ledger:verify</code></td><td>Verify the decision-ledger hash chain.</td></tr><tr><td>Daily 03:00</td><td><code>camera:cleanup-recordings</code>, <code>PurgeExpiredCitizenTipsJob</code></td><td>Storage hygiene.</td></tr><tr><td>Daily 06:00</td><td><code>GenerateDailyReportJob</code></td><td>Daily crime summary.</td></tr><tr><td>Daily</td><td><code>telescope:prune</code></td><td>Trim debug telemetry.</td></tr></tbody></table></div>\n<aside class=\"callout warning\"><div class=\"callout-icon\">⚠</div><div><strong>Important note</strong><p><code>EscalationService</code>, <code>EscalateStationCrimesJob</code>, and the <code>crime:escalate-stale</code> command exist, but the command is not currently registered in the scheduler. The every-minute scheduled task releases stale dispatcher claims; it does not escalate stale Crime assignments.</p></div></aside>\n<h2 id=\"why\"><a class=\"anchor\" href=\"#why\">#</a>Why Queue These</h2>\n<p>Three reasons recur:</p>\n<ol>\n<li><strong>Latency</strong> — ffmpeg evidence extraction can take tens of seconds; it must never block a dispatch.</li>\n<li><strong>Fan-out</strong> — one panic event notifies many officers; one crime broadcasts to multiple channels.</li>\n<li><strong>External I/O</strong> — Twilio, Firebase, the camera gateway, and geocoding are network calls with their own failure modes, so they run with retries/backoff off the request path.</li>\n</ol>\n<p>Continue to <a href=\"#/docs/testing\">Testing</a>.</p>", "headings": [{"level": 1, "title": "Background Jobs & Scheduling", "id": "background-jobs-scheduling"}, {"level": 2, "title": "Queue Topology", "id": "topology"}, {"level": 2, "title": "Queued Jobs", "id": "jobs"}, {"level": 2, "title": "Scheduled Tasks", "id": "schedule"}, {"level": 2, "title": "Why Queue These", "id": "why"}]}, {"slug": "testing", "title": "Testing", "section": "Reference", "description": "Testing As discovered from the repository on 23 June 2026 , CrimeLens contains 884 Pest test cases across 148 Laravel test files , plus 38 Flutter test files and a small isolated AI client s…", "raw": "# Testing\n\n---\n\nAs discovered from the repository on **23 June 2026**, CrimeLens contains **884 Pest test cases across 148 Laravel test files**, plus **38 Flutter test files** and a small isolated AI-client suite. Every behavioural change is expected to ship with a test.\n\n> {warning} These are discovered test counts, not a claim that every test passed in the latest run. The full Laravel suite requires the PostgreSQL `final-project-testing` database and Redis to be running.\n\n- [Philosophy](#philosophy)\n- [Structure](#structure)\n- [What's Covered](#coverage)\n- [Browser & Smoke Tests](#browser)\n- [Running the Suite](#running)\n- [Quality Gates](#gates)\n\n<a name=\"philosophy\"></a>\n## Philosophy\n\nTests are treated as part of the feature, not an afterthought. The bias is toward **feature tests** that exercise a real HTTP request through the full stack (routing → middleware → controller → service → database), with unit tests reserved for pure logic such as geometry, the priority formula, and the ledger hash chain.\n\n<a name=\"structure\"></a>\n## Structure\n\nEach module owns its tests under `Modules/<Module>/tests/`, split into `Feature/` and `Unit/`, plus app-level `tests/Feature` and `tests/Browser`:\n\n```\nModules/Police/tests/Feature/      → dispatcher, incidents, citizen tips, crimes, officers, panic, BOLO…\nModules/Camera/tests/Feature/      → control, recording, scene extraction, tamper, gateway sync\nModules/AiModel/tests/Feature/     → login, IP/HMAC, encryption, alert→incident, heartbeat\nModules/Admin/tests/Feature/       → stations, AI models, settings, auth, camera-health\nModules/Core/tests/Feature/        → chat, settings, ledger\ntests/Browser/                     → end-to-end smoke flows\n```\n\n<a name=\"coverage\"></a>\n## What's Covered\n\nThe suite concentrates on the critical paths a graduation demo must never break:\n\n- **Auth** — every guard's login/logout/profile, password reset, forced rotation, IP/HMAC enforcement.\n- **Incident → dispatch** — creation from AI/manual/citizen, priority scoring, claim/release race safety, dispatch/reject (single-crime guarantee), supervisory vs operator identity.\n- **Field ops** — assignment by proximity, accept/no-visit/resolve, escalation timeouts, panic + backup, BOLO.\n- **Citizen tips** — web/SMS intake, triage, promote/dismiss/reply, signed media serving, station isolation.\n- **Cameras** — control commands, recording lifecycle, scene extraction (both storage strategies), tamper events, detection filters.\n- **Real-time** — events dispatched on the right channels with the right payloads.\n- **Security** — cross-station 404s, signed-URL validity, ledger immutability/verification.\n\n<a name=\"browser\"></a>\n## Browser & Smoke Tests\n\n`tests/Browser` uses Pest's browser testing (Playwright under the hood) to smoke the React consoles — loading the key station pages and asserting they render without JavaScript errors — catching integration breaks the PHP feature tests can't see.\n\n<a name=\"running\"></a>\n## Running the Suite\n\n```bash\n# Everything (compact output)\nphp artisan test --compact\n\n# A single file or filter\nphp artisan test --compact Modules/Police/tests/Feature/Station/Web/DispatcherPageTest.php\nphp artisan test --compact --filter=claim\n\n# Code style (must pass before finalising)\nvendor/bin/pint --dirty\n```\n\n<a name=\"gates\"></a>\n## Quality Gates\n\nTwo gates guard every change: the relevant **tests** must pass, and **Laravel Pint** must report a clean format. The frontend additionally builds (`npm run build`) to catch TypeScript/JSX breakage. This trio — tests green, Pint clean, build green — is the definition of \"done\" for a change.\n\nThe Vite production build currently succeeds. A separate strict TypeScript `--noEmit` run still reports existing type/declaration issues, so a production release should include both build and type-check gates.\n\nContinue to [Installation & Setup](/{{route}}/{{version}}/installation).\n", "html": "<h1 id=\"testing\"><a class=\"anchor\" href=\"#testing\">#</a>Testing</h1>\n<hr>\n<p>As discovered from the repository on <strong>23 June 2026</strong>, CrimeLens contains <strong>884 Pest test cases across 148 Laravel test files</strong>, plus <strong>38 Flutter test files</strong> and a small isolated AI-client suite. Every behavioural change is expected to ship with a test.</p>\n<aside class=\"callout warning\"><div class=\"callout-icon\">⚠</div><div><strong>Important note</strong><p>These are discovered test counts, not a claim that every test passed in the latest run. The full Laravel suite requires the PostgreSQL <code>final-project-testing</code> database and Redis to be running.</p></div></aside>\n<ul>\n<li><a href=\"#philosophy\">Philosophy</a></li>\n<li><a href=\"#structure\">Structure</a></li>\n<li><a href=\"#coverage\">What's Covered</a></li>\n<li><a href=\"#browser\">Browser &amp;amp; Smoke Tests</a></li>\n<li><a href=\"#running\">Running the Suite</a></li>\n<li><a href=\"#gates\">Quality Gates</a></li>\n</ul>\n<h2 id=\"philosophy\"><a class=\"anchor\" href=\"#philosophy\">#</a>Philosophy</h2>\n<p>Tests are treated as part of the feature, not an afterthought. The bias is toward <strong>feature tests</strong> that exercise a real HTTP request through the full stack (routing → middleware → controller → service → database), with unit tests reserved for pure logic such as geometry, the priority formula, and the ledger hash chain.</p>\n<h2 id=\"structure\"><a class=\"anchor\" href=\"#structure\">#</a>Structure</h2>\n<p>Each module owns its tests under <code>Modules/&amp;lt;Module&amp;gt;/tests/</code>, split into <code>Feature/</code> and <code>Unit/</code>, plus app-level <code>tests/Feature</code> and <code>tests/Browser</code>:</p>\n<div class=\"code-card\"><div class=\"code-toolbar\"><span>text</span><button class=\"copy-btn\" type=\"button\">Copy</button></div><pre><code class=\"language-text\">Modules/Police/tests/Feature/      → dispatcher, incidents, citizen tips, crimes, officers, panic, BOLO…\nModules/Camera/tests/Feature/      → control, recording, scene extraction, tamper, gateway sync\nModules/AiModel/tests/Feature/     → login, IP/HMAC, encryption, alert→incident, heartbeat\nModules/Admin/tests/Feature/       → stations, AI models, settings, auth, camera-health\nModules/Core/tests/Feature/        → chat, settings, ledger\ntests/Browser/                     → end-to-end smoke flows</code></pre></div>\n<h2 id=\"coverage\"><a class=\"anchor\" href=\"#coverage\">#</a>What's Covered</h2>\n<p>The suite concentrates on the critical paths a graduation demo must never break:</p>\n<ul>\n<li><strong>Auth</strong> — every guard's login/logout/profile, password reset, forced rotation, IP/HMAC enforcement.</li>\n<li><strong>Incident → dispatch</strong> — creation from AI/manual/citizen, priority scoring, claim/release race safety, dispatch/reject (single-crime guarantee), supervisory vs operator identity.</li>\n<li><strong>Field ops</strong> — assignment by proximity, accept/no-visit/resolve, escalation timeouts, panic + backup, BOLO.</li>\n<li><strong>Citizen tips</strong> — web/SMS intake, triage, promote/dismiss/reply, signed media serving, station isolation.</li>\n<li><strong>Cameras</strong> — control commands, recording lifecycle, scene extraction (both storage strategies), tamper events, detection filters.</li>\n<li><strong>Real-time</strong> — events dispatched on the right channels with the right payloads.</li>\n<li><strong>Security</strong> — cross-station 404s, signed-URL validity, ledger immutability/verification.</li>\n</ul>\n<h2 id=\"browser\"><a class=\"anchor\" href=\"#browser\">#</a>Browser &amp; Smoke Tests</h2>\n<p><code>tests/Browser</code> uses Pest's browser testing (Playwright under the hood) to smoke the React consoles — loading the key station pages and asserting they render without JavaScript errors — catching integration breaks the PHP feature tests can't see.</p>\n<h2 id=\"running\"><a class=\"anchor\" href=\"#running\">#</a>Running the Suite</h2>\n<div class=\"code-card\"><div class=\"code-toolbar\"><span>bash</span><button class=\"copy-btn\" type=\"button\">Copy</button></div><pre><code class=\"language-bash\"># Everything (compact output)\nphp artisan test --compact\n\n# A single file or filter\nphp artisan test --compact Modules/Police/tests/Feature/Station/Web/DispatcherPageTest.php\nphp artisan test --compact --filter=claim\n\n# Code style (must pass before finalising)\nvendor/bin/pint --dirty</code></pre></div>\n<h2 id=\"gates\"><a class=\"anchor\" href=\"#gates\">#</a>Quality Gates</h2>\n<p>Two gates guard every change: the relevant <strong>tests</strong> must pass, and <strong>Laravel Pint</strong> must report a clean format. The frontend additionally builds (<code>npm run build</code>) to catch TypeScript/JSX breakage. This trio — tests green, Pint clean, build green — is the definition of \"done\" for a change.</p>\n<p>The Vite production build currently succeeds. A separate strict TypeScript <code>--noEmit</code> run still reports existing type/declaration issues, so a production release should include both build and type-check gates.</p>\n<p>Continue to <a href=\"#/docs/installation\">Installation &amp;amp; Setup</a>.</p>", "headings": [{"level": 1, "title": "Testing", "id": "testing"}, {"level": 2, "title": "Philosophy", "id": "philosophy"}, {"level": 2, "title": "Structure", "id": "structure"}, {"level": 2, "title": "What's Covered", "id": "coverage"}, {"level": 2, "title": "Browser & Smoke Tests", "id": "browser"}, {"level": 2, "title": "Running the Suite", "id": "running"}, {"level": 2, "title": "Quality Gates", "id": "gates"}]}, {"slug": "glossary", "title": "Glossary", "section": "Reference", "description": "Glossary A quick reference for the domain terms used throughout CrimeLens. CAD Computer Aided Dispatch — software that manages the lifecycle of an emergency response from report to resolutio…", "raw": "# Glossary\n\n---\n\nA quick reference for the domain terms used throughout CrimeLens.\n\n<a name=\"terms\"></a>\n\n**CAD (Computer-Aided Dispatch)** — software that manages the lifecycle of an emergency response from report to resolution. CrimeLens is an *AI-originated* CAD: the report usually comes from an AI detection rather than a phone call.\n\n**Incident** — a candidate event awaiting a dispatch decision. Created from an AI alert/crime, a citizen tip, or a manual entry. Lives in `pending_review` until a dispatcher dispatches or rejects it. **Not** yet a crime.\n\n**Crime** — a committed response, created when an incident is dispatched. Carries the assigned officer, evidence, and the full response lifecycle.\n\n**Dispatcher** — the human operator who reviews incidents and commits dispatch decisions, working from the Station Console. The role that keeps AI in the loop, not in charge.\n\n**Priority Engine** — the explainable weighted scorer that turns an incident's factors (confidence, crime type, weapon, repeat area, time of day, crowd) into a `priority_score` and a tier.\n\n**Tier** — the operational bucket of a priority score: Critical / High / Medium / Low.\n\n**Auto-Dispatch Policy** — the narrow rule allowing a critical, high-confidence, weapon-bearing incident to skip manual review. Everything else waits for a human.\n\n**Claim / Release** — a dispatcher taking (or returning) personal ownership of an incident, so colleagues can see who is handling what. Race-safe at the database level.\n\n**Citizen Tip** — a public report (web form or SMS) that enters a triage queue. A dispatcher **promotes** it into an incident or **dismisses** it.\n\n**BOLO** — \"Be On the Look-Out\": a station-wide broadcast about a person, vehicle, or object, with an area and an expiry.\n\n**Panic / SOS** — an officer-safety alert that pages nearby officers for backup and raises a blocking modal on the station console.\n\n**Scene** — the recorded video evidence for a crime, cut from camera storage or merged from gateway recording segments.\n\n**Camera Gateway** — the Python (Flask + FFmpeg + MediaMTX) service that fans one camera's RTSP feed into HLS, WebRTC, and pass-through RTSP, and brokers control commands.\n\n**HLS / WebRTC / RTSP** — streaming formats: HLS for resilient mobile/web playback, WebRTC for low-latency monitoring, RTSP as the raw camera source.\n\n**Detection Filter** — a per-camera, per-crime-type confidence threshold that decides whether an AI detection is accepted or filtered.\n\n**Tamper Event** — a detected attempt to blind a camera (blackout, variance collapse, FOV shift, static stream, heartbeat gap).\n\n**Pattern Alert** — an automatically-detected cluster of related incidents (e.g. multiple weapon detections in one area).\n\n**Decision Ledger** — the append-only, hash-chained audit trail of every consequential action; the system's \"defensible record\".\n\n**Heartbeat** — a periodic liveness ping. AI models and cameras that stop pinging are flagged offline.\n\n**Guard** — a Laravel authentication context. CrimeLens runs eight (admin, station web ×2, station/officer/AI mobile-or-machine, plus framework defaults).\n\n**Presence** — the live set of online dispatchers (tracked in Redis), used for \"who's online\" and queue load-balancing.\n\n**Escalation** — reassignment/escalation capability for a Crime whose assigned officer has not responded within the timeout. The command exists; periodic scheduling is not currently enabled.\n\n**Station User vs Police Station account** — an individual dispatcher (`station_user_web`) versus the institutional, supervisory station login (`police_station_web`).\n\n---\n\nThat completes the CrimeLens documentation. Return to the [Overview](/{{route}}/{{version}}/overview).\n", "html": "<h1 id=\"glossary\"><a class=\"anchor\" href=\"#glossary\">#</a>Glossary</h1>\n<hr>\n<p>A quick reference for the domain terms used throughout CrimeLens.</p>\n<p><strong>CAD (Computer-Aided Dispatch)</strong> — software that manages the lifecycle of an emergency response from report to resolution. CrimeLens is an <em>AI-originated</em> CAD: the report usually comes from an AI detection rather than a phone call.</p>\n<p><strong>Incident</strong> — a candidate event awaiting a dispatch decision. Created from an AI alert/crime, a citizen tip, or a manual entry. Lives in <code>pending_review</code> until a dispatcher dispatches or rejects it. <strong>Not</strong> yet a crime.</p>\n<p><strong>Crime</strong> — a committed response, created when an incident is dispatched. Carries the assigned officer, evidence, and the full response lifecycle.</p>\n<p><strong>Dispatcher</strong> — the human operator who reviews incidents and commits dispatch decisions, working from the Station Console. The role that keeps AI in the loop, not in charge.</p>\n<p><strong>Priority Engine</strong> — the explainable weighted scorer that turns an incident's factors (confidence, crime type, weapon, repeat area, time of day, crowd) into a <code>priority_score</code> and a tier.</p>\n<p><strong>Tier</strong> — the operational bucket of a priority score: Critical / High / Medium / Low.</p>\n<p><strong>Auto-Dispatch Policy</strong> — the narrow rule allowing a critical, high-confidence, weapon-bearing incident to skip manual review. Everything else waits for a human.</p>\n<p><strong>Claim / Release</strong> — a dispatcher taking (or returning) personal ownership of an incident, so colleagues can see who is handling what. Race-safe at the database level.</p>\n<p><strong>Citizen Tip</strong> — a public report (web form or SMS) that enters a triage queue. A dispatcher <strong>promotes</strong> it into an incident or <strong>dismisses</strong> it.</p>\n<p><strong>BOLO</strong> — \"Be On the Look-Out\": a station-wide broadcast about a person, vehicle, or object, with an area and an expiry.</p>\n<p><strong>Panic / SOS</strong> — an officer-safety alert that pages nearby officers for backup and raises a blocking modal on the station console.</p>\n<p><strong>Scene</strong> — the recorded video evidence for a crime, cut from camera storage or merged from gateway recording segments.</p>\n<p><strong>Camera Gateway</strong> — the Python (Flask + FFmpeg + MediaMTX) service that fans one camera's RTSP feed into HLS, WebRTC, and pass-through RTSP, and brokers control commands.</p>\n<p><strong>HLS / WebRTC / RTSP</strong> — streaming formats: HLS for resilient mobile/web playback, WebRTC for low-latency monitoring, RTSP as the raw camera source.</p>\n<p><strong>Detection Filter</strong> — a per-camera, per-crime-type confidence threshold that decides whether an AI detection is accepted or filtered.</p>\n<p><strong>Tamper Event</strong> — a detected attempt to blind a camera (blackout, variance collapse, FOV shift, static stream, heartbeat gap).</p>\n<p><strong>Pattern Alert</strong> — an automatically-detected cluster of related incidents (e.g. multiple weapon detections in one area).</p>\n<p><strong>Decision Ledger</strong> — the append-only, hash-chained audit trail of every consequential action; the system's \"defensible record\".</p>\n<p><strong>Heartbeat</strong> — a periodic liveness ping. AI models and cameras that stop pinging are flagged offline.</p>\n<p><strong>Guard</strong> — a Laravel authentication context. CrimeLens runs eight (admin, station web ×2, station/officer/AI mobile-or-machine, plus framework defaults).</p>\n<p><strong>Presence</strong> — the live set of online dispatchers (tracked in Redis), used for \"who's online\" and queue load-balancing.</p>\n<p><strong>Escalation</strong> — reassignment/escalation capability for a Crime whose assigned officer has not responded within the timeout. The command exists; periodic scheduling is not currently enabled.</p>\n<p><strong>Station User vs Police Station account</strong> — an individual dispatcher (<code>station_user_web</code>) versus the institutional, supervisory station login (<code>police_station_web</code>).</p>\n<hr>\n<p>That completes the CrimeLens documentation. Return to the <a href=\"#/docs/overview\">Overview</a>.</p>", "headings": [{"level": 1, "title": "Glossary", "id": "glossary"}]}], "stats": [{"label": "Application Routes", "value": "345", "detail": "non-vendor app routes"}, {"label": "Laravel Tests", "value": "884", "detail": "Pest cases"}, {"label": "Data Tables", "value": "35+", "detail": "PostgreSQL + PostGIS"}, {"label": "Domain Events", "value": "18", "detail": "real-time catalogue"}], "stack": [{"name": "Laravel 13", "desc": "Modular monolith backend architecture"}, {"name": "React + Inertia", "desc": "Admin and station web consoles"}, {"name": "Flutter", "desc": "Officer mobile experience"}, {"name": "PostgreSQL + PostGIS", "desc": "Transactional and spatial data"}, {"name": "Redis + Horizon", "desc": "Queues, cache, presence and GPS"}, {"name": "Pusher + FCM", "desc": "Realtime web and mobile notifications"}, {"name": "Python Gateway", "desc": "Flask, FFmpeg, MediaMTX stream fan-out"}, {"name": "AI Detection Service", "desc": "Constrained machine client with HMAC/IP controls"}], "features": [{"title": "Human-in-the-loop Dispatch", "desc": "AI detections become reviewable incidents before officers are committed."}, {"title": "Explainable Priority Scoring", "desc": "Weighted, auditable scoring surfaces why an incident is urgent."}, {"title": "Live Operations Console", "desc": "Queue, map, officers, cameras, tips and active crimes in one screen."}, {"title": "Camera Evidence Pipeline", "desc": "RTSP streams convert to HLS/WebRTC and evidence-ready scene clips."}, {"title": "Secure Machine APIs", "desc": "Sanctum, IP allow-listing, HMAC signatures and encrypted camera handshakes."}, {"title": "Field Officer Workflow", "desc": "Assignment, accept, arrive, resolve, panic and body-cam flows for mobile responders."}]};
